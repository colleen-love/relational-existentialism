-- `Paper1` — the live-arena Lean root (Spec Series 2).
-- Spec 2.00's unblocked theorem group (T1, P0–P3) is mechanized in `Spec200`;
-- Spec 2.01's two-sorted signature (P4s, PR-J(a), C3, L1) in `Spec201`;
-- Spec 2.01b's transfer theorem and design-ready group in `Spec201b`;
-- Spec 2.02's Reveal, Internal, C1 conditional, and T14 laws in `Spec202`.
import Spec200
import Spec201
import Spec201b
import Spec202
import Spec201c
import Spec201d
