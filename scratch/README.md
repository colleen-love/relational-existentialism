# scratch — live staging

Mid-experiment work. Imports `foundation/` + `theory/`; **nothing imports from here**. Promote stable,
shared material to `theory/` (forked) or `foundation/` (imported) per [`../STRUCTURE.md`](../STRUCTURE.md);
once a result is paper-bound and frozen, it moves to a paper root. Empty at the reorg.
