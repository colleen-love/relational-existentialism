-- `Series2` — the live-arena Lean root (Spec Series 2).
-- Spec 2.0's unblocked theorem group (T1, P0–P3) is mechanized in `Spec20a`;
-- Spec 2.1's two-sorted signature (P4s, PR-J(a), C3, L1) in `Spec21a`;
-- Spec 2.1b's transfer theorem and design-ready group in `Spec21b`;
-- Spec 2.2's Reveal, Internal, C1 conditional, and T14 laws in `Spec22a`.
import Spec20a
import Spec21a
import Spec21b
import Spec22a
import Spec21c
import Spec21d
import Spec23a
