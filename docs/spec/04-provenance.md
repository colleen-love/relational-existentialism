# 04 — Provenance Ledger

> Part of the [Formalization Spec](README.md). *The honest accounting a referee reads first.* Every
> other page argues the theory; this one states, per result, **what is rederived, what is synthesis,
> and what (if anything) is novel** — and names the genuinely open residues so they are found here, by
> us, rather than on page one of a review.
>
> **Paper-one scope.** This ledger was written for the *whole* development. **Paper one**'s kept results
> are the seam→arrow→energy chain (`Mirror`, `Relating`, `Seam`, `SeamBridge`, `Trace`, `KnowingFeeling`,
> `Decoherence`, `Attending`, `SeamForcing`, `Orientation`, `TimeFlow`, `KnowingFromArrow`,
> `RotatingSpectrum`, `BandCoincidence`, `BandFromAxioms`); their provenance is **R / S** —
> rederivation of standard pieces (Lawvere, bisimulation, traced monoidal categories, decoherence-free
> subalgebras, phase-damping channels, spectral decay) with the synthesis being the seam↔energy
> identification. Entries below concerning sparsity, the cosmos/conservation readings, the functorial
> semantics, and the free `Cl(𝕋)` construction pertain to the **archived** later papers and are retained
> here for the full accounting, not as paper-one claims.

---

## 0. The one-sentence claim

> The **mathematical content** of this development is, almost entirely, **rederivation** of established
> results (bisimulation, Lawvere, traced monoidal categories, the graph model, the partial trace,
> Rosen, Banach/Neumann series, Perron–Frobenius, the GoI/`Int` construction). The **contribution** is a
> **synthesis** — a single typed identification across decoherence, fixed-point logic, and relational
> dynamics — together with a set of philosophical **readings** mounted on theorems, and the
> **mechanization artifact** itself (a sorry-free, axiom-audited Lean+Agda corpus). Every bridge this
> ledger once flagged open is now **closed** (Bridge B / the quantum seam, the Conway fixpoint-trace
> instance, the `Int` composition + snake equations and the non-cartesian reflexive object in the
> canonical model, the conjecture lift's spectral structure, decay dynamics, and **Perron–Frobenius
> existence**, and the **cost-graded sparsity** of Conjecture 3.7.3 in its correct, exclusivity-conditioned
> form). The one *structurally new* theorem the framework's commitments promised —
> **orientation-from-the-seam** (the knower→known arrow generated by the lossy idempotent `E`,
> [03.5 §4.3](03.5-decoherence.md)) — is now `[proved]` at its structural core. None of these is new
> mathematics: each is a rederivation of a standard fact or a synthesis on standard pieces.

This is deliberately the *deflationary* reading. We would rather under-claim here — calling a clean
mechanized rederivation a rederivation — than overclaim a theorem and have a reviewer find the seam
unmarked. The residues that genuinely remain are narrow and named in §4.

---

## 1. The three tiers

| Tier | Meaning | What it would take to be more |
| --- | --- | --- |
| **R — Rederivation** | The theorem is established mathematics; we re-prove it (often in a proof assistant, sometimes in a lighter special case) to have it *inside* the corpus with an audited axiom footprint. Novelty: ~0; value: the machine-checked artifact and the uniform setting. | A new theorem, not a re-proof. |
| **S — Synthesis** | No single new theorem, but a non-obvious **identification** or **transport**: the same structure recognized across two domains the literature keeps apart, so that a known fact in one becomes a statement in the other. Novelty: the bridge, not the endpoints. | Showing the identification is *forced* (an equivalence/obstruction), not merely *available*. |
| **N? — Novel-candidate** | A statement that, if it holds at the claimed generality, is not a re-proof of anything we can cite. In this corpus these are now **only** philosophical readings (explicitly lenses, not proofs of the world); the one *mathematical* candidate — the quantum-seam question (Bridge B) — was **resolved** as a firewall dichotomy, i.e. as synthesis, not a new theorem. | Nothing further (the readings are honest as readings; Bridge B is settled). |

A result can be **R in content, S in framing**: the theorem is old, the recognition that it *is* this
phenomenon is the contribution. Most of the corpus is exactly this. We mark such rows `R / S`.

---

## 2. The ledger

Grouped by area. "Prior art" cites the result being rederived or transported; it is representative, not
exhaustive. Lean/Agda names are the audited artifacts (see [`formal/README.md`](../../formal/README.md)).

### 2.1 Identity, bisimulation, the first-person surplus

| Result | Prior art | Tier | Note |
| --- | --- | --- | --- |
| `≈ := νΘ`, coinduction, `≈` an equivalence, shared world `D/≈` | Park, Milner (bisimulation); Aczel (non-well-founded sets); Rutten (universal coalgebra) | **R** | Lean via `OrderHom.gfp` (Knaster–Tarski); Agda via native final-coalgebra copatterns — two independent mechanizations agreeing. |
| `≈ ⊊ ≅` (lived exceeds observed); `≅` observational/trace equivalence | van Glabbeek (linear-time/branching-time spectrum); Hennessy–Milner | **R / S** | The theorem is the spectrum's standard strict inclusion. *Synthesis:* reading the surplus as the **first-person remainder**. |
| `Deterministic ⟹ (≈ ⟺ ≅)` — the surplus *is* branching | Folklore of the spectrum (determinism collapses the hierarchy); Hennessy–Milner under determinism | **R / S** | Mechanized both assistants. *Synthesis:* "you exceed how you appear" is, exactly, a fact about systems that *could have done otherwise*. |
| Relational appearance `≅ₒ` (an other's marginal of you); `≈ ⊆ ≅ ⊆ ≅ₒ`; deterministic *relational* surplus | Coarser-than-observation equivalences are standard; the marginal/quotient is elementary | **S** | The move that corrects the "view from nowhere": the operative outside is a *coupled other*, and a **deterministic** system still has surplus against it (feeling from finite access, not from branching). |

### 2.2 Knowing, the mirror, the seam (Lawvere) — and the reflexive object

| Result | Prior art | Tier | Note |
| --- | --- | --- | --- |
| Lawvere diagonal; no complete self-model; remainder | Lawvere (1969); Yanofsky (survey) | **R** | Core, **0 axioms** — fitting, since incompleteness is the contrapositive of a one-line diagonal. |
| No reflexive object in Set/finite (Cantor); the obstruction for `Bool` | Cantor; Lawvere | **R** | `no_reflexive_object_for_Bool`, **0 axioms**. |
| The seam: `self_cannot_trace_relation` — the one trace a self can't take on itself | Lawvere, specialized | **R / S** | The *theorem* is Lawvere at `Env = Self`. *Synthesis:* identifying it as the relational seam (you cannot hold a complete account of a relation you are inside). |
| **Bridge A** — the abstract Lawvere agent is a *supplied* map (instantiated at set-level forgetting) | — (specialization) | **R / S** | Closes the "is the agent real?" gap; *not new math* (Lawvere + trivial projection lossiness). |
| The duality: reflexive object exists **xor** a settling-refusing observable | Lawvere/Cantor duality | **R / S** | Resolves "route 1": the obstruction **is** the non-existence; a reflexive object only ever *constructs* `Y`, never obstructs. |
| **`Pω` graph model** — `D ≅ [D→D]` as a retract, `Y`, `K`; the internal `Y` by self-application | Plotkin, Scott; Engeler; standard λ-model `Y` | **R / S** | `GraphModel`, `SelfApplication`. The **cartesian** reflexive object (Knaster–Tarski fixpoint, `app (Graph f) = f`); `Y f := app (W f)(W f)`. The construction side; by the duality, orthogonal to the seam. |
| **A non-cartesian reflexive object** — `ℕ ≅ ℕ ⊗ ℕ = ℕ* ⊗ ℕ = [ℕ,ℕ]` in compact-closed `Rel` | Plotkin–Scott / Cantor (the obstruction); the relational λ-model | **R / S** | `ReflexiveCompact.{natReflexive, finite_not_reflexive, rel_no_cloning}`. The **linear/compact** counterpart of `Pω`: no finite object works (`|D|=|D|²`, the Cantor obstruction) but `ℕ` does, in a category with **no copying**. Closes the bridge's reflexive object on the non-cartesian side. |

### 2.3 Decoherence, the firewall, no-cloning, orientation

| Result | Prior art | Tier | Note |
| --- | --- | --- | --- |
| Partial trace on matrices; `⊗` = Kronecker; its defining laws | Standard linear algebra / quantum info | **R** | `MatrixModel`, `PartialTrace`. |
| Decoherence **is** the partial trace; coherence conserved, relocated | Joos–Zeh, Zurek (einselection / reduced density matrix) | **R / S** | `Conservation.decoherence_is_partial_trace`. *Synthesis:* the same map is "forgetting the relation." |
| **The seam on the genuine `ptrace`** (Bridge B, compact face); route 1 firewall-obstructed | Non-injectivity of the partial trace; no-broadcasting (Barnum–Caves–Fuchs–Jozsa–Schumacher); the firewall | **R / S** | `QuantumSeamTrace.{ptrace_collapses_entanglement, no_ptrace_recovery, unresolved_fiber_is_coherence, route1_needs_copy_blocked}`. The seam bites the real `ptrace` via no-broadcasting, *necessarily* the only face (see §4). |
| Firewall: compact-closed + cartesian copying ⇒ **thin**; no-cloning | Folklore (a cartesian compact-closed category is degenerate); Abramsky; Wootters–Zurek, Dieks | **R / S** | `Compact.collapse`, `Compact.no_cloning`, `NoCloning.no_linear_clone`. *Synthesis:* this degeneracy **is** the knowing/feeling firewall. |
| One forgetting: identity-collapse, dephasing, partial trace as one `Coarsening` | — (recognition) | **S** | Each non-injective for one reason (a residue); the unification is the content. |
| Feeling **modeled** as a relational decoherence differential | — | **N? (reading)** | *Consequence-and-consistency, not identification.* The model's dynamics are theorems; "this *is* feeling" is an explicit `[reading]`, the hard-problem residue. |
| Knowing `E` as a **conditional expectation**; the **orientation** it generates | Conditional expectations onto a commutative subalgebra (Tomiyama; Umegaki); idempotence + a strict monovariant ⇒ acyclic direction | **R / S** | `Orientation.{Knowing, dephaseKnowing, knows_antisymm, arrow_strictAnti, no_recovery}`. **The structural core of orientation-from-the-seam** — the one new theorem the four commitments promise: a correlation is symmetric, but the knower→known asymmetry is *generated* by the lossy idempotent `E` (directed + temporal + irreversible, three faces of one operator). The *identification* of the monovariant with relational time stays a `[reading]`. |

### 2.4 Trace, fixed points, functorial semantics — and the `Int` arena

| Result | Prior art | Tier | Note |
| --- | --- | --- | --- |
| Traced symmetric monoidal category (full JSV axioms); `Rel`, matrix instances; coherence; free `Cl(𝕋)` + universal functor | Joyal–Street–Verity; standard | **R** | Typeclass + non-trivial validated models (`Quot.sound`-only for the free objects). |
| Feedback **is** the trace (GoI execution formula); recurrence as `νΦ` | Girard (GoI); Abramsky, Haghverdi–Scott; Hasegawa | **R / S** | `Recurrence.feedback_eq_trace`. *Synthesis:* the AI-domain functor. |
| Cartesian trace ⟺ Conway operator; the **fixpoint-trace `TracedSMC`** on complete lattices | Hasegawa; Hyland; Bloom–Ésik (iteration theories) | **R** | `ConwayTrace` (operator + `pfp`, **rolling/dinaturality**, Bekić **diagonal**) and `DomainFixpoint.domainFixpointTracedSMC` — the genuine multi-object instance, all seven JSV axioms discharged (sliding via heterogeneous `rolling'`, vanishing-II via **product Bekić** `lfp_prod`, superposing via `tr_superpose`). The Hasegawa "only-if" direction in full; `[propext, Quot.sound]`-only. |
| The `Int` / GoI construction — a compact closed category from a traced SMC | Joyal–Street–Verity; Abramsky–Haghverdi–Scott | **R** | `IntConstruction`: the object-level compact arena (objects, two-way homs, tensor/unit, the object **dual** proved involutive/monoidal/unit-fixing) **and the morphism-level dual functor** `IntDualHom` (involutive, identity-preserving, **0 axioms**), on the *abstract* `TracedSMC`. |
| `Rel` is **compact closed**, snake equations + GoI composition | Joyal–Street–Verity; Abramsky–Haghverdi–Scott | **R** | `RelCompact`: self-dual `Rel` with both **zigzag/triangle (snake) identities** `= id`, the `Compact.CompactClosed` name bijection, and the **GoI composition-via-trace** making `Int(Rel)` a **category** (identity + associativity). Closes composition + snake in the canonical model (`[propext, Quot.sound]`). |
| Autocatalytic set = eigenform; (M,R)-system closure = `ν(repair∘metabolize)` | Kauffman; Rosen; Letelier et al. | **R / S** | `Chemistry`, `Biology` — the functors are *definitional* (a self-sustaining set just **is** a fixed point). |
| Self-in-other bounded iff `‖x‖<1`; quantitative eigenform unique | Neumann series; Banach fixed point | **R / S** | `Distribution`. *Synthesis:* the individuation threshold as a spectral/norm condition. |

### 2.5 Nondeterminism, causality, sparsity, and the spectral lift

| Result | Prior art | Tier | Note |
| --- | --- | --- | --- |
| Deterministic entangled whole ⇒ nondeterministic marginal; robust for any global law | Reduced/open-system dynamics; coarse-graining ⇒ stochasticity | **R / S** | `Marginal`, `RelationalMarginal` (all **0 axioms**). *Synthesis:* "relation manufactures openness; the seam forces the marginal view." |
| The missing cause is the other: conditioning restores determinism | Conditional/causal factorization | **R / S** | `Causation.indeterminism_is_unviewed_cause`, **0 axioms**. *Synthesis:* the causal reading. |
| Knowing decoheres (open when unknown, definite when conditioned) | The marginal fact + the trace = σ-move identification | **R / S** | `Causation.knowing_decoheres`. Structural core of "knowing = decoherence"; only *phenomenal* knowing stays a reading. |
| Sparsity counting (`\|Stab\| ≤ β/m`), density `→ 0`, nowhere-dense over the final coalgebra | Pigeonhole; cylinder topology on a coalgebraic state space | **R / S** | `Sparsity`, `SparsityReal`, Agda `selves-nowhereDense`. The form is standard; the *application* to selfhood-rarity is the synthesis. |
| The two posits discharged (`d ≥ 2` structural; `μ` constructed); cost-sharing; the valuation boundary | Strict-chain / well-founded arguments; inclusion–exclusion; order density | **R / S** | `SparsityPosits`, `SparsitySharing` (private-footprint correction), `ValuationBoundary` (dense ⇒ no `ℕ`-valuation). The sparsity residues, closed (see §4). |
| The **spectral picture of `E`**: spectrum `⊆ {0,1}`, peripheral = fixed = classical, invariant trace, **sparse** | Idempotent linear maps; conditional expectations | **R / S** | `Peripheral.{idempotent_eigenvalue, dephase_no_rotating_peripheral, peripheral_sparse}`. The **veto-check** (no rotating peripheral spectrum) for *any* conditional expectation; the peripheral set is `1/n` density. The finite-dim spectral form of "few modes self-sustain." |
| The **decay dynamics**: `T = P + N` (peripheral + subdominant `‖N‖<1`) ⟹ `Tⁿ → P` | Mean-ergodic / spectral-gap folklore | **R** | `SpectralDecay.spectral_decay`. Subdominant modes decay; only the peripheral eigenforms self-sustain — Conjecture 3.7.4's decay mechanism, in any normed ring. |
| **Perron–Frobenius existence** — eigenvalue 1, and a positive **invariant state** | Perron–Frobenius; the stochastic-matrix stationary distribution | **R** | `PerronFrobenius.{exists_invariant_vector, exists_invariant_state}`. A column-stochastic `Φ_c` has eigenvalue 1 (`det(M-1)=0` via the all-ones vector fixed by `Mᵀ`); a nonnegative one has a positive invariant state (the `w=\|v\|` trick — no Brouwer/Cesàro). The relational **weight** of Decision 1, existence discharged in general finite dim. |

---

## 3. Where the contribution actually is

Stripping the rederivations, what remains:

1. **The identification (the synthesis proper).** One typed thread runs through the whole corpus:
   **objectifying knowing = the σ-move = the trace = the partial trace = the cartesian (copyable)
   move**, opposed to **feeling = monoidal coherence = the entangled (compact, no-cloning) move**, with
   the **cartesian/compact firewall** (`Compact.collapse`) as the formal wall between them. Every domain
   functor and every decoherence result is a sighting of this one structure. The sharpest instance is
   **Bridge B**: the partial-trace seam was the candidate for a genuinely new theorem, and the
   resolution turned out to *be* the firewall — the seam bites the real `ptrace` on the no-broadcasting
   face, the Lawvere face is provably ruled out. The work was recognizing which face; not a new
   obstruction.

2. **The one structurally new theorem — orientation-from-the-seam.** `Orientation.lean` proves that a
   symmetric correlation acquires a *direction* (knower→known) purely from the lossy idempotent `E`: the
   same operator yields a directed arrow, a strict temporal monovariant, and irreversibility, three
   faces of one cause (the self cannot occupy both sides of its own knowing). The *content* is
   elementary (idempotent + monovariant ⇒ acyclic direction); the *framing* — that this is where 1.4
   ("the knower knows the known") stops being a slogan and becomes structure — is the contribution. The
   identification of the monovariant with relational *time* stays an explicit `[reading]`.

3. **The readings on theorems.** Feeling-as-decoherence-differential, the seam-as-perpetual-feeling,
   indeterminism-as-unviewed-cause, the first-person surplus as freedom, the monovariant as time. These
   are **lenses**: each sits on a genuine theorem, but the assignment-to-phenomenon is interpretive and
   labeled so. They claim nothing about the world the formalism cannot reach (3.3's typed-out residues —
   valence, the hard problem, freedom — are predicted *unreachable*, and we do not reach them).

4. **The artifact.** A sorry-free, axiom-audited Lean 4 corpus (dependency-free core at **0 axioms**
   for the Lawvere/seam results; mathlib-backed `Scratch` for the analytic/quantum/categorical layer)
   plus an independent Agda coinductive layer, with provenance and residues marked in-line. The
   artifact's value is that the synthesis is *checkable* and its boundaries are *enforced*, not asserted.

---

## 4. The bridges — closed, and the narrow residues that remain

None is papered over. Every bridge this ledger once carried as open is now **closed** (in the canonical
model where a fully-general close needs infrastructure mathlib lacks). What remains is a short, precise
list of residues, each blocked on a *named* gap rather than on vagueness.

| # | Bridge | Where it lives | Status |
| --- | --- | --- | --- |
| **B** | Does the seam bite the **quantum partial trace specifically**? | `QuantumSeamTrace`, `QuantumSeam`, `Compact` | **CLOSED on the compact face; route 1 ruled out.** The genuine `ptrace` collapses an entangled joint and its decohered shadow to one marginal; a self with only its marginal cannot recover the relation; the unrecoverable fiber is the non-broadcastable coherence. The Lawvere face is **firewall-obstructed** (needs cartesian copy, which a non-trivial compact structure provably lacks). Synthesis on standard pieces, **not new mathematics**. |
| **Conway-trace instance** | The **fixpoint**-trace on domains (Hasegawa). | `ConwayTrace`, `DomainFixpoint` | **CLOSED.** `domainFixpointTracedSMC`: the genuine multi-object `TracedSMC` on complete lattices, `⊗ = ×`, fixpoint trace, all seven JSV axioms discharged (vanishing-II via product Bekić `lfp_prod`). The Hasegawa "only-if" direction in full. |
| **`Int` composition + snake** | The compact-closed axioms of the GoI / `Int` construction. | `IntConstruction`, `IntCompose`, `RelCompact` | **CLOSED in the canonical model; the abstract composition now built.** `Rel` is compact closed with both **snake/zigzag identities** `= id`, the **GoI composition-via-trace** makes `Int(Rel)` a category (identity + associativity), and the morphism-level dual functor `IntDualHom` is involutive/identity-preserving (0 axioms). The **abstract composition `IntCompose`** is now a genuine definition on *any* non-strict `TracedSMC` (`sigma1`/`sigma2`/`IntCompose`, explicit associator/braid plumbing, **type-checked, 0 axioms**) — the wiring `IntConstruction` had flagged as *absent* is built; the type-correctness of the composite on a non-strict base *is* the coherence content. *Residue:* the abstract **law-verification** (the JSV/AHS compact-closure theorem) and the mechanized identification of `IntCompose` with `relIntComp` — the laws themselves are discharged concretely in `Int(Rel)`. |
| **Non-cartesian reflexive object** | A linear/compact `D ≅ D* ⊗ D` where `Y` is the trace. | `ReflexiveCompact` | **CLOSED.** In compact-closed `Rel` the internal hom is `[D,D] = D* ⊗ D = D × D`, so a reflexive object is a `D ≅ D × D`: **no finite `D`** (the Cantor obstruction `\|D\|=\|D\|²`) but **`ℕ`** via the pairing bijection, in a category with **no copying** (`rel_no_cloning`) — the linear counterpart of `Pω`. |
| **Sparsity step 2 (cost-sharing)** | Cost-sharing over a poset of couplings. | `SparsitySharing` | **CLOSED, with an honest correction.** Sharing lowers spend; full sharing **breaks** the naive count bound; a **positive private footprint** rescues it. The load-bearing condition is an exclusive cost per self. |
| **Non-unit valuation** | A numeric per-return cost `λ > 1` on a dense lattice. | `ValuationBoundary`, `SparsityPosits` | **CLOSED as a boundary.** A densely-ordered standing lattice admits **no** `ℕ`-valuation; ACC/finite-depth ⇒ a canonical `μ`. The boundary is the order-theoretic dichotomy, not a gap. |
| **The conjecture lift (spectral form, 3.4)** | The structure, dynamics, and existence behind "few modes self-sustain." | `Peripheral`, `PeripheralAlgebra`, `SpectralDecay`, `PerronFrobenius`, [`03.7`](../archive/03.7-sparsity.md) | **CLOSED at finite dimension; the peripheral subalgebra commutativity now closed in any dimension.** The `Φ_c`–`E`–weight reconciliation is **decided** (in the lift's reconciliation, 03.7). The **structure** is proved (`E` spectrum `⊆ {0,1}`, peripheral = fixed = classical, no rotating part, the peripheral set `1/n`-sparse); the **decay dynamics** is proved (`Tⁿ → P`, subdominant decays); the **Perron–Frobenius existence** is proved (eigenvalue 1, and a positive **invariant state** for a nonnegative `Φ_c`); and the **peripheral (fixed-point) subalgebra is commutative** — proved **dimension-independently** (`PeripheralAlgebra.fix_comm`/`dephaseStandardForm`): over *any* ring `𝒜` (so `B(H)` and any C\*-algebra, not just finite matrices) the fixed-point subalgebra of a commutative-image projection is a commutative unital subring, with `E = dephase` the witness. *Residue:* the **analytic** placement of an arbitrary primitive CP map *into* that standard form — its peripheral/modulus-1 spectrum ↦ this commutative subalgebra — in infinite dimension; a refinement that, per the discipline, does not block the result. |
| **The conjecture lift (cost-graded, 3.7.3)** | Sparsity for **all of `Cl(𝕋)`** (the cost-graded lift). | `SparsitySharing`, `SparsityCapstone`, Agda `Sparsity` | **CLOSED, in its correct form.** *Topological:* nowhere-dense + sharp dichotomy (Agda, `selves-nowhereDense`, `trivial→allSelf`). *Cost-graded counting/density/dichotomy:* `SparsityCapstone.conjecture_3_7` — **with** a positive private footprint per self the density of selves `→ 0` (`cost_graded_density_tendsto_zero`); **without** it (full sharing) the count is unbounded within budget. The one correction (`SparsitySharing`): the unconditional "sharing only helps" is *false* — counting sparsity holds iff a finite budget **and** an exclusive cost per self. *Residue:* only the literal instantiation in the free `Cl(𝕋)` (that its couplings carry a private footprint) — exactly the exclusivity the dichotomy isolates. |

The phenomenal residues — valence, the hard problem, freedom — are **not** on this list: 3.3 predicts the
formalism cannot reach them, so their absence is a *result*, not an open bridge.

**The named gaps, in one place.** Every *bridge* this ledger ever carried is now closed, and both
formerly-named gaps have had their build-able core extracted and mechanized; what remains is, in each
case, a single *analytic / law-level* generalization of an already-proved theorem. (i) **Abstract
`Int(C)`:** the composition **operation** is now built — `IntCompose` is a genuine, type-checked,
axiom-free definition on *any* non-strict `TracedSMC` (so the wiring `IntConstruction` flagged as absent
exists); what stays open is the abstract **law-verification**, the full JSV/AHS structural-iso chase
proving `IntCompose` satisfies the compact-closed axioms — with no traced/compact-closed scaffolding in
mathlib to inherit, and the laws themselves discharged concretely in `Int(Rel)`. (ii) **Operator-algebra
standard form:** the **algebra** of the standard form is now closed *in any dimension* —
`PeripheralAlgebra.fix_comm` proves the peripheral (fixed-point) subalgebra of a commutative-image
projection is a commutative unital subring over an arbitrary ring (`B(H)`, any C\*-algebra), with
`E = dephase` the witness; what stays open is the **analytic placement** of an arbitrary primitive CP map
into that form (its modulus-1 spectrum ↦ this commutative subalgebra) in infinite dimension — mathlib has
C\*-algebras but no conditional-expectation / decoherence-free-subalgebra spectral theory. Both residues
are now *law-/analysis-level* generalizations of theorems mechanized in the canonical / finite-dimensional
case (and, for the standard form's commutativity, in *every* dimension), not open conjectures.

---

## 5. How to read a claim in this corpus

- A `[theorem]` tag with a Lean/Agda name and an audited axiom footprint is **checkable** — that is its
  warrant, and it is `R` or `R / S` unless this ledger marks it `N?`.
- A `[reading]` is a **lens on** a theorem: the underlying statement is proved, the interpretation is
  not, and is honest only as interpretation. The load-bearing readings are named in §3.
- An `[open]` / residue entry is **research**: a statement we believe but have not closed at the claimed
  generality. Every *bridge* is now closed; the two residues that remain are *generalizations* of
  already-mechanized theorems, named at the end of §4 — the abstract non-strict `Int(C)` **law-verification**
  (the composition operation `IntCompose` is now built abstractly; the canonical-model `Int(Rel)` laws are
  done) and the **analytic** placement of a general CP map into the standard form (the standard form's
  commutativity is now proved in *every* dimension; the finite-dim Perron–Frobenius existence is done).
  Everything else is closed.

If a future reader finds a claim in these pages stated more strongly than its tier here, **this ledger
is the correction of record.**

---

### Cross-references

- The mechanization index and axiom footprints: [`formal/README.md`](../../formal/README.md).
- The seam and its bridges: [`03.5-decoherence.md`](03.5-decoherence.md).
- The sparsity posits, the spectral lift, and its decisions: [`03.7-sparsity.md`](../archive/03.7-sparsity.md).

→ Back to [the spec index](README.md).
