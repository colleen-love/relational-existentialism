# 03.8 — Energy: the rotating band of self-relating

> Paper one's step 5 (see [`paper-one.md`](paper-one.md) and the [spec index](README.md)), continuing
> [`03.6-time-flow.md`](03.6-time-flow.md). Mechanized in
> [`Scratch/RotatingSpectrum.lean`](../../formal/Scratch/RotatingSpectrum.lean). Status tags as in
> [paper one](paper-one.md).
>
> *(The companion **space** result — the coupling-graph quasi-metric of `Scratch/Space.lean` reading
> "space = coexistence minus connection" off the same generator — is **archived** as paper-two material
> ([`../archive/03.12-universe-and-cosmos.md`](../archive/03.12-universe-and-cosmos.md) and
> `formal/Archive/Scratch/Space.lean`); it is not load-bearing for paper one and is dropped from this
> page. What remains is the **energy** half.)*

---

## The capstone

> **Theorem (energy = the conserved band).** Over ℂ the dynamics of self-relating is the phase-damping
> channel `schur μ` (`M i j ↦ μ i j · M i j`). Each off-diagonal coherence falls in a band by `‖μ i j‖`:
> **transient** (`‖μ‖ < 1`, decays — becomes known), **rotating** (`‖μ‖ = 1, μ ≠ 1`, magnitude conserved
> for all `n` — sustained, never known), **fixed** (`μ = 1`, the held diagonal — knowing). The rotating
> band is the conserved, never-collapsing remainder, and **that is energy**: the conserved oscillating
> charge, reversible, periodic. The real self-adjoint model carries only the dissipative (arrow) band;
> the rotating band that carries energy lives in a sector it provably excludes, reached by extending to ℂ.

## What is proved

`[proved]` (footprint `[propext, Classical.choice, Quot.sound]`):

**The rotating spectrum** ([`RotatingSpectrum.lean`](../../formal/Scratch/RotatingSpectrum.lean)) — the
phase-damping channel `schur μ` splits each edge into three bands by `‖μ i j‖`: **fixed**
(`μ i i = 1`, `schur_fixed`), **rotating** (`‖μ‖ = 1`, magnitude conserved for all `n`, `schur_sustained`),
and **transient** (`‖μ‖ < 1`, `schur_transient_tendsto`). The witness `quarterMul` carries all three at
once, with `E₀₁` a genuine rotating eigen-operator (`phaseChannel_eigen`: `Φ U = i·U`, `rotating_sustained`).

**Energy** — the energy/arrow split is two theorems about one generator (`μ = exp s`): on the rotating
band `Re(log μ) = 0` (no decay, `energy_conserved_generator`) with `Im(log μ) = π/2 ≠ 0` (a nonzero
frequency, `frequency_nonzero`); on the transient band `Re(log μ) < 0` (the dissipative arrow,
`arrow_negative_generator`) — assembled in `energy_arrow_split` / `energy_arrow_spectrum`. Energy as
**frequency** (uniform winding `Φ^n U = i^n·U`, `rotating_winds`) and as **reversibility/recurrence**
(`i⁴ = 1`, so `Φ⁴ U = U`, `rotating_recurs` — a closed periodic orbit, against the transient's monotone
decay).

## Honest scope

- `[reading]` That the conserved modulus-1 band *is* energy and its rotating phase a physical frequency.
- **Two conserved remainders, kept distinct here, identified in [`03.9`](03.9-band-coincidence.md):**
  the *dynamical* (coupling symmetry → rotating spectrum → conserved charge — energy) and the
  *operational* (self-inclusion → the un-attendable seam → the seam-protected remainder, `SeamForcing`).
  That these coincide — *what self-relating conserves beyond the known is exactly energy* — is the next
  step, and is derived from A1–A3.
- `[open]` The CPTP peripheral **structure theorem** (rotating unitaries normalize `Fix`) and that
  `quarterMul` is a bona fide CPTP map; the full `L = −i[H,·] + D` generator split and the `⊗`-additivity
  of `H`.

**Provenance.** `R / S` — phase-damping channels and spectral decompositions rederived; the synthesis is
reading one generator's rotating band as conserved energy. See [04 — Provenance](04-provenance.md).

---

→ Back to [paper one](paper-one.md) · the time half is [`03.6-time-flow.md`](03.6-time-flow.md) · the
coincidence of this band with the seam-protected remainder is
[`03.9-band-coincidence.md`](03.9-band-coincidence.md).
