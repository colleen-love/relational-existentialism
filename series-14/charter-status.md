# Relational Existentialism, Series 14: Charter Status

**The mutable companion to `charter.md`. All progress, revisions, per-workstream status, discharges, and open items live here; the charter stays clean.**

*Series 14 asks what the defect GENERATES, along two axes. Series 13 proved the FIT: the diagonal manufactures imports (`ws2_mint_lands_in_opening`, `ws2_transport_forall`), the manufacture is a Galois connection (`ws3_galois`) and not an equivalence (`ws3_roundtrip_not_identity`, at `bRefActive`), and the imports it cannot manufacture are located up to `≈` and never sorted (`ws4_mint_not_surjective`, `ws4_exclusion_structural`); the computed verdict was DUAL at the flat layer (`ws5_verdict_eq`), TOTAL on the degenerate single-hold carrier (`ws5_verdict_degenerate`). Geometry here means the shape of DISTINCTION-SPACE, the space whose points are differences, never physical spacetime; the charter fixes this at the outset. The DEPTH question: does the defect compound under ITERATION? The flat answer is near-certain and pre-registered as the floor, not the finding: `L := mintL h₀ ∘ readInsp h₀` is an interior operator and `ws3_roundtrip_closure` proves the closure side is the identity up to `≈` two-sidedly, so `L² ≈ L` and the flat tower is constant from step one. The live half is whether REIFICATION un-collapses it, against Series 10's `ws2_growth_is_bookkeeping` (the plain engine cannot see the tower grow) and Series 11's bound. The BREADTH question, new: every Series 13 object is indexed to a self (`mintL h₀`, `readInsp h₀`, `Lab dest h₀`, and even `outWit h₀ h₁` carries `h₀` in its type), so the whole of the fit speaks only of self-indexed difference. Whether any distinction forgets its index (other-to-other difference, no self in the sentence) is the DE-INDEXING question, with PERSPECTIVAL, proved, being the program's own thesis landing in Lean, not a shortfall. The verdict is a PAIR (tower × index), computed independently, never synthesized by a theorem. Series 12 remains the terminus; Series 13 remains the fit; this ledger reopens neither and adds no name. Phase A is complete: the charter, this ledger, and the protocol are written. Phase B (design-all) is next.*

---

## How to read this file

- **Status vocabulary** (charter §6). **Tower axis:** **Generative** (lift exists, strictness witnessed up to `≈⁺`, visibility proved, axis classified) · **Rigid** (lift exists, saturation proved) · **Broken** (no non-vacuous lift; the obstruction precise, which settles Series 13's layer-stability open by refutation and is a full finding) · **Partial** (per-instance or degenerate). **Index axis:** **De-indexed** (a choice-free invariant distinction exhibited, non-trivial) · **Choice-gated** (invariants exist but every construction consumes a section, exhibited as an undischarged named hypothesis) · **Perspectival** (no non-trivial invariant on the carrier of interest, proved: the program's thesis as theorem) · **Partial**. Plus **Refuted** · **Not started**.
- **The verdicts are a PAIR, and no theorem combines them.** The tower can grow while staying perspectival; de-indexed distinctions could exist with no tower at all. The questions are orthogonal and the pairing's interpretation lives in charter §8 prose only.
- **Series 12 is the terminus; Series 13 is the fit; this reopens neither.** Nothing here decides the compass's content, convergence's direction, or the differentiating act.
- **The fork stays open, on every floor and from every angle** (charter §4.a). Reification gives more places to commit the central sin; re-indexing gives more angles. No theorem at any layer, and no invariance fact, may classify an out-of-image import as *given* or *chosen*. If CHOICE-GATED lands, the section the gluing consumes is likewise never classified.
- **No growth by cheap equality** (§4.b). Strictness is tested up to the lifted `labEquiv`, never literal equality; reification manufactures syntactically new terms trivially.
- **No bookkeeping re-hit relabeled** (§4.c, the depth axis's signature risk). Any growth must be visible to the LIFTED CONNECTION's own comparison (`ws4_growth_visible`), or it is Series 10's `ws2_growth_is_bookkeeping` re-proved and called geometry.
- **No exogeneity decay** (§4.d). Reification MOVES the line the mint must live above; exogeneity is a per-layer proof obligation, never inherited.
- **No de-indexing by smuggled choice** (§4.f, the breadth axis's signature risk). A gluing that quietly consumes a global section has performed the differentiating act off the books. If invariants need choice, the theorem must SAY so (outcome CHOICE-GATED), never bury a `Classical.choice` doing structural work.
- **Names are not terms** (§4.e, inherited). Room, volume, dimension, geometry, space, axis, depth, breadth, kind, self, other, given, chosen, consciousness, choice, God, compass: prose and docstrings only.

## Program-level snapshot

| Item | Status |
|---|---|
| **Headline target** | The room, and its two axes. The flat collapse as the pre-registered floor (WS1); the lift of the connection along `reifyStep` with per-layer exogeneity, or its precise obstruction (WS2); the defect tower's strictness or saturation up to `≈⁺`, with each strictness witness classified DEEPER or BESIDE by the lifted order (WS3); the bookkeeping discriminator and the bound (WS4); the de-indexing trichotomy (WS5); the verdict pair computed (WS6). **Not started (Phase B next).** Expected shape, pre-registered (not a target): WS1 is near-certain (the closure round trip does the work); WS2 is the FIRST KNOT, with BROKEN pre-registered and the unsatisfiability of total `IsReify` a known headwind; WS3 is genuinely uncertain in DIRECTION (strict vs saturated) and in AXIS (deeper vs beside); WS5 is the SECOND KNOT, independent of the first. |
| **The two axes** | DEPTH (the tower): does the loss compound across layers, so that a layer-(n+1) defect exceeds the transport of the layer-n one? Time-shaped in the prose reading. BREADTH (the index): does any distinction forget its `h₀`, so that two others can differ from each other with no self in the statement? Kind-shaped in the prose reading. The conjecture that these are two axes of one defect is PROSE and is tested, never assumed: WS3's axis trichotomy exists precisely because iteration might generate breadth on its own. |
| **The first knot** | WS2, the lift. Everything on the depth axis rests on whether the mint, the adjoint, and the connection survive one application of `reifyStep` over the reified carrier, with the lifted orders non-trivial and exogeneity re-proved at the new line. Total `IsReify` is unsatisfiable on the carriers of interest (Series 12's disclosure), so the lift must be built POINTWISE and the design must disclose what that costs. **Not started; BROKEN pre-registered as honorable.** |
| **The second knot** | WS5, de-indexing. Does the family `{Lab dest h \| h : Hold dest}` glue into distinctions invariant under re-indexing? Independent of WS2: the flat family exists with no lift at all, so WS5 lands even under BROKEN. The risk named in §7: if every candidate re-indexing map already consumes data, the action's DEFINITION pre-decides the trichotomy toward CHOICE-GATED before the question is posed. **Not started.** |
| **The carrier** | To be transcribed: from Series 13, the two-region labelled coalgebras and the whole fit (`Insp`, `Lab`, `instLEInsp`, `instLELab`, `mintL`, `readInsp`, `labEquiv`, `outWit`, `ws3_galois`, `ws3_roundtrip_closure`, `ws3_roundtrip_interior`, `ws3_roundtrip_not_identity`, `ws4_mint_not_surjective`, `ws4_exclusion_structural`, `coalg`, `plainOf_coalg_val`); from Series 10, the reification apparatus (`IsReify`, `reifyStep`, `towerN`, `ws2_growth_is_bookkeeping`, `ws2_reify_bisim_embeds`); from Series 11, the self-bounding result. Transcribed, never imported across series. Series 14's OWN additions: one operator fact (`L`, flat idempotence), one lift (the reified carrier with its lifted connection), one tower (the defect tower with transport and axis trichotomy), one action (re-indexing on labels, with invariants). |
| **Relation to the program** | Series 12 measured the door; Series 13 named the hinges. Series 14 asks whether going through again compounds, and whether any difference ever stops being a difference from me. It reopens nothing. Its guaranteed yield, independent of verdict: TWO named opens CLOSE. Series 13's layer-stability open closes by construction (GENERATIVE/RIGID) or refutation (BROKEN); the index question closes by the index verdict. |

## Phase status

| Phase | Name | Status |
|---|---|---|
| A | Charter | **Complete.** `charter.md`, `charter-status.md`, `protocol.md` written. |
| B | Design-all (six `spec/wsNN-design.md` + `spec/README.md`, plus `spec/ws2-lift-design.md` and `spec/ws5-action-design.md`) | **Not started.** |
| C | Blind design-review → `spec/design-review-1.md` | **Not started.** |
| D | Address design findings | **Not started.** |
| E | Build-all (`formal/Series14/wsNN.lean`, `Series14.lean`, `AxiomCheck.lean`) | **Not started.** |
| F | Blind series-review → `spec/series-review-N.md` | **Not started.** |
| G | Address series findings | **Not started.** |
| H | Close (summaries + root README row) | **Not started.** |

*Series 14 adds a BLIND DESIGN REVIEW (Phase C) before any Lean is written, a change from Series 13's shape. The reason is in the ledger's own record: Series 13's two SERIOUS findings both landed on the design (the orders solved for the map), were caught only after the build, and cost a full D→E loop to fix. Two knots (the lift, the action) and two signature risks (the re-hit, smuggled choice) are decidable ON PAPER, and this series pays for that check up front. The canonical run is **B → C → D → E → F → G → H**, with C→D repeated once if the design review returns SERIOUS, and F→G repeated once if the series review does. The anti-loop discipline (protocol §0.2a) is in force at every address phase.*

## Workstream status

*Six workstreams. WS1 the flat collapse (the floor), WS2 the lift (the first knot), WS3 the tower and the axis (the depth payoff), WS4 the discriminator and the bound, WS5 de-indexing (the second knot, the breadth payoff), WS6 the verdict pair and the audit folded in. None started; the summaries below fix the contract each will be judged against.*

### WS1, The flat collapse · *blocking, near-certain, the floor*
**Status: Not started.** Target: `ws1_interior_idempotent : L (L b) ≈ L b` where `L := mintL h₀ ∘ readInsp h₀` and `≈` is `labEquiv h₀`, from the transcribed `ws3_roundtrip_closure` (the closure side is two-sided `≤`, so the residue is recovered exactly) and `ws3_mint_monotone`; and its corollary `ws1_flat_tower_degenerate` (the flat defect tower is constant from step one). This is the FLOOR, proved rather than assumed, and it is the series' framing device: whatever the room is, the flat layer does not contain it, as a theorem. Expected effort: small. **If this FAILS it is a SERIOUS finding against Series 13's WS3 and the series halts to route back**, since an interior operator that is not idempotent would mean the transcribed connection is not what `ws3_galois` says it is.

### WS2, The lift · *the first knot, BROKEN pre-registered*
**Status: Not started.** Target: transcribe the reification apparatus (Series 10's `IsReify`, `reifyStep`, `towerN`; the pointwise realization per Series 12's disclosure that total `IsReify` is unsatisfiable on the carriers of interest), construct the lifted inspection and label types over the once-reified carrier, the lifted mint `mintL⁺` and adjoint `readInsp⁺`, and prove `ws2_galois_lifted : GaloisConnection mintL⁺ readInsp⁺` between lifted orders proved non-trivial, plus `ws2_lift_exogenous` PROVED FRESH (the lifted mint non-recoverable from the ONCE-REIFIED plain relating, because reification moves the line). The design must record what lifting means precisely: does `reifyStep` act on the label positions, the carrier, or both; candidates and rejections. **DISCONNECTED-analogue: if no non-vacuous lift exists, that is BROKEN**, reported with the obstruction precise, and it settles Series 13's layer-stability open by refutation, a full and publishable finding. Blocking for WS3/WS4 only; WS5 proceeds regardless.

### WS3, The tower, the strictness, and the axis · *the depth payoff, GENUINELY uncertain in direction AND axis*
**Status: Not started.** Target: define the defect tower (`D₀` the flat defect in its witnessed form at `bRefActive`; `Dₙ₊₁` the corresponding non-identity fact for the lifted connection over `towerN … n`) and the transport of a layer-n witness to layer n+1 along `reifyStep`. Prove or refute STRICTNESS: `ws3_defect_compounds`, a layer-(n+1) witness not equivalent up to the LIFTED `labEquiv` to the transported layer-n witness. Tested up to `≈⁺`, never literal equality (the cheaper test would make GENERATIVE true by construction and is pre-named as the degenerate move, inheriting Series 13's honest-resolution discipline from its own `≈` retarget). Where strictness lands, prove the AXIS trichotomy in the lifted order: ABOVE the transported witness (**DEEPER**, refinement of the same seam), INCOMPARABLE (**BESIDE**, a lateral decoupling: breadth generated by iteration, and the prose's depth/breadth split fails to be clean in the most interesting way), or BELOW (degenerate, pre-registered as evidence the equivalence was mis-transported, routing back to design). The classification is computed from the order, never chosen. Both DEEPER and BESIDE are findings; the charter conjectures nothing about which falls.

### WS4, The discriminator and the bound · *the depth axis's guard*
**Status: Not started.** Two obligations. FIRST, `ws4_growth_visible`: any WS3 strictness witness must be separated by the LIFTED CONNECTION's own comparison, so the theorem says the adjunction sees what the plain relating cannot, rather than re-counting what Series 10's `ws2_growth_is_bookkeeping` already collapsed. **A growth fact that strips to plain-layer cardinality bookkeeping is the re-hit relabeled and closes as SERIOUS** (charter §4.c); the honest verdict in that case is RIGID. SECOND, owed if and only if GENERATIVE lands: the bound, transposing Series 11's self-limitation (`ws4_tower_bounded` or its refutation). A capped compounding is the strongest available finding; an uncapped one is reported in its direction. Not owed under RIGID or BROKEN.

### WS5, De-indexing · *the second knot, the breadth payoff, independent of WS2*
**Status: Not started.** Target: define the action of re-indexing on labels (the design's FIRST obligation, and the place this workstream can be lost before it begins: what is the honest map `Lab dest h₀ → Lab dest h₁`, is it canonical, or does every candidate consume data), define an INVARIANT distinction as one whose content is preserved by every re-indexing on the carrier of interest, and prove exactly one of: **(a) `ws5_deindexed_exists`** (an invariant distinction constructible without choice, exhibited, proved non-trivial: not `⊤`, not `⊥`, separating two named points); **(b) `ws5_deindexed_choice_only`** (invariants exist but every construction consumes a choice function or global section, with the consumption EXHIBITED as a hypothesis the theorem cannot discharge, which is the given/chosen fork surfacing on a new face and must be left standing, never filled); **(c) `ws5_perspectival`** (no non-trivial invariant distinction exists on the carrier of interest, proved: perspectival structural realism as theorem, the program's own thesis landing in Lean). All three are honorable. (b) is the strangest and the design must treat it with the most care, because it locates the differentiating act inside the gluing problem, which the prose may point at and the core must only locate. The words self, other, cat, plant, kind live in prose and docstrings only.

### WS6, The verdict pair, and the audit folded in · *owns both verdicts, COMPUTED*
**Status: Not started.** Target: two data-level forks and two branching functions, computed never hand-set, inheriting Series 13's WS5 shape (`FitFork` / `verdictOfFit` / `ws5_verdict_eq` / `ws5_dual_witnessed` as the pattern). The TOWER verdict (Generative / Rigid / Broken / Partial) and the INDEX verdict (De-indexed / Perspectival / Choice-gated / Partial), each with a falsifiability triple proving the computed verdict is not the others, and each with both live branches exhibited firing at named carriers where the carrier permits (Series 13's SR2-2 lesson: a verdict conditional on undischarged carrier hypotheses is conditional, and the fix is a named carrier). **The charter pre-registers that NO theorem combines the two verdicts** into a single pronouncement about worlds; the combination is interpretive and lives in charter §8. The audit folds in here: fork-open at every layer and angle, no growth by cheap equality, visibility, no exogeneity decay, no smuggled choice, the strip test aggregated, names-not-terms.

## The result tracker

| Result | WS | Status | Non-circular? |
|---|---|---|---|
| The flat collapse (`L² ≈ L`, the degenerate flat tower) | WS1 | **Not started.** | To be proved from the transcribed `ws3_roundtrip_closure` + `ws3_mint_monotone`, not assumed; it is the FLOOR, pre-registered, and its failure impeaches Series 13's WS3. |
| The lift (`ws2_galois_lifted`, `ws2_lift_exogenous`), or BROKEN | WS2 | **Not started (the first knot).** | Lifted orders to be proved non-trivial; exogeneity a FRESH proof term at the moved line, never inherited from the flat `ws2_mint_exogenous`. |
| Strictness or saturation, with the axis classified | WS3 | **Not started (uncertain in direction and axis).** | Tested up to `≈⁺` with the transport defined and used, never literal equality; the axis computed from the lifted order (DEEPER / BESIDE / degenerate), never chosen. |
| Growth visible to the connection; the bound if owed | WS4 | **Not started.** | Visibility separates this from Series 10's `ws2_growth_is_bookkeeping`: the lifted comparison must distinguish what the plain bisimulation collapses, or the finding is the re-hit. |
| The invariance trichotomy | WS5 | **Not started (the second knot).** | The re-indexing action defined with candidates and rejections recorded; choice-usage EXHIBITED, not buried; (c) PERSPECTIVAL is the program's thesis and its landing is a success, per §4.g. |
| The verdict pair, computed; the fork open on every floor and angle | WS6 | **Not started.** | Two independent forks, two falsifiability triples, named carriers discharging hypotheses; no theorem synthesizes the pair; no import classified anywhere. |

## Open obligations register

1. **The flat collapse (WS1)**, `L² ≈ L` from the transcribed round trips. **Open; near-certain; blocking as the floor.**
2. **The lift (WS2)**, non-vacuous lifted orders, `GaloisConnection` at layer one, `ws2_lift_exogenous` fresh. **Open (the first knot); BROKEN pre-registered.**
3. **The strictness (WS3)**, compounding or saturation up to `≈⁺`, with the transport defined. **Open; genuinely uncertain in direction.**
4. **The axis (WS3)**, each strictness witness classified DEEPER or BESIDE by the lifted order. **Open; genuinely uncertain in axis; the prose conjecture is tested here, not assumed.**
5. **The visibility (WS4)**, growth separated by the lifted connection's comparison, distinct from Series 10 bookkeeping. **Open; the depth axis's signature risk.**
6. **The bound (WS4)**, Series 11 transposed. **Open, owed if and only if GENERATIVE lands.**
7. **The re-indexing action (WS5)**, canonical or choice-consuming, candidates and rejections recorded. **Open; may pre-decide obligation 8 if mishandled.**
8. **The invariance trichotomy (WS5)**, (a) de-indexed / (b) choice-gated / (c) perspectival. **Open (the second knot); all three honorable.**
9. **The verdict pair (WS6)**, both computed, both falsifiable, both witnessed at named carriers. **Open.**
10. **The permanent opens, inherited and untouched**: the content of the compass, the direction of convergence, the differentiating act, and the classification of the out-of-image imports (now to be left open on every floor and from every angle). **Permanently open; this series adds no name.**
11. **The new open, if CHOICE-GATED lands**: whether the section the gluing consumes is given or chosen. Open by the same theorem that requires it. **Contingent; never to be filled.**

## Closed log

*Phase A complete: `charter.md`, `charter-status.md`, `protocol.md` written. The charter fixes the geometry vocabulary at the outset (distinction-space, never spacetime), poses both axes (depth via iteration, breadth via de-indexing), pre-registers the flat collapse as the FLOOR rather than a finding, and pre-registers the verdict as a PAIR with no synthesizing theorem permitted. Two escapes are new and are the axes' signature risks: §4.c (the bookkeeping re-hit relabeled, the depth risk, guarded by `ws4_growth_visible`) and §4.f (de-indexing by smuggled choice, the breadth risk, guarded by the axiom check and the requirement that choice-consumption be an exhibited hypothesis). One escape is new and cuts both ways: §4.g, the two-way relabeling guard on the index axis, because PERSPECTIVAL is the program's own thesis and DE-INDEXED would complicate it, so neither may be softened toward the other in prose; the verdict function decides. The protocol adds a BLIND DESIGN REVIEW before any Lean, on the evidence of Series 13's own ledger: both of that series' SERIOUS findings were design errors caught after the build. Next action: Phase B (design-all).*

---

*No em dashes in final academic copy. The premises presupposed and named: Series 13 whole (the fit: mint, connection, defect, DUAL at the flat layer), Series 10 WS1-WS3 (reification and the bookkeeping collapse), Series 11 (the bound and the re-hit). What is to be proved, not assumed: the flat idempotence, the lift or its precise obstruction, per-layer exogeneity, strictness or saturation with the axis classified, visibility against the bookkeeping guard, and the invariance trichotomy with choice-usage exhibited. What is never proved, by design: which out-of-image imports are given and which chosen, on any floor and from any angle; whether the gluing section, if one is needed, is given or chosen; what fills the opening; anything the terminus or the fit sealed. Series 12 remains the terminus. Series 13 remains the fit. This series is the room and its axes, or the proof that there isn't one, and either way the door stays exactly where it was measured.*
