# T2.03 — The seam

> *Node `T2.03`.* Lean: `RelExist.Seam` (`paper-2/formal/RelExist/Seam.lean`); imports **T2.02** (`Relating`).

The seam is the self-relation a system cannot completely view from outside — the un-attendable block that the
first-person surplus protects. It is the structural heart paper one's headline results route through: the
arrow (T2.13) is the orientation self-relating cannot escape, and the conserved remainder at the seam is
energy (T0.03). Fourth in the spine is **T2.04** (`SeamBridge`).

Narrative: [`paper-two.md`](paper-two.md). Axioms: [`../../theory/spec/AXIOMS.md`](../../theory/spec/AXIOMS.md).
