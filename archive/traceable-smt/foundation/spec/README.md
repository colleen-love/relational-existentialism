# foundation — spec

`foundation/` is overwhelmingly Lean (the traced-SMC typeclass + JSV axioms, `formal/Foundation/Traced.lean`)
and mathlib-bound; this `spec/` holds only what notes the layer needs. Its lifecycle is different from the
rest of the repo — it is the part destined to *leave* for a mathlib PR (not yet). See
[`../../STRUCTURE.md`](../../STRUCTURE.md).
