# T2.09 — Directed attention

> *Node `T2.09`.* Lean: `RelExist.Attending` (`paper-2/formal/Scratch/Attending.lean`); imports **T2.08**
> (`Decoherence`).

Attention is directed self-relation in the matrix model: the channel that decoheres (T2.08) read as an
*attending*. It feeds the seam-protection node (**T2.12**) — the proof that the self-block cannot fully
decohere — and the quantum-seam/orientation chain (T2.10 → T2.11) that yields the arrow (T2.13).

Narrative: [`paper-two.md`](paper-two.md). Axioms: [`../../theory/spec/AXIOMS.md`](../../theory/spec/AXIOMS.md).
