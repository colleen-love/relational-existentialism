# docs/archive — quarantined spec pages (later papers)

These specification pages are **not** part of paper one (see
[`../spec/paper-one.md`](../spec/paper-one.md)). They were moved here by the same rule that scoped the
Lean: *keep what the headline needs; archive the complement.* They are **kept, not deleted** — each is
the prose of a later paper.

- **Paper two — cosmos & the conservation law:** `03.11-seam-permanence`, `03.12-universe-and-cosmos`,
  `03.13-distributed-self`, `03.14-cosmos-knowing-itself`, and the `undifferentiated = knowing + energy`
  conservation framing.
- **Paper three — sparsity:** `03.6-the-self-quantified`, `03.7-sparsity`.
- **Sequel engine — the generative self (growth law):** `03.16-generative-self` — how self-relating makes
  *more* self. The mutual, capacity-limited coupling dynamics (handoffs XI + XI-a), with the verdict
  (generative `[earned]`, conditional and asymmetric; divergence excluded by the capacity bound `α`;
  extraction absent under constant `α`). Lean: `../../formal/Scratch/MutualCoupling.lean`.
- **Functorial semantics:** `04-functorial-semantics`.
- **The full catalog** these were indexed from: `03-theorems`. *(The lived-identity page
  `03.2-lived-identity` has been **restored** to `../spec/` as theorem 3.2 — it is part of paper one.)*

These pages are **quarantined**: their internal cross-links (to each other, to kept `../spec/` pages, and
to Lean modules) are *not* maintained and may be stale. Kept pages reference them only as future work,
never as load-bearing citations. The corresponding Lean lives under
[`../../formal/Archive/`](../../formal/Archive).
