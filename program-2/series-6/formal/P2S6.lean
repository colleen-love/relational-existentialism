/-
`program-2/series-6/formal/P2S6.lean` — Program 2 Series 6 (2.6), THE THREAD (aggregator).

Imports the five workstreams. Series 2.6 proves that STRICT relational identity fails across a tick (WS1, the
trivial ground: the reified successor is plain-bisimilar to its constituent yet rank-lift-separated), forks the
WEAK continuity's recoverability (WS2: recoverable on the merged carrier - WOVEN - an import on the cut carrier -
SEVERED - and genuinely weaker than strict identity), reads the single lived line as the self's linearization
IMPORT (WS3, on Series 2.1's `ws4_linearization_import`, the total order non-recoverable, not scalar rank), and at
the knot draws the fork on the weak continuity (WS4: WOVEN and SEVERED both reachable, the continuity
self-relative, with a companion attention-relative cessation). The verdict is computed (WS5): SHAPE-DRAWN - the
self is at most a thread continuously re-woven, and whether the relating carries the weave is self-relative,
undecidable from within.
-/
import P2S6.ws1
import P2S6.ws2
import P2S6.ws3
import P2S6.ws4
import P2S6.ws5
