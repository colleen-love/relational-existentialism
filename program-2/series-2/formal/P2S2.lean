/-
`program-2/series-2/formal/P2S2.lean` — Program 2 Series 2 (2.2), the OTHER (aggregator root).

Imports the five workstreams: the other as a second attending locus (`ws1`), genuinely two reader-wise (`ws2`),
the four readings asymmetric and partial (`ws3`), the mutual residue (`ws4`), and the computed verdict (`ws5`).
Series 2.2 builds on the `P2S1` ground (and through it, transitively, `P2S0` and the `P1` foundation); it never
reaches `P2S0` or `P1` directly. The `P2S2.AxiomCheck` root runs the axiom pass.
-/
import P2S2.ws1
import P2S2.ws2
import P2S2.ws3
import P2S2.ws4
import P2S2.ws5
