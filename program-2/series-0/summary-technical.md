# Relational Existentialism, Program 2 Series 0 (2.0): Technical Status Summary

*A machine-checked account of the GROUND: relating as finite attending. The carrier is `attends : X → Finset X` (finite out-attention the sole ontological bound, no cardinal ceiling); the reification section lives on the finite functor; the inherited collapse is transcribed from the imported P1 engine; the asymmetry of knowing is proved genuine, non-recoverable, and load-bearing; and the import is seated as a quantified, unnamed ingredient. The verdict is GROUND-ESTABLISHED.*

## 1. The headline

> **Relating IS finite attending: the carrier admits a reification section on the finite functor with no cardinal ceiling, the collapse is inherited unchanged, and the asymmetry of knowing (what I reach for versus what reaches for me) is a genuine structural fact the symmetric relating cannot see, not a label; the import acts, quantified and unnamed.**

Series 0 does NOT relitigate Program 1's collapse. It IMPORTS the P1 foundation (`program-2/formal/P1`, Program 2 permits it) and builds one new carrier and one new distinction on it. Five machine-checked payoffs, one computed verdict:

- **The carrier and reification (WS1).** The attention carrier `attends : X → Finset X` gives the directed knowing (`knows x y := y ∈ attends x`), the symmetric relating (`sym x y := y ∈ attends x ∨ x ∈ attends y`), and the passive in-attention (`attendedBy x := {z | x ∈ attends z}`, possibly infinite). Finite out-attention is viewed as a `PkObj κ`-coalgebra through `finsetToPk`, whose neighborhoods are finite for EVERY `κ`. The reification section is on the FINITE functor: `FinReify attends reify := ∀ s : Finset X, attends (reify s) = s`, the honest analog of P1's `IsReify` (total `IsReify` on `PkObj κ` is unsatisfiable for the finite functor, since finite images cannot equal infinite patterns; disclosed). `ws1_reification_exists` discharges it non-vacuously on a carrier where `Finset X ≃ X` (`Cardinal.mk_finset_of_infinite`, `X := ℕ`), giving a BIJECTIVE section (`Ω ≅ F(Ω)` realized). `ws1_bound_is_finite_attention` proves the ontological bound is finite: `Cardinal.mk ↥((outDest hinf attends x).1) < ℵ₀` for every `x`, uniform in `κ` (audit (a): the bound is ℵ₀, below any infinite `κ`, never `κ` itself). The finite tower `finReifyStep`/`finTowerN`/`ws1_tower_monotone` is the companion the tick S1 will need.
- **The inherited collapse (WS2, baseline, NOT relitigation).** `ws2_collapse_inherited` is the imported `P1.Core.ws2_import_theorem_static` APPLIED to the symmetric relating `symDest`: without the import, a behaviorally-identified atomless symmetric world is a `Subsingleton` (the One). The proof term IS the imported engine; nothing is re-derived. `symDest`'s `hcar : mk X < κ` bounds the possibly-infinite in-attention neighborhoods by ambient carrier size (audit (a)), never an ontological ceiling. No verdict hinges on it (audit (d)).
- **The asymmetry of knowing (WS3, the knot).** The knowing-labelled lift `knowLiftD` tags each symmetric neighbor with the knowing bit; on the two-state passive witness (`a := true` attends `b := false`; `b` attends nothing), the plain reduct `plainOf (knowLiftD)` is the symmetric relating (`a ↦ {b}`, `b ↦ {a}`). `ws3_direction_not_recoverable : ¬ Recoverable (knowLiftD hinf)` proves direction is NOT recoverable from the symmetric relating — the plain bisimulation relating `a`,`b` (both atomless, `ws1_atomless_bisim`) is not a label-bisimulation, since `a` knows `b` (label `true`) while `b` does not know `a` (label `false`) — a DISCHARGED obligation, not an assumption (audit (c)). `ws3_passive_constitution` proves passive constitution real and LOAD-BEARING: `b` is attended-but-not-attending (`a ∈ attendedBy b ∧ attendsD b = ∅`) yet plain-bisimilar to the active `a` and label-separated (`AttentionDistinguishes`, imported from `P1.Reader`), so the directed reader does work the plain relating cannot (audit (b), the central risk; NOT the Series 11 Bookkeeping tag). `ws3_active_passive_distinct` proves active and passive genuinely two: `attendsD a = {b} ≠ ∅ = attendsD b` at plain-bisimilar positions, invisible to `symDest`.
- **The import seated (WS4).** `impLift hinf (f : Bool → Q) i := pkSingle hinf (f i, i)` lays an exogenous label on the symmetric self-loops (P1's free-label shape, generalized to an arbitrary import value-space `Q` and import function `f`). `ws4_import_breaks_baseline` proves that for ALL `Q` and `f`, whenever `f true ≠ f false`, the labelled self-loops are plain-bisimilar yet label-separated: the import breaks the WS2 baseline, quantified over every import, none selected (audit (e)). `ws4_import_quantified` proves it carried WITHOUT being named: the plain projection is constant in the import (`∀ Q f`), and the `id` import (the canonical non-choice) is non-recoverable, a non-vacuity witness.
- **The verdict and the audit (WS5).** `verdict (reif asym imp : Bool) : Outcome` is a total function; `ws5_verdict_eq : verdict true true true = groundEstablished` by `rfl`, with the three flags EARNED by `ws5_flags_justified` (reification exists, direction non-recoverable, the import separates). `ws5_verdict_not_obstructed` / `ws5_verdict_not_partial` (`by decide`) show the function DISCRIMINATES: had a WS1/WS3 obligation fallen short, `verdict` would compute `obstructed`/`partial'`. The collapse (WS2) is NOT an argument. The five audit clauses (a)-(e) are proved propositions: `ws5_audit_no_ceiling`, `ws5_audit_asymmetry_not_label`, `ws5_audit_direction_free`, `ws5_audit_collapse_inherited` (by `rfl`, `ws2_collapse_inherited` IS the imported engine), `ws5_audit_import_quantified`.

## 2. Verification status

- **`sorry`-free and axiom-clean:** no proof-position `sorry`, `admit`, `sorryAx`, or custom `axiom` in `ws1`–`ws5`. Every headline rests only on Mathlib's standard three (`propext`, `Classical.choice`, `Quot.sound`), captured by `P2S0/AxiomCheck.lean`, against **Lean 4 v4.15.0 / Mathlib v4.15.0**. The three `ws5_verdict_*` theorems depend on NO axioms.
- **Closure:** `scripts/gate.sh` covers `program-2/series-0/formal/` (executed, `OK program-2/series-0`) and confirms imports resolve only to `P1`, `P2S0.*`, and Mathlib. Series 0 is NOT standalone: it IMPORTS the P1 foundation (Program 2 permits it), and uses `P1.Core.*` (the collapse engine `ws2_import_theorem_static`, `ws1_atomless_bisim`, `Recoverable`, `plainOf`, `IsBisim`/`IsBisimL`, `mk_singleton_lt`, `mk_subtype_le`) and `P1.Reader.AttentionDistinguishes`.
- **Names-not-terms:** `grep -rniE "\b(love|loved|import|god|choice|self|other|knowing|attention)\b"` over `formal/` hits ONLY docstring/comment prose and the Lean `import` keyword; NO definition, theorem name, or bound content is named for the interpretive content. The carrier names are neutral (`attends`, `knows`, `sym`, `attendedBy`, `knowLift`/`knowLiftD`, `impLift`).
- **Blind design review (Phase C, `spec/blind-seed-C.md`):** ZERO SERIOUS, ZERO REAL, four COSMETIC; no signature unsatisfiable. Each of the eight pointed risks verified clean (WS1 witness constructibility and the `FinReify` reading, WS3 non-recoverability/load-bearing/audit-b, WS4 quantification, WS5 verdict-a-function/audit-d).
- **Blind code review (Phase F, `spec/blind-seed-F.md`):** ZERO SERIOUS, ZERO REAL, two COSMETIC; no theorem's proof FAILS to discharge its signature, no recurrence. The reviewer independently ran the axiom check (standard three; `ws5_verdict_*` axiom-free), the `sorry` grep (prose only), and the names-not-terms grep (all hits prose or the `import` keyword), and traced every proof against the imported P1 definitions, confirming the eight pointed checks: the WS1 section is genuinely bijective and non-vacuous with a κ-free `< ℵ₀` bound; `ws2_collapse_inherited` is the imported engine applied, not re-derived; `plainOf (knowLiftD)` is the symmetric relating (`a ↦ {b}`, `b ↦ {a}`), direction non-recoverability is a discharged proof term, and the asymmetry is a genuine load-bearing reader, not a bare tag (audit (b)); WS4 is genuinely `∀ Q, f` with none selected; and `verdict` is a real discriminating function of the flags with the collapse not an argument (audit (d)). The two COSMETIC notes: F1-C1 (`symDest`'s `hinf` is an unused over-hypothesis kept for interface uniformity; the bound comes from `hcar`), F3-C1 (`knowLiftD`'s labels are hand-wired constants that coincide by value with the `knows` predicate, and `knows` is defined-but-unused, idiomatic exactly as P1's `labelLoop i = {(i,i)}` hand-wires its label). Neither weakens any theorem; both accepted as recorded (no post-review code churn).

## 3. The spine

- **Imported (from `P1.Core` / `P1.Reader`, NOT transcribed):** `PkObj`, `PkMap`, `toPk`, `mk_singleton_lt`, `SReaches`, `SHNE`, `IsBisim`, `BehaviorallyIdentified`, `ws1_atomless_bisim`, `ws2_import_theorem_static` (the collapse engine); `LkObj`, `IsBisimL`, `plainOf`, `Recoverable` (the labelled/import test); `AttentionDistinguishes` (the load-bearing shape).
- **New in Series 0 (`P2S0`):** the carrier `finsetToPk`, `pkSingle`, `knows`, `sym`, `attendedBy`, `outDest`, `symDest`; the finite-functor reification `FinReify`, `ws1_finreify_injective`, `ws1_reification_exists`, `ws1_bound_is_finite_attention`, `finReifyStep`, `finTowerN`, `ws1_tower_monotone`; the inherited collapse `ws2_collapse_inherited`; the knowing-asymmetry `knowLiftD`, `ws3_direction_not_recoverable`, `ws3_passive_constitution`, `ws3_active_passive_distinct`; the import `impLift`, `ws4_import_breaks_baseline`, `ws4_import_quantified`; the verdict `Outcome`, `verdict`, `ws5_verdict_eq`, `ws5_verdict_not_*`, `ws5_flags_justified`, `ws5_audit_*` (a-e).

## 4. The verdict

**GROUND-ESTABLISHED**, computed from the built WS1/WS3/WS4 theorems (`ws5_verdict_eq`), not hand-set, the function shown to discriminate (`ws5_verdict_not_obstructed`, `ws5_verdict_not_partial`). The carrier and reification are built (WS1), the collapse is inherited cleanly (WS2, baseline), the asymmetry of knowing is genuine, non-recoverable, and load-bearing (WS3), and the import is seated and quantified (WS4). The pre-registered alternatives OBSTRUCTED (no reification section on the finite functor, or the asymmetry a mere label) and PARTIAL (per-instance only) were live and are in the verdict function's other branches; GROUND-ESTABLISHED is the residue of the process, not its premise.

The ground is the result: when S1 moves the tick and the other arrives, they stand on relating that is nothing but finite attending, made by what it reaches for and by what reaches for it, and never bounded by anything it did not itself attend.

*Lean 4 v4.15.0 / Mathlib v4.15.0. No em dashes in final academic copy; this working summary is not final copy.*
