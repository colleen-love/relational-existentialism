# Program 2 Series 10 (2.10), Charter Status

**The living ledger. The charter is the fixed bar; this file records what is proved, what is open, and how every SERIOUS finding closed (Fixed or Relabeled, per protocol section 0.2a). It never edits the target to record progress.**

*Current phase: **COMPLETE** (Phase F returned zero SERIOUS; all exit criteria met). Final verdict: **noCore** (NOT-RECOVERED / IRREVERSIBLE) — computed at WS5 (`ws5_verdict_eq : verdict true true true false true = Outcome.noCore := rfl`), never hand-set, confirmed by the blind code review. The built reification tick raises the measure at every scale, the section only decodes, and no measure-preserving bijective sub-dynamics exists inside it though the criterion is satisfiable: the arrow is fundamental. Summaries written (`summary.md`, `summary-technical.md`). This is the second tier-1 probe of Phase 3 (`charter-extension-2.md`) and the most consequential, THE REVERSAL: does the tick have an invertible core — a measure-preserving bijective sub-dynamics one could run backward — or is reification irreversibly one-way at every scale? The verdict is the common root of conservation (Noether needs a reversible symmetry) and of the quantum reconstruction axiom of continuous reversibility. The expected close is IRREVERSIBLE (the arrow fundamental, per Series 2.7's `ws2_tick_raises` / `ws2_residue_free`) — a NOT-RECOVERED that specifies the sharpest fork of the program; REVERSIBLE-CORE (the surprise) and SHAPE-DRAWN are pre-registered. Phase B may begin: Series 2.8 has landed. Runs IN PARALLEL with Series 2.9 (The Cone), the other tier-1 probe, which is independent of it.*

---

## 0. Snapshot

- **Phase:** COMPLETE. Phases A–G run: design (B) → blind design review (C, 2 rounds, closed 0 SERIOUS) → design repair (D) → code (E) → blind code review (F, 0 SERIOUS / 0 REAL / 1 COSMETIC) → no code repair needed (G). Summaries written; ledger finalized. **Precondition:** Series 2.8 landed (FRUSTRATED) — met.
- **Verdict:** **noCore (NOT-RECOVERED / IRREVERSIBLE)** — computed from the built flags `(tickMoves, notInvertible, sectionNotMP, builtHasCore, criterionSat) = (true, true, true, false, true)`. The built reification tick raises the measure at every scale, the section only decodes, and no nonempty measure-preserving bijective sub-dynamics exists inside it, though the criterion is satisfiable — the arrow is fundamental, the sharpest NOT-RECOVERED specification. Pre-registered alternatives coreFound (RECOVERED-CORE, the surprise), shapeDrawn, disconnected, partial' were reachable in the verdict function and not realized. Matches the expected close.
- **Build state:** BUILT and REGISTERED. Namespace `P2S10`, `srcDir ../program-2/series-10/formal`, roots `["P2S10", "P2S10.AxiomCheck"]`, appended to `defaultTargets`; gate line `(P2S8|P2S10)`. Layout: `P2S10.lean` (aggregator) + `P2S10/ws1..ws5.lean` + `P2S10/AxiomCheck.lean`. `lake build P2S10 P2S10.AxiomCheck` and the full default build both succeed.
- **Axiom state / gate state / names grep (Phase E, protocol §6):** ✔ sorry-free; ✔ axiom-clean — every payoff depends only on `propext` / `Classical.choice` / `Quot.sound` (or no axioms: `ws5_verdict_eq`, `ws5_verdict_discriminates`, `ws5_audit_names_not_terms`); ✔ gate-green (`P2S10` imports resolve only to `P2S8` / `P2S10` / Mathlib; S7–S0 / P1 transitive); ✔ names grep clean (the concept-words appear only in docstring/comment prose; no proof term, definition, or discharged obligation carries a forbidden whole-word identifier).
- **Open SERIOUS findings:** none. Phase C closed with a zero-SERIOUS pass; Phase F (blind code review) pending.

## 1. The carrier — the tick's invertibility on the imported chain

**S10 stands on the imported chain** (`program-2/series-8`, namespace `P2S8`, reaching `P2S7` / … / `P1` transitively). Its working material, reached transitively:

| Carrier piece | Where |
|---|---|
| The tick and its reification section (the map under study; `attends ∘ reify = id` on patterns) | `P2S1` (transitive) |
| The reification rank (the measure the tick moves; must be PRESERVED for a genuine reversal) | `P2S7` (transitive) |
| The collapse engine (the identification that makes the tick non-injective) | `P1` / `P2S0` (transitive) |
| The diagonal (`ws2_residue_free`, self-reference as creation — the obstruction to reversal) | `P1.Reader` (transitive) |
| **The INVERTIBILITY question** (measure-preserving bijection), the section-vs-reversal distinction, the core criterion | **built at S10 WS1–WS4 — the crux, de-risked on paper first** |

**Design note (from `charter-extension-2.md` §4, 2.10):** two senses of "invertible" must be held apart — a DECODABLE section (a right-inverse that recovers the pattern but does NOT preserve the measure) versus a genuine dynamical REVERSAL (a measure-preserving bijection one could run backward). Only the higher bar is the physics. The core must be EARNED from the built tick (no inverse smuggled). This series inherits Series 2.7's exact lesson (a genuine invariance vs a look-alike). IRREVERSIBLE is a pre-registered honorable NOT-RECOVERED and the expected, most consequential close.

## 2. Targets (all OPEN until built and reviewed)

| WS | Target theorem(s) | Status | Closed how |
|----|-------------------|--------|-----------|
| WS1 (the ground) | `ws1_tick_moves` (the tick changes the configuration and raises its measure — there is something to reverse) | ✔ BUILT | — |
| WS2 | `ws2_tick_not_invertible` (the full tick is not a measure-preserving bijection: non-injective under the collapse AND raises the built rank) | ✔ BUILT | — |
| WS3 (the crux) | `ws3_section_not_measure_preserving` (charter label `ws3_section_is_not_reversal`; the section decodes but does not preserve the measure — decodability ⊊ reversibility), `ws3_core_criterion` (a genuine core = a rank-preserving bijective tick-restriction; entails the higher bar AND is satisfiable) | ✔ BUILT | — |
| WS4 (the knot) | `ws4_core_reachable` (charter label `ws4_reversible_core_reachable`; a measure-preserving bijective sub-dynamics exists on the built rank) / `ws4_no_core_built` (charter label `ws4_irreversible_reachable`; the BUILT tick has no such core) — both tested, same rank | ✔ BUILT | — |
| WS5 | `ws5_verdict_eq (= noCore)`, `ws5_verdict_discriminates`, `ws5_flags_justified`; audits (a) `ws5_audit_no_smuggle`, (b) `ws5_audit_fork_genuine`, (c) `ws5_audit_decode_not_measure_preserving`, (d) `ws5_audit_measure_is_built_rank`, (e) `ws5_audit_names_not_terms` | ✔ BUILT | — |

**Charter-label → built Lean-name map (neutral names, audit e; deviations §5):** `ws3_section_is_not_reversal` → `ws3_section_not_measure_preserving`; `ws4_reversible_core_reachable` → `ws4_core_reachable`; `ws4_irreversible_reachable` → `ws4_no_core_built`; verdict outcomes REVERSIBLE-CORE / IRREVERSIBLE → `coreFound` / `noCore`.

## 3. Audit clauses (protocol §0; UNVERIFIED until Phase F)

- (a) NO INVERSE SMUGGLED — `ws5_audit_no_smuggle`: the map tested in the criterion IS the built `tick` (`tick = fun x => reifyM {x}`, `rfl`), and it has no nonempty core (`ws4_no_core_built`). BUILT; pending Phase F confirmation. **The clause the series lives or dies on.**
- (b) THE FORK NOT BY FIAT — `ws5_audit_fork_genuine`: a core is reachable (`tickR`, `{e0,e0'}`) AND the built tick has none, both against the same built `mu`. BUILT; pending Phase F.
- (c) DECODABILITY IS NOT REVERSAL (the costume gate) — `ws5_audit_decode_not_measure_preserving`: `attendsM (reifyM {e0}) = {e0}` (decodes) yet `mu (reifyM {e0}) ≠ mu e0` (not measure-preserving); the verdict rests on `IsCore` (the higher bar). **The exact Series 2.7 lesson.** BUILT; pending Phase F.
- (d) THE MEASURE IS THE BUILT RANK — `ws5_audit_measure_is_built_rank`: `mu = P2S7.rankM` (`rfl`), the rise is `P2S7.ws2_tick_raises.1`. BUILT; pending Phase F.
- (e) NAMES-NOT-TERMS — `ws5_audit_names_not_terms` (`True` placeholder) + the §6 grep (the teeth), run clean at Phase E (prose only). BUILT; pending Phase F.

## 4. Findings ledger (Phases C, F, and any Tier-1 landing review)

Phase C and Phase F findings are recorded here with stable IDs, grades, and closure (Fixed / Relabeled per §0.2a). A Tier-1 landing-review finding is recorded with a `T1-` id.

**Phase C — round 1 (blind design review).** The reviewer confirmed the mathematical crux sound: `ws4_no_core_built` (no nonempty measure-preserving bijective sub-dynamics inside the built tick) is genuinely provable and non-vacuous; the fork is not a fiat (`IsCore` satisfiable by `tickR` / `{e0,e0'}`, unsatisfiable by the built `tick`, both against the same built rank `mu`); the verdict rests on the measure-preserving bijection (`IsCore`), not the decodable right-inverse (`attendsM`); audits (a)–(d) discharged.

| ID | Grade | Location | Defect | Closure |
|----|-------|----------|--------|---------|
| C1-S1 | SERIOUS | `ws5_audit_decode_not_reversal` (name) | The theorem NAME embedded the forbidden whole-word "reversal" (audit e). | **Fixed** — renamed to `ws5_audit_decode_not_measure_preserving` (named for the measure, not the interpretive word). The theorem's body is self-standing plain fact; only the label changed. |
| C1-S2 | REAL | `ws5_audit_names_not_terms : True` | The names-audit is a `True` placeholder that proves nothing and did not itself catch C1-S1. | **Accepted (house convention)** — names-not-terms is enforced by the protocol §6 mechanical grep, not by a Lean proposition (identical to `P2S7`/`P2S8`). With C1-S1 fixed the grep is clean; the placeholder is documented as such. |
| C1-S3 | COSMETIC | `ws3_core_criterion` conjuncts | First conjunct is a projection of `IsCore`'s clause (4); second duplicates `ws4_core_reachable`. | **Accepted** — kept as intentional documentation that the criterion entails the higher bar and is satisfiable; does no independent work but is not an overclaim. |
| C1-S4 | COSMETIC | `ws5_verdict_discriminates`, `ws5_flags_justified` | Stated as prose in the seed; to be fully typed in the build. | **Fixed at build** — typed as Props in `formal/`, discharged by `decide`/the WS payoffs. |

**Also caught in Phase D (build de-risking, not a review finding).** A prototype compile against the imported build surfaced that `Decidable (IsCore t m D)` is not auto-synthesized (it is a `def`, semireducible). Added `instance decIsCore … := by unfold IsCore; infer_instance` to the shared objects so `ws4_no_core_built` / `ws4_core_reachable` discharge by `decide`. The full prototype (all WS1–WS5 payoffs, including the `P2S7.ws2_tick_raises` citation for audit d) compiled clean (`lake env lean`, exit 0) before `formal/` was written.

**Phase C — round 2 (blind design review, repaired seed).** Returned **0 SERIOUS** — the C→D loop exits. The reviewer re-verified by hand: `ws4_no_core_built` TRUE (the measure-preservation clause holds only for `e0'`, which fails closure, so no nonempty core), `{e0,e0'}` satisfies `IsCore tickR mu`, the fork is genuine against the same `mu`, and audits (a)–(e) pass (names clean, strip test clean, verdict rests on `IsCore` not `attendsM`).

| ID | Grade | Location | Defect | Closure |
|----|-------|----------|--------|---------|
| C2-S1 | REAL | `ws3_core_criterion` conjunct 1 | Was a tautological projection of one `IsCore` clause. | **Fixed** — restated to entail the FULL higher bar (rank-preservation AND injectivity ⇒ a measure-preserving bijection). The verdict's `criterionSat` flag is earned by `ws4_core_reachable` (the satisfiability fact), not by this documentation lemma, so `shapeDrawn` remains reachable and `ws5_verdict_discriminates` holds. |
| C2-S2 | REAL | `ws5_flags_justified` | Prose in the seed; not yet a typed proposition. | **Fixed at build** — typed as a conjunction of the exact WS1–WS4 statements in `formal/P2S10/ws5.lean`, discharged from the WS payoffs. |
| C2-S3 | COSMETIC | `ws5_verdict_discriminates` | Prose placeholder. | **Fixed at build** — typed and discharged by `decide` (all five outcomes reached). |
| C2-S4 | COSMETIC | `ws5_audit_names_not_terms : True` | Vacuous. | **Accepted (house convention)** — the §6 grep is the teeth. |
| C2-S5 | COSMETIC | duplication across `ws3_core_criterion` / `ws4_core_reachable` / audits | Harmless documentation overlap. | **Accepted.** |
| C2-S6 | COSMETIC | `IsCore` clause 1 | `(∀ x ∈ D, t x ∈ D)` is implied by `(D.image t = D)`. | **Accepted** — kept for the readable closed+surjective+injective formulation. |

Phase C is CLOSED (a pass returned zero SERIOUS). Proceeding to Phase E (code).

**Phase F (blind code review).** The reviewer read only `spec/blind-seed-F.md` and the `formal/` sources (confirmed no forbidden file), judged whether the code proves each signature, ran the strip test and the names grep, verified the crux by hand on `Fin 4`, and confirmed audits (a)–(e). Result: **0 SERIOUS / 0 REAL / 1 COSMETIC.** Every target theorem proves its exact signature (no `sorry`, no `axiom`, no weakening, no hidden hypothesis); `ws4_no_core_built` verified genuinely true by hand; `{e0,e0'}` verified a genuine `IsCore tickR mu`; the `noCore` verdict rests on the measure-preserving bijection `IsCore`, not on the decodable right-inverse `attendsM`; no inverse smuggled; `mu = rankM`; names clean.

| ID | Grade | Location | Defect | Closure |
|----|-------|----------|--------|---------|
| F1-C1 | COSMETIC | `ws5_audit_names_not_terms : True` | `True` placeholder for the grep-enforced names audit. | **Accepted (house convention)** — explicitly permitted by the rubric; the §6 grep it stands for passes clean. |

**Phase G:** no SERIOUS or REAL findings — no code repair required. The verdict is unchanged. Series COMPLETE.

## 5. Deviations from the charter

- **Neutral Lean names for three charter labels (audit e, not a narrowing).** The charter names its targets interpretively (`ws3_section_is_not_reversal`, `ws4_reversible_core_reachable`, `ws4_irreversible_reachable`), but the names-not-terms rule forbids the words "reversal"/"reversible" as whole-word identifiers. The built theorems carry neutral names for the SAME statements: `ws3_section_not_measure_preserving`, `ws4_core_reachable`, `ws4_no_core_built` (map in §2). This is a naming discipline, not a change of target; each built statement matches its charter target.
- **No narrowing of content.** Every WS1–WS5 target is built at full strength on the imported carrier (`P2S7.MCar`) and the built rank (`P2S7.rankM`). The `IsCore` predicate carries a redundant closure clause (implied by the image-equality clause) for readability; this strengthens, not weakens, the criterion.

## 6. Permanent opens (inherited, untouched)

Program 1's four permanent opens stand: the content of the compass, the direction of convergence, the differentiating act, the classification of the out-of-image imports. Series 2.10 adds none and closes none. Landing IRREVERSIBLE (`noCore`), its question joins the standing catalogue of what the ontology does not yet own, and hands the next iteration its sharpest specification: to recover our universe, add a reversible substrate beneath the tick (the arrow emergent, as our physics has it), or commit to fundamental irreversibility and recover physics statistically (the causal-set route). Series 2.10 proves that one of these is now required; it does not choose between them.

## 7. Series log

- **2026-07-21 — Series 2.10 (The Reversal) COMPLETE. Verdict noCore (NOT-RECOVERED / IRREVERSIBLE), computed and blind-confirmed.** Phase F (blind code review) returned 0 SERIOUS / 0 REAL / 1 COSMETIC (the accepted `True` names placeholder); no code repair needed (Phase G empty). All exit criteria met: a Phase F pass returned zero SERIOUS; the build is sorry-free, axiom-clean (standard three), gate-green; the names grep is clean (prose only); the WS5 verdict computes `noCore` from the built theorems; every SERIOUS finding (only C1-S1) closed Fixed. The tick has no invertible core: it raises the built rank at every scale, the section only decodes, and no measure-preserving bijective sub-dynamics exists inside it though the criterion is satisfiable — the arrow is fundamental, the sharpest NOT-RECOVERED specification (add a reversible substrate, or recover physics statistically). Summaries `summary.md` / `summary-technical.md` written. Runs in parallel with Series 2.9 (The Cone).
- **2026-07-21 — Phase E (Code) complete; verdict computed noCore (NOT-RECOVERED / IRREVERSIBLE).** Built `formal/P2S10.lean` + `P2S10/ws1..ws5.lean` + `P2S10/AxiomCheck.lean` to the post-repair signatures, in namespace `P2S10`, importing `P2S8` only (S7–S0 / P1 transitive). Registered in `lake/lakefile.toml` (roots, `defaultTargets`) and `scripts/gate.sh` (`(P2S8|P2S10)`). On the imported carrier `Cfg = P2S7.MCar` and built rank `mu = P2S7.rankM`, the tick `tick x = reifyM {x}` genuinely moves and raises the rank (WS1); is non-injective (`tick e0' = tick e2 = e0`) and rank-raising, so not a measure-preserving bijection (WS2); the section decodes (`attendsM (reifyM {e0}) = {e0}`) but does not preserve the rank (WS3.i), and `IsCore` states the higher bar and is satisfiable via `tickR`/`{e0,e0'}` (WS3.ii); the built tick has NO nonempty measure-preserving bijective core (`ws4_no_core_built`, by `decide` over the 16 subsets) though the criterion is reachable (`ws4_core_reachable`) (WS4); the verdict computes `noCore` (`ws5_verdict_eq`, `rfl`), discriminating, flags earned from WS1–WS4, five audits (a)–(e) discharged. Mechanical checks (§6): sorry-free, axiom-clean (standard three or none), gate-green, names grep clean (prose only). Full default `lake build` succeeds (no regression). Phase F (blind code review) next. Also closed the Phase C round-2 REAL findings: C2-S1 (strengthened `ws3_core_criterion` to the full higher bar), C2-S2 (`ws5_flags_justified` fully typed and discharged).
- **2026-07-21 — Phase B (Design) complete.** Wrote `spec/reversal-derisking.md` (the paper gate) and `spec/ws1..ws5-design.md` + `spec/README.md`, committed as one batch before any `formal/` file. The design fixes the shared objects on the IMPORTED carrier and rank: `Cfg := P2S7.MCar`, `mu := P2S7.rankM` (the built rank — audit d), `tick := fun x => reifyM {x}` (the built reification map), a control `tickR` (the rank-0 swap), and the decidable core criterion `IsCore` (a rank-preserving bijective sub-dynamics — the higher bar). The paper hunt settled both cruxes: (i) the section decodes (`attendsM (reifyM {e0}) = {e0}`) but does not preserve the rank (`rankM (reifyM {e0}) = rankM e0 + 1 ≠ rankM e0`) — decodability ⊊ reversibility; (ii) the honest search finds NO nonempty measure-preserving bijective sub-dynamics inside the built tick (its only bijective sub-dynamics, the 3-cycle `e0→e1→e2→e0`, reads rank `0,1,2` and cannot preserve it), while the criterion IS satisfiable on the built rank (`tickR`, `{e0,e0'}`) so the fork is not a fiat. Targets mapped to neutral Lean names (audit e): `ws3_section_is_not_reversal` → `ws3_section_not_measure_preserving`, `ws4_reversible_core_reachable` → `ws4_core_reachable`, `ws4_irreversible_reachable` → `ws4_no_core_built`; verdict outcomes `noCore` (NOT-RECOVERED / IRREVERSIBLE) / `coreFound` (RECOVERED / REVERSIBLE-CORE) / `shapeDrawn` / `disconnected` / `partial'`. Expected computed verdict: `noCore`. Phase C (blind design review) next.
- **2026-07-21 — Series 2.10 (The Reversal) chartered.** Tier 1 authored `series-10/charter.md` / `charter-status.md` / `protocol.md`: the second tier-1 probe of Phase 3, the most consequential. Knot: does the tick have an invertible core — a measure-preserving bijective sub-dynamics one could run backward — or is reification irreversibly one-way at every scale? The verdict is the common root of conservation (Noether) and of the quantum reversibility axiom (the reconstruction theorems). Two senses of invertible must be held apart: a decodable section (right-inverse, does not preserve the measure) vs a genuine dynamical reversal (measure-preserving bijection) — only the higher bar is the physics; the core must be earned, not smuggled. Inherits Series 2.7's lesson (genuine invariance vs look-alike). Pre-registered: REVERSIBLE-CORE (the surprise), IRREVERSIBLE (the expected NOT-RECOVERED specification), SHAPE-DRAWN. Imports `P2S8`, namespace `P2S10`. Phase B begins now that 2.8 has landed; runs in parallel with Series 2.9 (The Cone). Handoff to a fresh Tier-2 executor pending.
