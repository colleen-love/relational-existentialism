/-
`program-2/series-13/formal/P2S13.lean` — Program 2 Series 13 (2.13), THE CURVE (aggregator).

Imports the five workstreams. Series 2.13 is the capstone of Phase 3 and the last series of the built arc: does the
information grain (Series 2.7's measure of distinction) SOURCE the metric (Series 2.4's directed distance) — does the
measure packed into a region bend the distances around it (gravity)? It imports `P2S9` only (the cone, reaching
`P2S8 / P2S7 / … / P2S4 / … / P2S0 / P1` transitively — chiefly Series 2.4's distance and Series 2.7's grain); it does
NOT import the quantum sub-arc `P2S11`/`P2S12` or the reversibility fork `P2S10` (their content is not reused). It adds
FRESH exactly one thing — the reading of the grain as a possible SOURCE and the grain-dependence test on the model's
OWN distance — and proves: a non-constant grain and a non-trivial distance (WS1); the distance a function of the
adjacency and responsive to it (WS2); the imported distance INVARIANT under grain-change at fixed adjacency and
reading only the adjacency, no by-hand coupling (WS3, the anti-costume core); INERT reached by the imported distance
and the GRAVITATIONAL pole inhabited only by a fenced-off counterfactual foil (WS4). The verdict is computed (WS5):
INERT — the imported distance is decoupled from the grain, no gravity; the honest NOT-RECOVERED that Series 2.4's
proof that space and the measure are DISTINCT axes foretold, and the specification for the next iteration: couple the
two axes 2.4 kept apart. The full Einstein equation and the area law (Bekenstein / holographic) are the disclosed
forward-note. No grain-weighted distance is the recovered object; nothing is smuggled. This closes Phase 3 and the
built arc.
-/
import P2S13.ws1
import P2S13.ws2
import P2S13.ws3
import P2S13.ws4
import P2S13.ws5
