# Program 2 Series 13 (2.13), Charter Status

**The living ledger. The charter is the fixed bar; this file records what is proved, what is open, and how every SERIOUS finding closed (Fixed or Relabeled, per protocol section 0.2a). It never edits the target to record progress.**

*Current phase: DESIGNED (Phase B complete; `spec/` committed, no `formal/` yet). Current verdict: **PENDING** (computed at WS5, never hand-set); the paper hunt (`grain-derisking.md`) finds the imported distance INVARIANT under grain-change at fixed adjacency, so the honest expectation is INERT. This is the CAPSTONE of Phase 3 (`charter-extension-2.md`) and the LAST series of the built arc, THE CURVE: does the information grain (Series 2.7's measure) source the metric (Series 2.4's directed distance) — does the measure packed in a region bend the distances around it (gravity)? The honest expectation is shaped by the phase's own foundation: Series 2.4 proved SPACE and the MEASURE are DISTINCT axes, and gravity is precisely their coupling, so INERT (geometry decoupled from the grain — no gravity, the NOT-RECOVERED grounded in 2.4) is a real and perhaps expected outcome; GRAVITATIONAL (the grain genuinely sources the metric) would be the surprise, scoped to a proportionality. The coupling must be EARNED — the model's OWN metric tested for grain-dependence — never a grain-weighted distance defined by hand. The area law (Bekenstein/holographic) is the hardest piece, a disclosed forward-note. Phase B may begin: Series 2.9 has landed (CONE) and the quantum sub-arc has cleared (2.11 INTERFERING, 2.12 BORN).*

---

## 0. Snapshot

- **Phase:** CODED (Phase E complete; awaiting Phase F blind code review). `spec/` and `formal/` committed. A Phase C pass returned zero SERIOUS (round 2), recorded — P3-D1 precondition met. Design de-risked: the full core (`pathDist`, `adjDist`, `grainDependent`, the verdict) prototyped and compiled clean; the built `formal/` compiles, is sorry-free, axiom-clean, gate-green, names-grep clean.
- **Verdict:** **INERT (computed at WS5, `ws5_the_verdict` / `ws5_verdict_eq = Outcome.inert`, by `rfl`).** The imported distance (`adjDist c = pathDist c.1`, Series 2.4's `stepsFrom` re-seated) is invariant under grain-change at fixed adjacency — geometry decoupled from the grain, no gravity. The honest NOT-RECOVERED grounded in Series 2.4's proof that space and the measure are DISTINCT axes, and the specification for the next iteration: couple the two axes 2.4 kept apart. Pre-registered alternatives GRAVITATIONAL (`sourced`) / SHAPE-DRAWN / PARTIAL all reachable in the verdict function (`ws5_verdict_discriminates`) but not computed. Not pre-baked: the flags are earned by the WS1-WS4 headlines (`ws5_flags_justified`).
- **Build state:** BUILT. Registered (namespace `P2S13`, `srcDir ../program-2/series-13/formal`, roots `["P2S13", "P2S13.AxiomCheck"]`, appended to `defaultTargets`; gate line `(P2S9|P2S13)` — imports `P2S9` only, reaching 2.4's distance / 2.7's grain / 2.9's cone transitively; NOT the quantum sub-arc 2.11/2.12 or the reversibility fork 2.10). `lake build P2S13 P2S13.AxiomCheck` and the full `lake build` both succeed.
- **Axiom state:** standard three or fewer. Every payoff `#print axioms`-clean: `[propext, Classical.choice, Quot.sound]` or (the pure Bool/`Outcome` computations `ws5_verdict_eq` / `ws5_verdict_discriminates` / `ws5_audit_names_not_terms` / `ws5_the_verdict`) NO axioms.
- **Gate state:** GREEN (`OK program-2/series-13/ — imports resolve only to allowed roots`; only `P2S9`/`P2S13`/Mathlib imported).
- **Names grep:** CLEAN. `\b(gravity|curvature|curve|mass|geometry|metric|spacetime|self|import|god|choice)\b` over `formal/` hits only docstring prose and the Lean `import` keyword — no proof term, definition, or constructor named for the content (the `_`-delimited `ws2_metric_*` / `ws4_gravitational_*` headlines are word-internal, not matched; the distance defs are `pathDist`/`adjDist`/`foilDist`, the GRAVITATIONAL constructor is `sourced`).
- **Open SERIOUS findings:** none (Phase C round 2 returned zero SERIOUS; Phase F pending).

## 1. The carrier — the grain and the metric on the imported chain

**S13 stands on the imported chain** (`program-2/series-9`, namespace `P2S9`, reaching `P2S8` / `P2S7` / `P2S4` / … / `P1` transitively; the quantum sub-arc 2.11/2.12 and the reversibility fork 2.10 are NOT imported). Its working material, reached transitively:

| Carrier piece | Where |
|---|---|
| The directed, self-relative distance (the METRIC — the candidate curved geometry; `ws3_metric_source_relative`) | `P2S4` (transitive) |
| The measure of distinction (the GRAIN — the candidate source; the reification rank) | `P2S7` (transitive) |
| The causal cone / horizon (across which the information-geometry relation would live) | `P2S9` (imported) |
| **The reading of the grain as a SOURCE of the metric**, and the grain-dependence test (does the model's distance vary with the measure at fixed adjacency?) | **built at S13 WS1–WS4 — the crux, tested on the model's own metric, de-risked on paper first** |

**Design note (from `charter-extension-2.md` §4, 2.13):** the coupling must be EARNED — the model's OWN metric (Series 2.4) tested for grain-dependence — never a grain-weighted distance defined by hand (the no-smuggling gate, at the capstone). INERT (geometry decoupled from the grain) is a pre-registered honorable outcome and the one Series 2.4's DISTINCTNESS of space and the measure suggests. Scope: at most a proportionality; NOT a full Einstein equation, NOT the area law (Bekenstein/holographic) — the disclosed forward-note.

## 2. Targets (all OPEN until built and reviewed)

| WS | Target theorem(s) | Status | Closed how |
|----|-------------------|--------|-----------|
| WS1 (the ground) | `ws1_grain_and_metric_nontrivial` (the grain non-constant AND the metric non-trivial — a varying source and a geometry it could bend) | BUILT | axiom-clean; `decide` |
| WS2 | `ws2_metric_from_adjacency` (Series 2.4's metric is a function of the directed adjacency — the baseline a grain-dependence would exceed); `ws2_metric_reads_adjacency` (the distance genuinely responds to the adjacency — not a rigged constant) | BUILT | axiom-clean; `simp`/`decide` |
| WS3 (anti-costume core) | `ws3_grain_test` (change the grain at FIXED adjacency: the imported distance is INVARIANT — INERT), `ws3_grain_test_witnessed`, `ws3_no_by_hand_coupling` (the distance under test is the imported adjacency-distance, a function of adjacency, NOT the grain-weighted foil) | BUILT | axiom-clean; structural (`rfl`) + `decide` |
| WS4 (the knot) | `ws4_inert_reachable` (the imported distance invariant under grain-change at fixed adjacency — geometry decoupled, the pole reached) / `ws4_gravitational_reachable` (the GRAVITATIONAL pole inhabited by the fenced-off foil — the test two-sided, no fiat) | BUILT | axiom-clean; `decide` |
| WS5 | verdict + audit (`ws5_verdict_eq (= inert)`, `ws5_verdict_discriminates`, `ws5_flags_justified`, `ws5_the_verdict`; audits (a) coupling earned not defined, (b) fork not by fiat, (c) the distance is the built one, (d) scope disclosed — proportionality not Einstein/area-law, (e) names-not-terms) | BUILT | computed (`rfl`/`decide`), flags earned from WS1-WS4 |

## 3. Audit clauses (protocol §0; BUILT at Phase E, blind-CONFIRMED at Phase C round 2 on the design, to be re-confirmed at Phase F on the code)

- (a) THE COUPLING IS EARNED, NOT DEFINED — `ws5_audit_coupling_earned`: `adjDist c = pathDist c.1` reads the adjacency component ONLY (structural — no channel to the grain `c.2`); the single grain-weighted object `foilDist` is proved DISTINCT and is not the object under test. VERIFIED (built + Phase C round 2 PASS). **The clause the series lives or dies on — the capstone no-smuggling gate.**
- (b) THE FORK NOT BY FIAT — `ws5_audit_fork_genuine`: INERT reachable by the imported distance (`¬ grainDependent adjDist cfgFlat cfgBump`) and GRAVITATIONAL inhabited by the foil (`grainDependent foilDist cfgFlat cfgBump`) on a pair whose grain genuinely differs; the verdict discriminates all four outcomes. VERIFIED (built + Phase C round 2 PASS).
- (c) THE METRIC IS THE BUILT ONE — `ws5_audit_metric_is_built`: the distance under test is the imported adjacency-distance (`adjDist c = pathDist c.1`) and genuinely responds to the adjacency (`pathDist aChain ≠ pathDist aStar`), not a rigged constant, not a fresh grain-weighted gadget. VERIFIED (built + Phase C round 2 PASS).
- (d) THE SCOPE IS DISCLOSED — `ws5_audit_scope`: the verdict is `inert` (no coupling claimed of the imported distance — less than a proportionality); no Einstein field equation, no area law (Bekenstein/holographic — the disclosed forward-note). VERIFIED (built + Phase C round 2 PASS).
- (e) NAMES-NOT-TERMS — `ws5_audit_names_not_terms` (the four outcomes pairwise distinct, non-vacuous) + the protocol §6 grep clean (docstring prose / `import` keyword only). VERIFIED (built + grep clean + Phase C round 2 PASS).

## 4. Findings ledger (Phases C, F, and any Tier-1 landing review)

Phase C and Phase F findings are recorded here with stable IDs, grades, and closure (Fixed / Relabeled per §0.2a). A Tier-1 landing-review finding is recorded with a `T1-` id. **Note (per program finding P3-D1):** Tier 1 will check phase-completeness on this landing — that a Phase C (blind design review) genuinely ran, `blind-seed-C.md` is present, and its findings are recorded; an undisclosed skip is a reviewability defect in its own right.

### Phase C (blind design review, round 1) — 2026-07-22
Blind reviewer (general-purpose, read ONLY `spec/blind-seed-C.md` — blindness confirmed). Both central audits PASS: (a) `adjDist` is STRUCTURALLY blind to the grain `c.2` (it reads `c.1` only), so the INERT invariance is structural not coincidental, and the one grain-reading function `foilDist` is proved distinct and fenced off — the reported verdict does not smuggle it; (b) the fork is genuine on a config pair whose grains genuinely differ (`cfgFlat.2 ≠ cfgBump.2`), both poles reachable, verdict discriminates all four outcomes.

- **C1-S1 — SERIOUS — `ws5_audit_names_not_terms : True` is vacuous.** The audit-(e) certificate asserted nothing (`True`, discharged by `trivial`). **Closed (Fixed):** replaced with a non-vacuous proposition — the four `Outcome` values are pairwise distinct (`by decide`) — certifying the verdict's codomain is a genuine four-way discrimination among NEUTRALLY-named outcomes; the full identifier property (no proof term named for the content) remains the protocol §6 mechanical grep, the real teeth, now noted in the docstring rather than stubbed as `True`.
- **C1-S2 — REAL — `ws4_gravitational_reachable` name imports "gravity."** **Noted, not changed (accepted):** this is the charter's specified WS4 target theorem name (charter §2 WS4, §2 status table), it is grep-clean (`\bgravity\b` does not match the `_`-delimited "gravitational", and "gravitational" is not itself a forbidden word), and it matches the established house standard — Series 2.9 kept `ws4_cone_reachable` with "cone" on its own forbidden list identically. The GRAVITATIONAL OUTCOME CONSTRUCTOR is neutrally named `sourced` (not `gravitational`), so no definition or constructor is named for the content. Renaming away from the charter target would be an undisclosed narrowing; the name stays.
- **C1-S3 — COSMETIC — theorem names containing "metric"** (`ws2_metric_from_adjacency`, `ws2_metric_reads_adjacency`, `ws5_audit_metric_is_built`). **Noted, accepted:** these are the charter's specified target names; "metric" appears ONLY in `_`-delimited `ws*` headline composites (grep-clean, word-internal), never in a definition or constructor (the distances are `pathDist`/`adjDist`/`foilDist`). House standard, as C1-S2.
- **C1-S4 — COSMETIC — `verdict` parameters containing forbidden fragments** (`metricNonTrivial`, `metricFromAdj`, `importedGrainDep`). **Closed (Fixed):** these are internal (non-charter) identifiers, renamed to drop the fragments — `distNonTrivial`, `distFromAdj`, `couplingPresent` — grep-clean and clear of "metric"/"import".

Round-1 SERIOUS: 1 (C1-S1), closed Fixed by editing the design. Per protocol §Phase D, a fresh Phase C round re-runs on the repaired design.

### Phase C (blind design review, round 2) — 2026-07-22
Fresh blind reviewer (general-purpose, read ONLY `spec/blind-seed-C.md` — blindness confirmed) on the repaired design. **Zero SERIOUS.** Audits (a)-(e) and the strip test all PASS: (a) `adjDist` is definitionally `pathDist c.1` — no syntactic access to the grain `c.2`, so INERT is STRUCTURAL; `foilDist` (the one grain-reader) proved distinct and fenced off; (b) fork genuine on a genuinely-differing pair, verdict discriminates all four constructors; (c) distance responds to the adjacency, not a rigged constant; (d) no Einstein/area-law overclaim; (e) all defs and constructors clean of the forbidden set (`sourced`/`inert`, not `gravitational`). C1-S1 fix confirmed non-vacuous.

- **C2-S1 — REAL — theorem labels containing "metric"** (`ws2_metric_from_adjacency`, `ws2_metric_reads_adjacency`). **Noted, accepted:** charter-specified target names; "metric" appears only in `_`-delimited `ws*` headline composites (grep-clean), no def named `metric` (the distances are `pathDist`/`adjDist`). House standard (cf. S9 `ws4_cone_reachable`, "cone" forbidden). Same as C1-S3.
- **C2-S2 — REAL — `ws4_gravitational_reachable` label.** **Noted, accepted:** the charter's WS4 target name; grep-clean; the GRAVITATIONAL constructor is neutrally named `sourced`. Same as C1-S2. Renaming would be an undisclosed narrowing from the charter target.

**Phase C CLOSED: a Phase C pass returned zero SERIOUS (round 2), recorded here with `blind-seed-C.md` present (P3-D1 satisfied). Precondition for Phase E met.**

## 5. Deviations from the charter

None yet. Any narrowing between charter and build is disclosed here.

## 6. Permanent opens (inherited, untouched)

Program 1's four permanent opens stand: the content of the compass, the direction of convergence, the differentiating act, the classification of the out-of-image imports. Series 2.13 adds none and closes none — it closes the built arc, not the openings. If it lands INERT or SHAPE-DRAWN, its question (does the grain curve the metric) joins the standing catalogue of what the ontology does not yet own; the area law joins it regardless, as the disclosed deepest-unbuilt question.

## 7. Series log

- **2026-07-22 — Phase E (Code) complete; verdict computes INERT.** Built `formal/P2S13/ws1..ws5`, `P2S13.lean`, `P2S13/AxiomCheck.lean` in namespace `P2S13`, importing `P2S9` only. Registered the lib in `lake/lakefile.toml` (roots `["P2S13","P2S13.AxiomCheck"]`, appended to `defaultTargets`) and added the gate line `check program-2/series-13 "^import (P2S9|P2S13)…"`. `lake build P2S13 P2S13.AxiomCheck` and the full `lake build` both succeed. Sorry-free; axiom-clean (standard three, the pure Bool/`Outcome` computations no axioms); gate-green; names-grep clean (docstring prose / `import` only). The verdict is COMPUTED to **INERT** (`ws5_verdict_eq = Outcome.inert` by `rfl`): the imported distance `adjDist c = pathDist c.1` (Series 2.4's `stepsFrom` re-seated) is invariant under grain-change at fixed adjacency — geometry decoupled from the grain, no gravity. The honest NOT-RECOVERED grounded in Series 2.4's DISTINCTNESS, and the specification for the next iteration: couple the two axes 2.4 kept apart. Next: Phase F (blind code review).
- **2026-07-22 — Phase C (blind design review, MANDATORY per P3-D1) complete; Phase D repair.** Round 1: one SERIOUS (C1-S1, vacuous `: True` audit-e certificate) closed Fixed; C1-S4 params renamed; C1-S2/S3 (charter-mandated `ws*` names) noted/accepted. Round 2 on the repaired design: **zero SERIOUS**, both central audits (coupling-earned, fork-not-by-fiat) PASS, blindness confirmed. `blind-seed-C.md` present and findings recorded (P3-D1 satisfied).
- **2026-07-22 — Phase B (Design) complete.** Wrote `spec/grain-derisking.md` (the paper hunt): the imported distance (Series 2.4's `stepsFrom`, re-seated as the decidable path-length `pathDist`) is a function of the ADJACENCY alone; on two configurations with the same adjacency (`aChain`) and different grain (`grainFlat` vs `grainBump`, differing at `p3`), the distance is INVARIANT — the paper verdict is INERT, grounded in Series 2.4's DISTINCTNESS of space and the measure. Wrote `spec/README.md` and `spec/ws1..ws5-design.md`: WS1 the grain non-constant and the distance non-trivial; WS2 the distance a function of the adjacency and genuinely responsive to it (not a rigged constant); WS3 the imported distance invariant under grain-change and reads only the adjacency (no by-hand coupling), with a single COUNTERFACTUAL FOIL (`foilDist`) exhibited and fenced off (the Series 2.12 `partsWeight` pattern); WS4 INERT reached by the imported distance and GRAVITATIONAL inhabited only by the foil (fork not by fiat); WS5 the verdict computed to INERT, discriminating, flags earned, audits (a)-(e). The whole core prototyped and compiled clean (`decide`/`rfl` on `Fin 4`). No `formal/` yet (Phase E). Next: Phase C (blind design review, MANDATORY per P3-D1).
- **2026-07-21 — Series 2.13 (The Curve) chartered.** Tier 1 authored `series-13/charter.md` / `charter-status.md` / `protocol.md`: the capstone of Phase 3 and the last of the built arc — does the information grain (Series 2.7's measure) source the metric (Series 2.4's directed distance), bending the distances around a region (gravity)? Reuses 2.4's metric, 2.7's grain, 2.9's cone; imports `P2S9` only. The coupling must be EARNED — the model's OWN metric tested for grain-dependence at fixed adjacency — never a grain-weighted distance defined by hand (the capstone no-smuggling gate). Pre-registered: GRAVITATIONAL (gravity recovered, scoped to a proportionality), INERT (geometry decoupled — the NOT-RECOVERED grounded in 2.4's proof that space and the measure are DISTINCT axes, the very things gravity unites), SHAPE-DRAWN. The area law (Bekenstein/holographic) is the hardest piece, a disclosed forward-note, not a target. Namespace `P2S13`. Phase B begins now that 2.9 has landed and the quantum sub-arc has cleared. Handoff to a fresh Tier-2 executor pending — all chartered phases mandatory (P3-D1), the blind Phase C especially. On landing, this closes Phase 3 and the built arc; Tier 1 will then compose the program-level Phase-3 close.
