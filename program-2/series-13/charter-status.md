# Program 2 Series 13 (2.13), Charter Status

**The living ledger. The charter is the fixed bar; this file records what is proved, what is open, and how every SERIOUS finding closed (Fixed or Relabeled, per protocol section 0.2a). It never edits the target to record progress.**

*Current phase: CHARTERED (Phase A complete). No design, no build yet. Current verdict: **PENDING** (computed at WS5, never hand-set). This is the CAPSTONE of Phase 3 (`charter-extension-2.md`) and the LAST series of the built arc, THE CURVE: does the information grain (Series 2.7's measure) source the metric (Series 2.4's directed distance) — does the measure packed in a region bend the distances around it (gravity)? The honest expectation is shaped by the phase's own foundation: Series 2.4 proved SPACE and the MEASURE are DISTINCT axes, and gravity is precisely their coupling, so INERT (geometry decoupled from the grain — no gravity, the NOT-RECOVERED grounded in 2.4) is a real and perhaps expected outcome; GRAVITATIONAL (the grain genuinely sources the metric) would be the surprise, scoped to a proportionality. The coupling must be EARNED — the model's OWN metric tested for grain-dependence — never a grain-weighted distance defined by hand. The area law (Bekenstein/holographic) is the hardest piece, a disclosed forward-note. Phase B may begin: Series 2.9 has landed (CONE) and the quantum sub-arc has cleared (2.11 INTERFERING, 2.12 BORN).*

---

## 0. Snapshot

- **Phase:** CHARTERED. `charter.md` / `charter-status.md` / `protocol.md` authored by Tier 1. `spec/` and `formal/` not yet created (Phase B creates the scaffold). **Precondition:** Series 2.9 landed (CONE); the quantum sub-arc cleared (2.11 INTERFERING, 2.12 BORN) — met.
- **Verdict:** **PENDING.** Pre-registered outcomes: GRAVITATIONAL (the grain sources the metric — gravity recovered, an equation-of-state shape, scoped to a proportionality), INERT (the metric invariant under grain-change at fixed adjacency — geometry decoupled, no gravity, the NOT-RECOVERED grounded in 2.4's DISTINCTNESS), SHAPE-DRAWN, PARTIAL. Honest expectation leans INERT; not pre-baked.
- **Build state:** none yet. Registered at Phase E (namespace `P2S13`, `srcDir ../program-2/series-13/formal`, roots `["P2S13", "P2S13.AxiomCheck"]`, appended to `defaultTargets`; gate line `(P2S9|P2S13)` — imports 2.9 only, reaching 2.4's metric / 2.7's grain / 2.9's cone transitively; NOT the quantum sub-arc 2.11/2.12 or the reversibility fork 2.10).
- **Axiom state / gate state / names grep:** to be verified at Phase E (standard three or fewer; imports `P2S9` only, S8–S4/…/P1 transitive; forbidden identifiers: gravity, curvature, curve, mass, geometry, metric, spacetime, self, import, god, choice — docstring prose excepted).
- **Open SERIOUS findings:** none yet (no review has run).

## 1. The carrier — the grain and the metric on the imported chain

**S13 stands on the imported chain** (`program-2/series-9`, namespace `P2S9`, reaching `P2S8` / `P2S7` / `P2S4` / … / `P1` transitively; the quantum sub-arc 2.11/2.12 and the reversibility fork 2.10 are NOT imported). Its working material, reached transitively:

| Carrier piece | Where |
|---|---|
| The directed, self-relative distance (the METRIC — the candidate curved geometry; `ws3_metric_source_relative`) | `P2S4` (transitive) |
| The measure of distinction (the GRAIN — the candidate source; the reification rank) | `P2S7` (transitive) |
| The causal cone / horizon (across which the information-geometry relation would live) | `P2S9` (imported) |
| **The reading of the grain as a SOURCE of the metric**, and the grain-dependence test (does the model's distance vary with the measure at fixed adjacency?) | **built at S13 WS1–WS4 — the crux, tested on the model's own metric, de-risked on paper first** |

**Design note (from `charter-extension-2.md` §4, 2.13):** the coupling must be EARNED — the model's OWN metric (Series 2.4) tested for grain-dependence — never a grain-weighted distance defined by hand (the no-smuggling gate, at the capstone). INERT (geometry decoupled from the grain) is a pre-registered honorable outcome and the one Series 2.4's DISTINCTNESS of space and the measure suggests. Scope: at most a proportionality; NOT a full Einstein equation, NOT the area law (Bekenstein/holographic) — the disclosed forward-note.

## 2. Targets (all OPEN until built and reviewed)

| WS | Target theorem(s) | Status | Closed how |
|----|-------------------|--------|-----------|
| WS1 (the ground) | `ws1_grain_and_metric_nontrivial` (the grain non-constant AND the metric non-trivial — a varying source and a geometry it could bend) | OPEN | — |
| WS2 | `ws2_metric_from_adjacency` (Series 2.4's metric is a function of the directed adjacency — the baseline a grain-dependence would exceed) | OPEN | — |
| WS3 (anti-costume core) | `ws3_grain_test` (change the grain at FIXED adjacency: does the imported metric change — GRAVITATIONAL — or stay invariant — INERT?), `ws3_no_by_hand_coupling` (the metric under test is the imported Series 2.4 distance, a function of adjacency, NOT a grain-weighted gadget) | OPEN | — |
| WS4 (the knot) | `ws4_gravitational_reachable` (the grain genuinely sources the metric, if the structure provides it) / `ws4_inert_reachable` (the metric invariant under grain-change at fixed adjacency — geometry decoupled) — both genuinely testable, no fiat | OPEN | — |
| WS5 | verdict + audit (`ws5_verdict_eq`, `ws5_verdict_discriminates`, `ws5_flags_justified`; audits (a) coupling earned not defined, (b) fork not by fiat, (c) the metric is the built one, (d) scope disclosed — proportionality not Einstein/area-law, (e) names-not-terms) | OPEN | — |

## 3. Audit clauses (protocol §0; UNVERIFIED until Phase F)

- (a) THE COUPLING IS EARNED, NOT DEFINED — no proof term defines the distance as a function of the measure; the metric under test is Series 2.4's imported adjacency-distance, its grain-dependence (or invariance) TESTED not built. UNVERIFIED. **The clause the series lives or dies on — the capstone no-smuggling gate.**
- (b) THE FORK NOT BY FIAT — GRAVITATIONAL and INERT both genuinely testable, the verdict discriminating. UNVERIFIED.
- (c) THE METRIC IS THE BUILT ONE — the distance under test is Series 2.4's, not a fresh grain-weighted gadget. UNVERIFIED.
- (d) THE SCOPE IS DISCLOSED — at most a proportionality / grain-dependence; NOT a full Einstein equation, NOT the area law (Bekenstein/holographic — the disclosed forward-note). UNVERIFIED.
- (e) NAMES-NOT-TERMS — no proof term named "gravity," "curvature," "curve," "mass," "geometry," "metric," "spacetime," "self," "import," "God," "choice" as content; the grep is the teeth. UNVERIFIED.

## 4. Findings ledger (Phases C, F, and any Tier-1 landing review)

Empty. Phase C and Phase F findings are recorded here with stable IDs, grades, and closure (Fixed / Relabeled per §0.2a). A Tier-1 landing-review finding is recorded with a `T1-` id. **Note (per program finding P3-D1):** Tier 1 will check phase-completeness on this landing — that a Phase C (blind design review) genuinely ran, `blind-seed-C.md` is present, and its findings are recorded; an undisclosed skip is a reviewability defect in its own right.

## 5. Deviations from the charter

None yet. Any narrowing between charter and build is disclosed here.

## 6. Permanent opens (inherited, untouched)

Program 1's four permanent opens stand: the content of the compass, the direction of convergence, the differentiating act, the classification of the out-of-image imports. Series 2.13 adds none and closes none — it closes the built arc, not the openings. If it lands INERT or SHAPE-DRAWN, its question (does the grain curve the metric) joins the standing catalogue of what the ontology does not yet own; the area law joins it regardless, as the disclosed deepest-unbuilt question.

## 7. Series log

- **2026-07-21 — Series 2.13 (The Curve) chartered.** Tier 1 authored `series-13/charter.md` / `charter-status.md` / `protocol.md`: the capstone of Phase 3 and the last of the built arc — does the information grain (Series 2.7's measure) source the metric (Series 2.4's directed distance), bending the distances around a region (gravity)? Reuses 2.4's metric, 2.7's grain, 2.9's cone; imports `P2S9` only. The coupling must be EARNED — the model's OWN metric tested for grain-dependence at fixed adjacency — never a grain-weighted distance defined by hand (the capstone no-smuggling gate). Pre-registered: GRAVITATIONAL (gravity recovered, scoped to a proportionality), INERT (geometry decoupled — the NOT-RECOVERED grounded in 2.4's proof that space and the measure are DISTINCT axes, the very things gravity unites), SHAPE-DRAWN. The area law (Bekenstein/holographic) is the hardest piece, a disclosed forward-note, not a target. Namespace `P2S13`. Phase B begins now that 2.9 has landed and the quantum sub-arc has cleared. Handoff to a fresh Tier-2 executor pending — all chartered phases mandatory (P3-D1), the blind Phase C especially. On landing, this closes Phase 3 and the built arc; Tier 1 will then compose the program-level Phase-3 close.
