/-
`program-2/series-12/formal/P2S12.lean` — Program 2 Series 12 (2.12), THE WEIGHT (aggregator).

Imports the five workstreams. Series 2.12 is the third Phase-3 recovery test (execution tier 3, the second half of the
quantum recovery, after Series 2.11 found genuine destructive interference — INTERFERING): do the imports — the
out-of-image differentiator, the program's one genuine freedom — carry a MEASURE, a probability over the possibilities,
and is it the Born rule, the SQUARE of the amplitude? It imports `P2S11` only (the finished object carrying the sign
`amp`, the two path amplitudes, and the two candidate weights `combinedWeight` / `partsWeight`), reaching `P2S8 / P2S7 /
… / P2S0 / P1` transitively; it does NOT import the tier-1 probes `P2S9`/`P2S10` (their content is not reused). It adds
FRESH exactly one thing — the reading of the imported weight as a probability and the FORM-AGNOSTIC consistency
predicate `respectsCancel` — and proves: the outcomes non-trivial and the candidate weight non-constant (WS1); the
weight non-negative and non-classical (WS2); the classical additive (square-then-add) form REFUTED and the
add-then-square form CONSISTENT, so the squared form FORCED and earned off the built holonomy, no probability defined
(WS3, the anti-costume core); the squared (BORN) and deterministic poles both reachable (WS4). The verdict is computed
(WS5): SQUARED — the imports carry a non-trivial measure whose only cancellation-consistent form is the squared
amplitude, the classical additive form refuted; quantum probability recovered in its REBIT form (real `±1`), with the
complex amplitude and Gleason's / Busch's uniqueness the disclosed forward-note. No probability is defined as `|amp|²`;
nothing is smuggled.
-/
import P2S12.ws1
import P2S12.ws2
import P2S12.ws3
import P2S12.ws4
import P2S12.ws5
