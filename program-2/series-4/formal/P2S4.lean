/-
`program-2/series-4/formal/P2S4.lean` — Program 2 Series 4 (2.4), SPACE (aggregator).

Imports the five workstreams. Series 2.4 builds THE WORLD (a genuine lateral population of same-rank
import-distinguished peers with a local attention graph) and two gradings on it (a directed granular
self-relative breadth-distance, and the reification depth), proves the two axes come apart (WS2), the lateral
separation is a genuine import (WS3, Series 07), and at the knot proves the two-axes fork DISTINCT with REDUCED
genuinely reachable on the tower `T` (WS4), the verdict computed (WS5).
-/
import P2S4.ws1
import P2S4.ws2
import P2S4.ws3
import P2S4.ws4
import P2S4.ws5
