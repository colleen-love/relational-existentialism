/-
`program-2/series-5/formal/P2S5.lean` — Program 2 Series 5 (2.5), THE LOOP (aggregator).

Imports the five workstreams. Series 2.5 builds THE FOLD (the topology of the whole: no smallest by
atomlessness, no largest by unbounded reification, the largest bending into the smallest through the diagonal)
as the ground (WS1), proves the relating genuinely cycles (WS2) yet causation - a structural reification-
dependency (`causalDep`) - is acyclic (WS3, its acyclicity PROVED from the rank-lift, not defined), and at the
knot proves the sole candidate for a closed causal loop is the self-membered fold point (WS4), with the LOOPED
reading reachable on a second carrier (no fiat) and the fold resting on the P1 diagonal. The verdict is computed
(WS5): ACYCLIC - the relating loops but time does not, and the fold's self-reference is a degenerate fixed point.
-/
import P2S5.ws1
import P2S5.ws2
import P2S5.ws3
import P2S5.ws4
import P2S5.ws5
