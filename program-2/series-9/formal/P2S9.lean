/-
`program-2/series-9/formal/P2S9.lean` — Program 2 Series 9 (2.9), THE CONE (aggregator).

Imports the five workstreams. Series 2.9 is the first recovery test of Phase 3 and one of two tier-1 probes. It asks:
is there a maximal RATE at which attention-breadth becomes reification-depth — a finite fastest becoming, a LIGHT CONE
and a c? It builds FRESH, read off the finite attention, one thing: the RATE (breadth-to-depth conversion bounded by
the finite attention, WS1), its CONE (the rate-bounded reachable set, WS2), and the discipline that the rate is
genuine content earned from attention and not the bare causal order (WS3). It finds, HONESTLY: the rate is bounded and
EARNED from the finite attention (a `Finset.sup`, not a postulated c — WS1); the cone is the rate-bounded set and
NON-TRIVIAL (some event strictly outside — WS2); the cone carries a genuine rate, not Series 2.5's order (same order,
different rate, different cone — WS3); and the fork is genuine — a local attention has a finite speed and a
non-trivial cone (CONE) while a total attention reifies instantaneously (NO-CONE), both reachable, neither by fiat
(WS4). The verdict is computed (WS5): CONE — finite attention IS a maximal rate of becoming, and relativity's light
cone is the shadow of a bounded capacity to attend. The recovery earned, not smuggled; the cone a rate, not a costume.
-/
import P2S9.ws1
import P2S9.ws2
import P2S9.ws3
import P2S9.ws4
import P2S9.ws5
