/-
`program-2/series-11/formal/P2S11.lean` — Program 2 Series 11 (2.11), THE PHASE (aggregator).

Imports the five workstreams. Series 2.11 is the decisive quantum test of Phase 3 (execution tier 2, after the tier-1
pair 2.9/2.10 cleared): can distinction CANCEL — is there a signed amplitude, read off Series 2.8's holonomy, that
produces genuine DESTRUCTIVE INTERFERENCE (a combined weight strictly below its parts)? It imports `P2S8` only (the
finished object carrying the signed increment `incr` and the holonomy `hol`), reaching `P2S7 / … / P2S0 / P1`
transitively; it does NOT import the tier-1 probes `P2S9`/`P2S10` (their content is not reused). It builds FRESH, earned
off the built holonomy, one thing: the SIGN `amp` (the parity of the holonomy, WS1), proves it genuinely signed (WS1),
that it CANCELS (WS2), that the cancellation is genuine destructive interference in a WEIGHT — a combined weight
strictly below the parts — and the sign is EARNED off the built `incr`/`hol` (WS3), and that the interfering and
additive poles are both reachable (WS4). The verdict is computed (WS5): INTERFERING — the signed holonomy Series 2.8
built is a genuine phase; distinction can subtract; the door to quantum theory is open, honestly scoped as REAL
(signed, `±1`) interference, with the complex `U(1)` phase and the Born rule the disclosed forward-note. No complex
number is built; nothing is smuggled.
-/
import P2S11.ws1
import P2S11.ws2
import P2S11.ws3
import P2S11.ws4
import P2S11.ws5
