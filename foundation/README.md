# foundation — stable, shared infrastructure

The mathlib-bound shared layer: the traced symmetric-monoidal-category typeclass + JSV axioms. Lean
namespace `Foundation.*` (sources in [`formal/`](formal), prose in [`spec/`](spec)). Imports **mathlib
only**; imported by every other root, never importing them. Held to mathlib standards and destined to *leave*
the repository as an eventual mathlib PR. See [`../STRUCTURE.md`](../STRUCTURE.md).
