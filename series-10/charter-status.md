# Relational Existentialism, Series 10: Charter Status

**The mutable companion to `charter.md`. All progress, revisions, per-workstream status, discharges, and open items live here, the charter stays clean.**

*Initialized at series start (Phase A complete, 2026-07-11). Series 10 is the response to Series 09's hardest finding: the diagonal spine held (self-reference cannot close, independent of relational identity) and the residue was free, but on a FIXED carrier the residue could only MOVE (`ws4_residue_moves`), and a moving hole is still one hole (series-09 series-review-1 bottom line + inter-series seed). Series 10 lets the free residue REIFY into a new relatum via `reify : F(Ω) → Ω`, licensed by the founding equation `Ω ≅ F(Ω)`, so the carrier GROWS. The Series 09 diagonal makes the growth genuine: a reified self-relation recoverable from the relating would reconstruct a self-total hold, which the diagonal forbids, so blindness is productive. The κ-bound is deliberate SCAFFOLDING (Series 10 proves the engine on a known-safe carrier); its endogenous replacement by finiteness of attention is deferred to Series 11 by design. Phases B through F have NOT been run. Nothing below is proved. This file records intentions and pre-registered targets only.*

---

## How to read this file

- **Status vocabulary** (charter §7): **Discharged** · **Discharged-on-scaffold** (proved on the κ-bounded carrier, pending κ-removal in Series 11; honest and first-class, NOT a claim of endogenous boundedness) · **Impossibility proved** (sharp negative, first-class; CLOSE-forbidden is one) · **Partial** · **Failed** · **Circular** (WS7-only) · **Refuted hypothesis** (a pre-registered open law tested and found false; the crown kill condition can produce this, reported as FATAL, a success) · **Bookkeeping** (Series 10 addition: a "growth" that is only a longer external record while the carrier is unchanged up to bisimulation; the specific negative to avoid at the payoff, naming it honestly is first-class) · **Not started**.
- The **crown (reflexive fold) is NOT a target to prove true.** It is an open law to *settle*. "FATAL" (refuted) is a success outcome for it. See the kill condition (charter §5.4) and WS5.
- The **endogenous bound is OUT OF SCOPE.** Series 10 uses κ as scaffolding and proves nothing that relies on κ being small; removing κ and testing finiteness-of-attention as the endogenous bound is Series 11. A Series 10 result claiming endogenous self-bounding would be overclaiming (charter §5.3, §5.5 κ-by-fiat).
- The **payoff may fail productively.** If `reify` is too weak and growth is a longer `List` not a bigger carrier, that is **Bookkeeping**: Series 09's moving hole re-hit one level up, reported honestly, not buried.

## Program-level snapshot

| Item | Status |
|---|---|
| **Headline target** | Productive blindness → genuine carrier growth. **ENGINE DISCHARGED, PAYOFF BOOKKEEPING (Phase E re-grade, 2026-07-11).** Freeness through the diagonal is Discharged (`ws1_free_reification`, but of the residue content, `reify` absent, R1). Strict internal growth **provably resists**: the collapse engine makes the reified `SHNE` relatum bisimilar to prior relata, so `Ω_{α+1}` bisim-embeds into `Ω_α` (`ws2_reify_bisim_embeds`) — Series 09's moving hole re-hit one level up. The pre-registered **Bookkeeping** terminus (charter §5.5/§8.1), a first-class success. |
| **The three consequences** | Genuine growth → **BOOKKEEPING** (S1, `ws2_reify_bisim_embeds`: the tower bisim-embeds, growth cardinality-only). Break of the Series 07 collapse → **not proved** (subsumed by Bookkeeping; the `labelLoop` facts are about a fixed field). Close-or-fold dichotomy with **CLOSE forbidden at the INSPECTION level** (S3, `ws4_close_forbidden`, tower-independent; carrier closure is charter §9's open question). **BUILT + re-graded.** |
| **The central open law** | FOLD vs FATAL. CLOSE forbidden (inspection-level diagonal). **Verdict Partial (`.partialV`, built), honest** — but the per-step fold propping it is **definitional** (S2, `reifyStep`'s construction, not a substantive discharge); the substantive residue-fold + κ-removal are fully open (Series 11); **FATAL pre-registered**. Never assumed, never small-κ (κ-by-fiat genuinely avoided, PASS). |
| **Verdict (WS7)** | **`bookkeeping` (Re-graded, S1, 2026-07-11).** A verdict on a discharged `Audit` whose flagship field `growthBookkeeping` certifies the moving-hole re-hit; `ws7_verdict = bookkeeping`, provably never `reificationEstablished`/`selfTotalSmuggled`/`kappaByFiat`/`Circular` given the certificate. The honest headline: engine Discharged, payoff Bookkeeping — the correct seed for Series 11. Axiom pass executed (`spec/axiom-check-log.md`, R2). |
| **Relation to Series 09** | Response, not continuation. Transcribes (does not import) Series 09's `PkObj κ`, `dest`, `SHNE`, `BehaviorallyIdentified`, the diagonal `ws1_no_self_total_hold`, the free residue `ws2_residue_free`, and the Series 04/WS2 recoverability test, then ADDS `reify : F(Ω) → Ω` (the ingredient Series 09 lacked and the reason its hole could only move). Nothing imported across series (to be gate-confirmed at build). |
| **Relation to Series 11 (forecast)** | Series 10 proves the engine on scaffolding; Series 11 removes κ and tests finiteness of attention as the endogenous bound. Whatever horn (Discharged-on-scaffold / FATAL / Partial) Series 10 delivers for the fold is Series 11's seed. |

## Phase status

| Phase | Name | Status |
|---|---|---|
| A | Charter | **Complete (2026-07-11).** `charter.md`, `charter-status.md`, `protocol.md` written. |
| B | Design-all (seven `spec/wsNN-design.md` + `spec/README.md`) | **Complete (2026-07-11).** `spec/README.md` (the design index) + `spec/ws1-design.md` … `spec/ws7-design.md` written and committed as a batch. The two Series-10 design duties settled: `reify : F(Ω) → Ω` defined once in WS1 as a section of `dest` (`IsReify`, `dest ∘ reify = id`), ambient for all; the tower order `≺` derived once in WS3 from reification sequences (`prec = ReflTransGen reifyStep`), consumed by WS4/WS5, with the imported-ordinal branch designed in as a refuted failure mode. |
| C | Build-all (`formal/Series10/wsNN.lean`, `Series10.lean`, `AxiomCheck.lean`) | **Complete (2026-07-11).** All seven `formal/Series10/wsNN.lean` + `Series10.lean` + `AxiomCheck.lean` built, self-contained (Series 09/08/07/04 machinery transcribed, re-namespaced `Series10.WSn`, nothing imported across series). `lake build Series10` succeeds; **sorry-free and axiom-clean** (every headline reduces to `propext` / `Classical.choice` / `Quot.sound`, confirmed by `AxiomCheck`). Registered in `lake/lakefile.toml` (`defaultTargets` + `[[lean_lib]] Series10`). Four honest design adjustments recorded in the closed log below. |
| D | Blind series-review → `spec/series-review-1.md` | **Complete (2026-07-11).** 3 SERIOUS (S1 growth-is-Bookkeeping, S2 fold-definitional, S3 CLOSE-decoupled), 3 REAL (R1/R2/R3), 1 COSMETIC (C1). |
| E | Address (pass 1) | **Complete (2026-07-11).** All SERIOUS relabelled to pre-registered outcomes (S1→Bookkeeping, S2→definitional, S3→inspection-level); REAL/COSMETIC fixed/disclosed. Verdict re-graded to `bookkeeping`. Build green, axiom pass executed (`spec/axiom-check-log.md`). See series-review log. |
| (loop D→E) | Second review pass → `spec/series-review-2.md` | **Not started.** |
| F | Close (summaries + root README) | **Not started.** |

The canonical run is **B → C → D → E → D → E** (protocol §2), with more D→E loops added only if a review pass still returns SERIOUS findings.

## Workstream status

### WS1, The reifying carrier and productive blindness · *blocking, the spine*
**Status: Discharged, but disclosed as transcribed residue-freeness (series-review-1 R1, 2026-07-11).** `reify : PkObj κ Ω → Ω` fixed as a section of `dest` (`IsReify`), injective (`ws1_reify_injective`). **Freeness** `ws1_free_reification` Discharged — `ws2_residue_is_import`, routing through `ws1_no_self_total_hold` (GENUINE, the charter's non-negotiable met). **But it is freeness of the Series 09 residue CONTENT (`insp` only); `reify` is NOT in the term** (the design's `ReifiedResidueRecoverable` was `ResidueRecoverable` renamed, so it never tied `reify` in). Disclosed: the spine is Series 09 residue-freeness transcribed; the lift to relatum-freshness is interpretive, NOT machine-checked. **CLOSE-forbidden locally** `ws1_close_forbidden_local` Discharged as an Impossibility (`IsTotalityRelatum := SelfTotal`). All axiom-clean. **Design adjustment #1:** `ws1_reify_section_not_iso` ("surjective `reify` ⇒ False") is NOT provable (a κ-bounded section CAN be a bijection); replaced by `ws1_diagonal_forbids_closure` (non-closure is the inspection-level diagonal; the carrier-iso question stays charter §9's open question).

### WS2, Genuine growth, not bookkeeping · *the payoff*
**Status: BOOKKEEPING (Relabeled, series-review-1 S1, 2026-07-11).** The specified target — `Ω_{α+1}` does NOT bisim-embed into `Ω_α` — provably RESISTS on the plain `PkObj κ` carrier: the collapse engine `ws1_atomless_bisim` makes every reified `SHNE` relatum bisimilar to prior relata, so `Ω_{α+1}` DOES bisim-embed into `Ω_α`. This is the charter §5.5 pre-registered Bookkeeping antecedent, now a theorem: `ws2_growth_is_bookkeeping` (any two `SHNE` relata are bisimilar) and `ws2_reify_bisim_embeds` (the reified relatum bisim-embeds). Growth is cardinality-only — Series 09's moving hole re-hit one level up. `reify` (a plain section) grows the carrier as a SET but not its bisimulation quotient. The strict-internal-growth claim is RETRACTED. The `labelLoop` facts (`ws2_free_label_survives`, `ws2_label_free_import`, `ws2_plain_collapse_persists`) are transcribed import-test facts about a FIXED 2-state coalgebra, NOT tower growth, retained only as the honest record. **Reporting Bookkeeping is a first-class success (charter §7), not a failure** — it is the honest terminus the charter anticipated, and the deepest form of the plurality question is the Series 11 seed (charter §10, protocol §6).

### WS3, The reification tower and its order · *the engine of the tower*
**Status: Discharged (built 2026-07-11).** `reifyStep` / `towerN` (the carrier's growing subsets, not an external `List`); **(NL)** `ws3_reify_preserves_SHNE` (a reified non-empty hereditarily-non-empty pattern is a full relatum, never a leaf); the ONE endogenous order `prec := ReflTransGen reifyStep` (`ws3_order_endogenous`, `Iff.rfl`); the imported-ordinal branch refuted (`ws3_imported_order_refuted`, endogeneity-of-definition); monotonicity of the family (`ws3_tower_monotone_family` — renamed from `ws3_tower_well_founded`, series-review-1 C1, since it is monotonicity not well-foundedness of `≺`; `ws3_tower_monotone`). The fold is NOT defined here. **Design adjustment (recorded below, #2):** the "ordinal-indexed tower" (`Ordinal → Set X`) is realized as the index-free reachability `prec` (exactly "reached by reification sequences") plus a concrete ℕ-iterate `towerN`; the transfinite/limit behavior (which only matters for the fold's UNIVERSAL form) is deferred, consistent with the fold-universal already being deferred. No signature's meaning changes.

### WS4, Close-or-fold: the dichotomy and CLOSE-forbidden · *the structural heart*
**Status: Impossibility proved at the INSPECTION LEVEL (Relabeled, series-review-1 S3, 2026-07-11).** `ws4_close_forbidden` (`¬ Closes`) is a genuine Impossibility — no self-total hold exists — BUT its proof discards the tower hypotheses (`_hreach`/`_hmem`); the content is `ws1_no_self_total_hold`, a fact about `insp` alone (a free parameter, not induced by `reify`/`dest` on the tower). So it forbids the closing top at the inspection level, tower-INDEPENDENT; it does NOT show the *carrier-level* tower cannot close. The "the tower cannot close into a top" gloss is RETRACTED — that (the founding-equation iso reaching a totality-relatum) is charter §9's OPEN structural question, consistent with adjustment #1. Dichotomy exhaustive (`ws4_dichotomy`). Fold predicate `Folds` = distributed REFLEXIVITY (`ws4_fold_is_reflexivity`), `FoldsByCardinality` quarantined (κ-by-fiat guard, PASS). Fold TRUTH handed to WS5.

### WS5, The fold-or-fatal fork · *the honest open*
**Status: Not started. THE CENTRAL OPEN, TO BE SETTLED BY THE KILL CONDITION, NOT ASSUMED.** Target: attempt the crown (distributed reflexivity at every stage). Run the pre-committed kill condition (charter §5.4):
- exhibit a free tower that, with κ removed, strictly proliferates past every bounded stage with NO distributed-reflexivity fold → crown **FATAL** (refuted; "self-reference buys the many only at the cost of the whole"; the self-bounding hope retracted; a success outcome);
- exhibit distributed reflexivity at every stage on the bounded carrier → crown **Discharged-on-scaffold** (Series 11 licensed to remove κ);
- neither → crown **Partial** (defended thesis floored by the mechanized core; fold-or-fatal left open).
This is Series 10's atom-or-will door (inheriting Series 07's atom-or-will, Series 08's conservation, Series 09's monotonicity). Forbidden from assuming the crown OR relying on small κ. **Explicitly scopes the endogenous-bound question OUT** (Series 11).

**Status: Partial (built 2026-07-11); per-step fold RELABELED DEFINITIONAL (series-review-1 S2).** The verdict `.partialV` stands and is honest (settled by the kill condition, never assumed, no small-κ reliance). But the sub-claim "Discharged-on-scaffold" is RETRACTED: `ws5_fold_on_scaffold`'s proof discards `_hreach` and reads back `reifyStep`'s definition (`reifyStep Ω` contains `reify s` by construction), so it holds for ANY `Ω`, reachable or not — it is a DEFINITIONAL membership fact, NOT a discovered reflexivity or a substantive scaffold discharge. Docstrings relabelled DEFINITIONAL. The Partial's real positive content is thin; the **substantive fold question is fully open**: the free residue `diag insp` is a `HoldPred`, NOT a κ-bounded `PkObj κ` pattern, so whether it folds back is undecided, and forcing it would be κ-by-fiat. That residue-reifiability gap + κ-removal is Series 11's. **FATAL** pre-registered (`ws5_fatal_kill_condition_shape`). `ws5_fold_not_cardinality` certifies the fold is reflexivity, not `FoldsByCardinality` (κ-by-fiat PASS). The crown is never folded into `reify`/`reifyStep`.

### WS6, The heuristic ceiling and the Series 11 handoff · *the honest boundary*
**Status: Discharged (built 2026-07-11).** `ws6_provable_core` (the floor: productive blindness ∧ CLOSE-forbidden ∧ the per-step fold); `ws6_blindness_scope` (per-`insp` blindness is the theorem, semantic-intendedness the thesis); `ws6_fold_scope` (per-step fold theorem + verdict `.partialV`); `ws6_series11_handoff` (carries the fold verdict into the pre-declared Series 11 inheritance); `ws6_universal_theses` (the defended universals, documented not proved). Explicitly names the Series 11 inheritance: the κ-removal, the finiteness-of-attention unification, the fold verdict. The universal "every κ-bounded reifying carrier grows freely" is a defended thesis (the carrier is parameterized, not exhibited — design adjustment #3).

### WS7, The anti-circularity audit · *owns the verdict*
**Status: Discharged, verdict `bookkeeping` (Re-graded, series-review-1 S1, 2026-07-11).** The mechanized `Audit` is discharged (`ws7_audit`), but its flagship field is now `growthBookkeeping` (the reified relatum bisim-embeds, `ws2_reify_bisim_embeds`), so `ws7_verdict = bookkeeping` (`ws7_verdict_eq`, `rfl`; `ws7_audited_is_bookkeeping`), provably never `reificationEstablished`/`selfTotalSmuggled`/`kappaByFiat`/`Circular` given the certificate (`ws7_audited_not_reificationEstablished` etc.). The honest headline: **engine Discharged, payoff Bookkeeping.** The promoted checks: `ws7_bookkeeping_check` now DEMONSTRATES the moving-hole re-hit (the tower bisim-embeds, S1); `ws7_kappa_discipline` (fold reflexivity, all large κ — κ-by-fiat genuinely avoided, PASS). `ws7_close_forbidden_inspection_level` (S3 honest restatement), `ws7_spine_is_residue_freeness` (R1 honest restatement), `ws7_strip_ledger` (surfaces S1/S2/S3: growth strips to bisim-embedding, fold to `reifyStep`'s definition, CLOSE to the inspection-level diagonal). All axiom-clean (`spec/axiom-check-log.md`, R2 executed).

## The result tracker

| Result | WS | Status | Non-circular? |
|---|---|---|---|
| Freeness through the diagonal (R1 disclosed) | WS1 | **Discharged (residue-freeness)** | `ws1_free_reification` = `ws2_residue_is_import`, routes through `ws1_no_self_total_hold`. But of the residue CONTENT (`insp` only); `reify` absent, the reification lift is interpretive (R1). |
| CLOSE forbidden (inspection level, S3) | WS1/WS4 | **Impossibility proved (inspection level)** | `ws4_close_forbidden`: no self-total hold (`IsTotalityRelatum := SelfTotal`), tower-INDEPENDENT (`_hreach`/`_hmem` discarded). Carrier-level tower closure is charter §9's open question (S3). |
| Strict internal growth | WS2 | **BOOKKEEPING (S1)** | Provably resists: `ws2_reify_bisim_embeds` / `ws2_growth_is_bookkeeping` — the collapse engine makes the reified `SHNE` relatum bisimilar to prior relata, so `Ω_{α+1}` DOES bisim-embed into `Ω_α`. Growth is cardinality-only. The pre-registered Bookkeeping terminus, a first-class success. |
| Breaks the Series 07 collapse | WS2 | **Not proved (subsumed by S1 Bookkeeping)** | The `labelLoop` facts (`ws2_free_label_survives`) are about a FIXED 2-state coalgebra, not the tower; retained as transcription. The collapse is NOT broken at the carrier level (all atomless relata bisimilar). |
| Reification preserves SHNE (NL) | WS3 | **Discharged** | `ws3_reify_preserves_SHNE`: a reified non-empty hereditarily-non-empty pattern is a full relatum, never a leaf. The one place `reify` does real, non-trivial work. |
| Endogenous tower order | WS3 | **Discharged** | `ws3_order_endogenous` (`prec := ReflTransGen reifyStep`, `Iff.rfl`); imported-ordinal branch refuted (`ws3_imported_order_refuted`). |
| The close-or-fold dichotomy | WS4 | **Discharged** | `ws4_dichotomy` exhaustive; CLOSE forbidden as an inspection-level Impossibility (`ws4_close_forbidden`), not asserted. |
| The fold (crown) | WS5 | **Partial (per-step DEFINITIONAL, S2)** | The per-step fold `ws5_fold_on_scaffold` is `reifyStep`'s definition read back (NOT a substantive discharge); the substantive residue-fold + κ-removal are fully open; FATAL pre-registered. `.partialV`, never assumed, never small-κ. |

## Open obligations register

1. **Productive blindness (free reification)**, WS1. The reified self-relation is free, routing through the diagonal. The whole engine. **Not started.**
2. **`reify` well-defined, neither bookkeeping-weak nor closing-strong**, WS1. **Not started.**
3. **Strict internal growth**, WS2. `Ω_{α+1}` does not bisim-embed into `Ω_α`. The Series 09 moving-hole repair. **Not started.**
4. **Reification preserves SHNE (NL) + endogenous tower order**, WS3. The first build. **Not started.**
5. **Well-foundedness of the tower** (the κ-scaffolding's job), WS3. **Not started.**
6. **CLOSE forbidden + the dichotomy exhaustive**, WS4. **Not started.**
7. **The fold, settled by kill condition**, WS5. **Open by design; must not be assumed; must not rely on small κ.** **Not started.**
8. **The Series 11 handoff** (κ-removal, finiteness-of-attention unification), WS6. **Not started (forecast only).**

**Recurring lesson carried from Series 07, 08, and 09:** the sharpest result is a proved impossibility (CLOSE-forbidden is one), and an honestly-reported Partial or refutation beats a smuggled success. Series 08 added: a theorem can hold and still fail to be the fact you claimed (coincidence). Series 09 added: a payoff can be genuine yet thin, narrating self-inspection while proving `Function.update` (the bookkeeping ghost). Series 10's payoff is built to be tested against exactly that at the carrier level: reporting growth as Bookkeeping (if `reify` is too weak) or the fold as FATAL (if the crown fails) are success outcomes, not failures. Neither the growth nor the plurality depends on the crown being *true*, only on its status being *settled*; and the whole series depends not on the bound being endogenous (that is Series 11), but on reification generating strict internal growth at least once, which Series 09's fixed field could not.

## Closed log

*(Phase A complete 2026-07-11: charter, charter-status, protocol. Phase B complete 2026-07-11: seven `spec/wsNN-design.md` + `spec/README.md`. Phase C complete 2026-07-11: seven `formal/Series10/wsNN.lean` + `Series10.lean` + `AxiomCheck.lean` built, sorry-free and axiom-clean, `lake build Series10` green. Next action: Phase D, the blind whole-series review → `spec/series-review-1.md`.)*

**Phase C build results (2026-07-11), the headline delivered.** WS1 productive blindness **Discharged** (routes through the diagonal). WS2 strict growth **Discharged at the labelled level** (the reified free label survives the bisimulation quotient), with the plain-level collapse disclosed and **Bookkeeping pre-registered** as the honest alternative if the blind review judges the label external. WS4 **CLOSE-forbidden Discharged as an Impossibility** (a totality-relatum is a self-total hold). WS5 fold **Partial** (per-step / reifiable-pattern fold Discharged-on-scaffold; full residue fold + κ-removal open). WS7 verdict **`reificationEstablished`**, with the two promoted checks (bookkeeping, κ-by-fiat) built. Every headline reduces to `propext` / `Classical.choice` / `Quot.sound`.

**Four honest design adjustments discovered in the build (protocol §C — recorded, not silently retargeted).**
1. **WS1 `ws1_reify_section_not_iso` withdrawn; replaced by `ws1_diagonal_forbids_closure` (WS1) / `ws4_diagonal_forbids_closure` (WS4).** The design claimed "a surjective (iso) `reify` forces a self-total hold, so `reify` must embed." This is **NOT provable**: a κ-bounded section `reify : PkObj κ Ω → Ω` CAN be a bijection for suitable κ (`λ = λ^{<κ}`), because the diagonal bites at the INSPECTION level (the genuine unbounded content space `HoldPred = Hold → Prop`, via `ws1_insp_not_surjective`), NOT at the κ-bounded carrier map. The honest form: the diagonal forbids the closing top (the self-total hold) whatever the carrier's cardinality; whether the carrier iso `Ω ≅ F(Ω)` itself closes is charter §9's OPEN structural question, and Series 10 does NOT claim to settle it. This SHARPENS the charter §9 point rather than weakening it, and CLOSE-forbidden (WS4) is unaffected (it routes through the inspection-level diagonal). *This is a design correction, not a target-avoidance: the specified CLOSE-forbidden Impossibility is built; only the over-claimed carrier-non-surjectivity is withdrawn.*
2. **The "ordinal-indexed tower" realized as the index-free reachability `prec := ReflTransGen reifyStep` plus a concrete ℕ-iterate `towerN`.** The endogenous order (the load-bearing object WS4/WS5 consume) is `prec`, which is index-free ("reached by reification sequences") and needs no ordinal; the transfinite/limit indexing only matters for the fold's UNIVERSAL form, already deferred to Series 11. No signature's meaning changes; `reifyStep` and `prec` are exactly as designed.
3. **The reifying carrier is parameterized (a hypothesis `(dest, reify, IsReify dest reify, insp)`), not exhibited.** All theorems hold for ANY reifying carrier. The design's `ws1_reifying_carrier_exists` (D5) — a concrete witness needing a cardinal fixed point `λ = λ^{<κ}` — is **NOT mechanized**; it is a standard large-κ fact reported as a scope thesis (charter §5.3, WS6), and the universal theorems do not depend on it. Productive blindness is Discharged for every carrier, so this is Discharged, not Partial-on-a-witness.
4. **The fold verdict is Partial because of the residue-reifiability gap.** `ws5_fold_on_scaffold` proves the fold for reifiable PATTERNS (Discharged-on-scaffold). But a free RESIDUE `diag insp` is a `HoldPred` (a predicate over `Hold`), NOT a κ-bounded `PkObj κ` pattern, so it need not be reifiable; forcing it to be would be κ-by-fiat. This gap (plus κ-removal) is the genuine open κ-tension, handed to Series 11 — so the verdict is honestly `.partialV`, with FATAL pre-registered (a residue too large to reify), NOT a bare Discharged-on-scaffold. This is the charter's atom-or-will door, honestly left open.

**The two most important build checks (protocol §C), recorded.**
- **Bookkeeping check (WS2) — SUPERSEDED by the Phase E S1 re-grade (see series-review log).** The Phase C build reported growth "Discharged at the labelled level" via the old `ws2_growth_strict` (the `labelLoop` fact). Phase D (series-review-1 S1) correctly showed this is a fact about a FIXED 2-state coalgebra, not tower growth, and that the specified tower non-embedding provably resists on the plain carrier. Phase E relabelled the payoff **Bookkeeping** (`ws2_reify_bisim_embeds`: the reified relatum bisim-embeds) — the honest, pre-registered terminus. The old `labelLoop` theorems are retained only as transcription (`ws2_free_label_survives`).
- **κ-by-fiat check (WS4/WS5):** the fold predicate `Folds` is distributed REFLEXIVITY (`ws4_fold_is_reflexivity`, a reachability), NOT the cardinality bound `FoldsByCardinality`; `ws5_fold_on_scaffold` rests on `reifyStep`'s definition and holds for ALL large κ (no reliance on small κ). The endogenous bound is NOT claimed (Series 11's). `ws7_kappa_discipline` certifies both.

**Phase B design decisions (2026-07-11), the two settled design duties and the pre-registered headline.**
- **Duty 1 — `reify` defined once (WS1, `spec/README.md` §2.4).** The forward map of `Ω ≅ F(Ω)` is pinned as a **section** of `dest`: `IsReify dest reify := ∀ s, dest (reify s) = s`, so `reify` is injective (mints distinct relata from distinct patterns) and grows the carrier as a SET (`ws1_reify_injective`). Consumed by WS2–WS6; no workstream assumes a different `reify`. **(R3 correction, 2026-07-11):** the pre-build design's `ws1_reify_section_not_iso` / `ws4_reify_must_embed` ("a surjective `reify` forces a self-total hold, so `reify` must embed, not iso") were **withdrawn as unprovable** at build time (adjustment #1 below): a κ-bounded section CAN be a bijection, and the diagonal bites at the inspection level, not the carrier map. So the §4.5 "razor" is NOT settled — whether `reify` is an embedding or an iso (whether the founding equation closes) is charter §9's OPEN question. Those two theorem names do not exist in the code.
- **Duty 2 — the tower order derived once (WS3, `spec/README.md` §2.5).** `prec dest reify := ReflTransGen (fun a b => b = reifyStep dest reify a)`, endogenous by definition (`ws3_order_endogenous`, `Iff.rfl`), consumed by WS4 (close-or-fold) and WS5 (fold). The imported-ordinal branch is designed in as a refuted failure mode (`ws3_imported_order_refuted`): `≺` is a generated reachability, not a pre-assigned linear clock severable from `reify`.
- **Pre-registered headline (design predictions, NOT results — nothing built).** WS1 productive blindness **Discharged** (routes through the diagonal via `ws2_residue_is_import`). WS2 strict growth **Discharged at the labelled level** (`ws4_free_label_is_import`-shaped: the reified relatum's free label survives the bisimulation quotient), with the plain-level collapse disclosed and **Bookkeeping** pre-registered as the honest alternative if a reviewer judges the label external. WS4 **CLOSE-forbidden Discharged as an Impossibility** (a totality-relatum is a self-total hold), the sharpest result. WS5 fold **Partial** (Discharged-on-scaffold per-step — every residue reified at the next stage, measured as reflexivity not cardinality; universal/κ-removal open, handed to Series 11), with **FATAL** pre-registered as the refutation. Predicted verdict (WS7): **`reificationEstablished`**, with the two promoted checks — bookkeeping (growth not-bisim-embed, not a `List`) and κ-by-fiat (fold reflexivity, all large κ) — the spine of the audit. The one genuine scope hazard (charter §5.3): productive blindness may be Discharged only per-witness, the universal a WS6 thesis.

**Process change carried into Series 10 (the anti-loop discipline, protocol §0.2, §0.2a, Phase D recurrence check, Phase E sequence).** Diagnosed from Series 08, where the spine finding S1 recurred across all three review passes: each Phase E "addressed" it by building a new, cheaper theorem that established the antecedent of the charter's own pre-registered Failed condition and labelled the result a success, rather than reporting the Partial the charter had anticipated; the loop stopped only when pass 3 did what pass 1 directed. Series 09 is the positive contrast (F-8 Fixed by building the specified target; F-1 Relabeled with a real cardinality reason; no recurrence). Three guards added, all pre-build so Series 10 inherits them from the start: (1) Phase E must locate and consult the charter's pre-registered honest-failure outcome BEFORE reaching for the compiler, and report it when the finding shows it has been met; (2) every SERIOUS finding closes in exactly one named way, **Fixed** (originally-specified target built) or **Relabeled** (target not built, obstruction recorded, payoff demoted), with "adjacent weaker theorem" prohibited; (3) Phase D pass N+1 runs a recurrence check first, re-grading any finding "addressed" by a target-avoiding theorem as SERIOUS/RECURRING. This is a working-process change only; the charter (the mathematical bar) is untouched.

## Series-review log

### Pass 1 (`spec/series-review-1.md`, 2026-07-11) → addressed Phase E (2026-07-11)

The blind review's bottom line was correct and is accepted: **the engine parts are Discharged and honest, but the payoff — strict internal carrier growth — is not proved, and the honest terminus is Bookkeeping.** Three SERIOUS findings (S1/S2/S3), three REAL (R1/R2/R3), one COSMETIC (C1). Per protocol §0.2a / Phase E, each SERIOUS finding was closed by first locating the charter's pre-registered honest-failure outcome, asking whether the finding shows it is met, and reporting it when it is. **All three SERIOUS findings are Relabels to pre-registered outcomes, not new adjacent theorems** — a first-class success (charter §7, §8.1).

- **S1 (WS2, the payoff) — Growth is Bookkeeping. → RELABELED to Bookkeeping (Fixed-by-reporting).** Pre-registered failure: charter §5.5 ("if it does embed, growth is bookkeeping") and §8.1 ("`reify` too weak → Bookkeeping, reported honestly"). The finding shows the antecedent is MET: the specified target (`Ω_{α+1}` does not bisim-embed into `Ω_α`) is provably FALSE on the plain carrier, because the collapse engine `ws1_atomless_bisim` makes every reified `SHNE` relatum bisimilar to prior relata. Reported rigorously by building the DEMONSTRATION (not a new growth theorem): `ws2_growth_is_bookkeeping` and `ws2_reify_bisim_embeds` prove the tower bisim-embeds (growth is cardinality-only). The old `ws2_growth_strict`/`ws2_growth_is_free_label`/`ws2_history_not_merged`/`ws2_not_moving_hole` are removed or retained-as-transcription (`ws2_free_label_survives`, `ws2_label_free_import`) with the strict-growth claim RETRACTED. The WS7 verdict is re-graded to `bookkeeping`. **No third `labelLoop` theorem was built.**
- **S2 (WS5, the fold) — "Discharged-on-scaffold" is a definitional tautology. → RELABELED to definitional (the verdict Partial stands).** Pre-registered: the fold verdict is Partial (charter §5.4), settled by the kill condition, never assumed. The finding shows the per-step fold is `reifyStep`'s definition read back (`_hreach` discarded, holds for any `Ω`). `ws5_step_fold`/`ws5_fold_on_scaffold` docstrings relabelled DEFINITIONAL; the "Discharged-on-scaffold" sub-claim retracted; the verdict `.partialV` retained with its real content stated as the fully-open residue-fold. No theorem changed (it is true); its label did.
- **S3 (WS4, CLOSE-forbidden) — decoupled from `reify`/the tower. → RELABELED to inspection-level (Fixed-by-restating).** Pre-registered: charter §9 leaves the carrier-level closure OPEN. The finding shows `ws4_close_forbidden` discards the tower hypotheses (`_hreach`/`_hmem`); its content is `ws1_no_self_total_hold` (inspection level, `insp` free). Restated honestly: CLOSE-forbidden is the tower-INDEPENDENT inspection-level diagonal (a real Impossibility); the "the tower cannot close into a top" gloss is RETRACTED and the carrier-level closure named as charter §9's open question. Consistent with adjustment #1. No theorem changed; its claim did.
- **R1 (WS1) — the spine is Series 09 residue-freeness, not reified-relatum freeness. → DISCLOSED.** `ws1_free_reification` routes through the diagonal (genuine) but of the residue CONTENT (`insp` only); `reify` is absent, and the design's `ReifiedResidueRecoverable` was `ResidueRecoverable` renamed. Docstring + status now state the spine is transcribed residue-freeness with the reification lift interpretive, not machine-checked.
- **R2 (AxiomCheck) — axiom pass asserted, not executed in the review env. → EXECUTED.** `lake build Series10.AxiomCheck` run; every headline reduces to `propext`/`Classical.choice`/`Quot.sound` (or no axioms for `ws6_series11_handoff`); no `sorry`/`admit`/custom axiom. Captured in `spec/axiom-check-log.md`.
- **R3 (charter-status Duty-1 summary) — stale theorem names. → FIXED.** `ws1_reify_section_not_iso` / `ws4_reify_must_embed` (dropped as unprovable, adjustment #1) removed from the Phase B Duty-1 summary.
- **C1 (WS3 naming) — `ws3_tower_well_founded` is monotonicity. → FIXED.** Renamed `ws3_tower_monotone_family`, docstring states well-foundedness of `≺` is not claimed.

**Net:** verdict re-graded `reificationEstablished` → **`bookkeeping`** (the honest headline: engine Discharged, payoff Bookkeeping). The build remains sorry-free and axiom-clean. This is the pre-registered outcome the charter anticipated and the correct seed for Series 11.

---

*No em dashes in final academic copy. The crown is open by design; reporting it FATAL is a success. The payoff may fail productively; if `reify` is too weak, report growth honestly as Bookkeeping. The imported κ is scaffolding, named as such, removed only in Series 11. The entire series is the test of one question Series 09 left open: does the free residue, reified, GROW the world, or only move within it? Reification is the candidate answer, and it is not assumed to work. The structure is self-referential; whether it can therefore bound itself is the question after this one.*
