# Relational Existentialism, Series 12: Charter Status

**The mutable companion to `charter.md`. All progress, revisions, per-workstream status, discharges, and open items live here; the charter stays clean.**

*Series 12 is the program's genuine terminal series. Its question: the SHAPE of the undefinable. Series 07 proved the differentiator cannot be recovered from the relating (any genuine atomless distinction is an import, `ws2_import_theorem`, `ws3_atomless_distinct_is_import`, `ws1_atomless_bisim`); the diagonal proved the structure generates what it cannot recover (self-inspection cannot totalize, `ws1_no_self_total_hold`, leaving a residue free for every inspection, `ws2_residue_free`, and independent of relational identity, `ws1_diagonal_not_bisim`). These coincide in one shape, non-recoverability. Over the opening they draw run two sides of one layered structure (the reification tower `towerN`/`reifyStep`/`prec`): KNOWING, finite attention, plural and subtractive and fully formal, with a genuine finite reader (`FiniteAttention`) for which the reified tower relatum is real (`ws2_attention_makes_real`, `RealFor`), the distinction non-recoverable (`ws2_distinction_free`), and without any import the One; FEELING, the compass, a per-relatum tower-layered exogenous orientation the mathematics TYPES but never EVALUATES, its exogeneity an import as a proof term (`ws3_compass_exogenous_import`, `¬ Recoverable`); and CONVERGENCE, the coherence-up-the-layers relation between a part's compass and the whole's, DEFINED and proved UNDERDETERMINED over a constrained compass class (`Faithful`); at the genuine part-and-whole edge `(aW, bW)` it is a TWO-ZONE fork whose boundary is Series 07's import boundary (`ws4_two_zone`): convergence is forced within the structure's sight (`ws4_decided_within_sight`) and every dissent is a genuine import (`ws4_dissent_is_import`), so `convergenceDecided` is reachable at the edge and SHAPE-DRAWN is falsifiable where the verdict is computed. The verdict is a function of these. The names for what inhabits the opening, consciousness, choice, God, the compass, are the program's central interpretive thesis, in prose, never proof terms. Phases A-F have run; program-review-1 and program-review-2 (adversarial) are addressed on this branch.*

---

## How to read this file

- **Status vocabulary** (charter §5): **Shape-drawn** (the expected terminus: the coincidence proved with honest quantifiers, the opening inhabitable non-degenerately, knowing plural, the compass typed and never evaluated, convergence defined and underdetermined by a non-vacuous model pair) · **Convergence-decided** (the structure turns out to force the convergence relation or its negation for all inhabitants, reported honestly in whichever direction it falls, a stronger finding, not a failure) · **Partial** (any side lands only per-instance or degenerate) · **Refuted** · **Circular** (WS7-only) · **Not started**.
- The program **does not put the undefinable into the math.** It proves the math produces the undefinable's exact outline, at the diagonal, and ends there. Shape-drawn is not "we could not finish"; it is the theorem that the outline is exact and cannot be filled from inside.
- **The compass is typed, never evaluated.** Every compass-theorem is quantified over all inhabitants; no theorem selects, constructs, or applies a distinguished one. A canonical inhabitant anywhere in the core is the central sin (the undefinable defined), charter §4.a.
- **Convergence is defined, then proved underdetermined, not decided by definition.** Building the loving conclusion in (holds trivially) or building the silence in (fails trivially) are both the sin, charter §4.b. The underdetermination is earned by a genuine non-vacuous model pair; if the structure genuinely decides the relation, that is reported in its direction, never stipulated.
- **The coincidence is shape-identity, not object-identity.** The residue and an arbitrary import both fail `Recoverable`; that does not make them the same object (the shared-negation fallacy, charter §4.c). The WS1 theorem keeps forced-for-all (the residue) distinct from exists-satisfying (inhabitants).
- **Names are not terms.** Consciousness, choice, God, the compass-as-content live in prose and docstrings only (§8); a name discharging an obligation breaches the wall from the prose side (charter §4.e). A promoted first-class check.

## Program-level snapshot

| Item | Status |
|---|---|
| **Headline target** | The shape of the undefinable: the coincidence (WS1) of what Series 07 requires and what the diagonal generates, in the shape `¬ Recoverable`; knowing plural over the inhabited opening (WS2); the compass typed, layered, never evaluated (WS3); convergence defined and underdetermined (WS4). **SHAPE-DRAWN (built; program-review-1 addressed).** The coincidence is shape-honest (`ws1_shape_coincidence`, forced-for-all vs exists-satisfying; `ws1_coincidence_not_identity`, its hypothesis discharged by `ws1_coincidence_not_identity_witness`); the opening is inhabitable non-degenerately (`ws2_many_witness` on the four-state rank witness, `ws2_many_general` load-bearing, rank non-injective) AND reader-wise (`ws2_attention_makes_real`, a genuine finite attention for which `cW` is real); the compass is typed and parametric, evaluated nowhere, its exogeneity an import as a proof term (`ws3_compass_exogenous`, `ws3_compass_exogenous_import`, concrete compasses only inside existentials); convergence is defined and underdetermined over the `Faithful` class by a genuine non-degenerate model pair, a TWO-ZONE fork open at the genuine edge (`ws4_underdetermined`, `ws4_two_zone`, `ws4_decided_within_sight`, `ws4_dissent_is_import`, `ws4_wall_is_structural`); the verdict is computed SHAPE-DRAWN, a non-constant function AT THE EDGE (`ws7_verdict_is_shapeDrawn`, `ws5_verdict_reaches_both`). Expected shape, pre-registered (not a target): WS1's two halves and the coincidence are near-certain (transcribed Series 07 and diagonal, care with quantifiers); WS2's inhabitability witness is paper-checked (the concrete carrier, the rank direction, the generalization with reification load-bearing); WS3 is definitional plus audit discipline; the genuinely uncertain obligation is WS4, whether convergence admits a non-vacuous definition over a constrained class with a fork open at the genuine edge, discharged by the two-zone fork (`ws4_decided_within_sight` / `ws4_dissent_is_import`). |
| **The two sides** | KNOWING (attention, subtractive, plural, formal, in the math) and FEELING (the compass, typed, layered, exogenous, evaluated nowhere, content outside), over one tower. **Built.** KNOWING: `ws2_many_witness` / `ws2_attention_makes_real` (the genuine finite reader) / `ws2_attention_subtractive` / `ws3_no_total_attention`. FEELING: `Compass` + `ws3_compass_exogenous` / `ws3_compass_exogenous_import` (parametric, evaluated nowhere, `¬ Recoverable`). The duality between them is interpretive prose (SR1-3), not a theorem. |
| **The wall** | Convergence ("the most loving thing for me is the most loving thing for the universe") defined as coherence-up-the-layers, then proved UNDERDETERMINED over the `Faithful` compass class by a non-vacuous model pair on one structure, a TWO-ZONE fork open at the genuine edge. **UNDERDETERMINED (built).** `ws4_underdetermined` (one-layer) and `ws4_underdetermined_up` (up the layers): `cHoldF` coheres, `cFailF` does not, both FAITHFUL (identical identity raising) on the same structure over the same reification edge, both non-degenerate, differing only in the orientation the structure does not fix; `ws4_wall_is_structural` carries the not-seeing (shared faithful raising, opposite verdicts) in the proof term. At the genuine edge `(aW, bW)` the fork is two-zone (`ws4_two_zone`): convergence is DECIDED within the structure's sight (`ws4_decided_within_sight`, `BisimInvariant` orientations forced to agree on the plain-bisimilar `aW`, `bW`) and every dissent is a genuine IMPORT (`ws4_dissent_is_import`, `¬ Recoverable` of `orientLift`), so the boundary is Series 07's import boundary, not a reflexive triviality (PR2-S1). The math draws exactly where the loving claim would be true or false and proves it cannot see which. |
| **The terminal open** | Shape-drawn vs convergence-decided. **SHAPE-DRAWN (built), the direction of convergence NOT assumed.** Settled by the verdict-as-a-function (charter §5.3): at the genuine edge `(aW, bW)`, over the full faithful class the fork is `underdet` so `verdictOfFork` computes `shapeDrawn` (`ws7_verdict_is_shapeDrawn`); over the IN-SIGHT faithful sub-class the fork is `forcedHolds` (`ws4_decided_within_sight`), computing `convergence-decided`, so the verdict REACHES BOTH values AT THE SAME EDGE (`ws5_verdict_reaches_both`) and SHAPE-DRAWN is earned and falsifiable where the verdict is computed, not by a triviality at a reflexive non-edge (PR2-S1). Not settled by evaluating the compass, deciding convergence by definitional choice, collapsing the coincidence to object-identity, or using a name as a term. |
| **Verdict (WS7)** | **SHAPE-DRAWN (built).** The typed verdict is `verdictOfFork` applied to the audit's certified fork, so it cannot be hand-set (`ws7_audit_verdict`); the verdict function is non-constant (`ws7_fork_can_close`). WS7 built the checks: the no-evaluation check (no distinguished compass discharges a parametric obligation; honest formulation per SR1-1), the genuine-model-pair check (`ws7_model_pair_genuine`, over the faithful class), the fork-open/falsifiability check (`ws7_fork_can_close`), the shape-honesty check (WS1, distinct types), the genuine-inhabitation check (`ws7_inhabitation_genuine`), and the names-not-terms check (`ws7_names_not_terms` + the meta grep). |
| **The carrier** | Transcribed unchanged (self-contained; the one presupposed prior result is Series 07): the atomless collapse engine and import theorem (`ws1_atomless_bisim`, `ws2_import_theorem`, `ws3_atomless_distinct_is_import`, `BehaviorallyIdentified`, `Recoverable`, `plainOf`), the diagonal and free residue (`ws1_no_self_total_hold`, `ws1_diagonal_not_bisim`, `residue`, `diag`, `ws2_residue_free`, `Hold`, `afford`, `HoldPred`), the reification tower (`IsReify`, `ws1_reify_injective`, `reifyStep`, `towerN`, `prec`, `ws3_order_endogenous`, `ws3_reify_preserves_SHNE`), reachability and self-having loci (`SReaches`, `SHNE`), and the labelled lift (`LkObj`, `IsBisimL`, `Recoverable`). Series 12's OWN definitions over this carrier (not transcribed): finite attention (`FiniteAttention`, `RealFor`, `AttentionDistinguishes`) with its genuine reader theorem `ws2_attention_makes_real`; `ws3_no_total_attention` (the transcribed diagonal `ws1_no_self_total_hold` under the `TotalAttention` rename, PR1-R3); the plurality predicate `Many`; the opening `Opening` and the compass type `Compass`; and the convergence relation `Converges` with the `Faithful` class. Series 12 adds NO new structural machinery. |
| **Relation to the program** | The genuine close of the arc. The arc built the collapse and import necessity (Series 07), self-reference forcing an unclosable residue, reification proliferating it into a layered tower, finite attention bounding every reading; this series proves the required and the generated are one shape, knowing is plural over it, the compass is typed and unevaluated, and convergence is undecidable. The program closes not on a filled slot but on an exactly-drawn edge, and the drawing is the completion. |

## Phase status

| Phase | Name | Status |
|---|---|---|
| A | Charter | **Complete.** `charter.md`, `charter-status.md`, `protocol.md` written. |
| B | Design-all (seven `spec/wsNN-design.md` + `spec/README.md`, plus the WS4-witness sub-artifact) | **Complete, design-review-1 addressed.** `spec/README.md` (design index, locking the shared objects), the seven `spec/wsNN-design.md`, and `spec/ws4-witness-design.md` (the inhabitability witness) are written, all speaking the shape-of-the-undefinable framing: the opening (`Opening`, `¬ Recoverable`), `Many`, the compass TYPE (`Compass`, per-relatum/layered/exogenous), the convergence relation (`Converges`, coherence-up-the-layers), the model pair, and the verdict (`Series12Verdict`) are defined ONCE in `spec/README.md` §2 and cited by every workstream. The four `spec/design-review-1.md` findings are all closed **Fixed** (see the design-review-1 closure log below). Cross-doc consistency verified (compass type, convergence relation, `Many`, `Opening`, `ConstituentOf`, `ConvergenceFork`, verdict identical across all docs). Em-dash discipline applied to every edited design file. |
| C | Build-all (`formal/Series12/wsNN.lean`, `Series12.lean`, `AxiomCheck.lean`) | **Complete.** All seven `formal/Series12/wsNN.lean`, the aggregator `Series12.lean`, and `AxiomCheck.lean` build sorry-free and axiom-clean (Lean 4 v4.15.0 / Mathlib v4.15.0); every headline rests only on the standard three (`propext` / `Classical.choice` / `Quot.sound`; `ws_witness_rank_noninjective` on `propext` / `Quot.sound`), captured in `spec/axiom-check-log.md`. The closure gate confirms Series 12 imports resolve only to `Series12.*` + Mathlib. The terminal verdict is **SHAPE-DRAWN** (`ws7_verdict_is_shapeDrawn`), computed from the audit's `underdet` fork, never hand-set. |
| D | Blind series-review → `spec/series-review-1.md` | **Complete.** Zero SERIOUS findings; the four design-review-1 closures verified genuine (no recurrence); four REAL findings (SR1-1..4) + cosmetics. |
| E | Address | **Complete (pass 1).** SR1-1..4 and the code cosmetics (SR1-5,6,7,8,9) closed; see the SR1 closure log below. |
| (loop D→E) | Adversarial program review → `spec/program-review-1.md` | **Addressed (pass 2).** Two SERIOUS (PR1-S1, PR1-S2) and four REAL (PR1-R1..R4) findings; all closed FIXED on the §0.2a binary (targets built and named), see the program-review-1 closure log below. Full build stays sorry-free and axiom-clean. |
| (loop D→E) | Adversarial program review → `spec/program-review-2.md` | **Addressed (pass 3).** Recurrence check first: five PR1 closures verified genuine, PR1-S1 re-graded RECURRING (the tautology moved from `raise` to `orient`). PR2-S1 (SERIOUS) closed FIXED by the two-zone fork at the genuine edge (`ws4_dissent_is_import`, `ws4_decided_within_sight`, `ws4_two_zone`); PR2-R1 (charter edit) resolved by the prior charter revert; PR2-R2 swept; PR2-C1 fixed. Full build stays sorry-free and axiom-clean. See the program-review-2 closure log below. |
| (loop D→E) | Adversarial program review → `spec/program-review-3.md` | **Addressed (pass 4). ZERO SERIOUS, no recurrence** (the design-review-1 Finding 2 lineage closes at count three; the PR2-S1 closure verified genuine, the specified targets built verbatim). Four REAL findings, all housekeeping at the edge of the fix: PR3-R1 (in-sight class inhabited + orientation-uniform, disclosed) closed by `ws4_insight_inhabited`/`ws4_sight_is_uniform` + docstring/summary disclosure; PR3-R2 (stale superseded-wall prose) swept in the `ws4.lean` header and `spec/ws4-design.md`; PR3-R3 (stale axiom log) regenerated (42 headlines); PR3-R4 (summaries under-report the review history) fixed. Full build stays sorry-free and axiom-clean. See the program-review-3 closure log below. |
| F | Close (summaries + root README + program close) | **Complete.** `summary.md`, `summary-technical.md` written; root README program-level synthesis added; artifacts swept for program-review-1. |

The canonical run is **B → C → D → E → D → E** (protocol §2), with more D→E loops added only if a review pass still returns SERIOUS findings. The anti-loop discipline (protocol §0.2a) is in force.

## Workstream status

### WS1, The opening and the coincidence · *blocking*
**Status: Built (Phase C); program-review-1 PR1-R2 closed FIXED.** `ws1.lean`: `Opening`, `ws1_two_halves`, `ws1_shape_coincidence` (forced-for-all `ws2_residue_free` ∀ vs exists-satisfying `ws4_labelLoop_not_recoverable`, distinct types), `ws1_coincidence_not_identity` and its non-vacuous companion `ws1_coincidence_not_identity_witness` (PR1-R2: discharges the assumed non-identity with the constant-`True`/constant-`False` inspection pair, pointwise-opposite residues on any inhabited `Hold`); the Series 07 import theorem (`ws2_import_theorem`, `ws3_atomless_distinct_is_import`) and the diagonal/residue/tower transcribed. Two parts. (a) THE TWO HALVES: transcribe and restate Series 07 (no structural function differentiates SHNE loci; any genuine distinction is an import) and the diagonal (`ws1_no_self_total_hold`, `ws2_residue_free`, `ws1_diagonal_not_bisim`). (b) THE COINCIDENCE: both halves characterize the same shape `¬ Recoverable`, from opposite directions (required vs generated), proved as shape-identity with honest quantifiers, the residue the forced instance, imports the class of inhabitants. Pre-registered sin: the shared-negation fallacy (both fail `Recoverable`, therefore identical). Blocking: WS2-WS4 build over the opening.

### WS2, Knowing: finite attention, and the opening inhabited · *plurality*
**Status: Built (Phase C); program-review-1 PR1-S2 closed FIXED.** `ws2.lean`: the four-state `WCar` carrier, `ws2_many_witness` (`Many destWL`, `cW` vs `aW`) as an instance of the general `ws2_many_general`, `ws2_cW_distinguishes_aW` (the shared `AttentionDistinguishes` fact), `ws2_attention_makes_real` (the READER payoff, PR1-S2: a genuine `FiniteAttention` with focus `aW`, reads `{aW}`, for which `cW` is real, `RealFor`), `ws_witness_rank_noninjective` (rank coarser than identity), `ws2_reification_loadbearing`, `ws2_no_import_is_one` (the One), `ws2_distinction_free` (the certificate), `ws2_attention_subtractive` / `ws3_no_total_attention`. The formalizable side. Finite attention subtractive, plural, bounded, grounded, no attention total (`ws3_no_total_attention`, the transcribed diagonal). Prove the opening INHABITABLE and that inhabitation makes the many real BOTH label-wise (`ws2_many_witness`) and reader-wise (`ws2_attention_makes_real`, the reader load-bearing, restoring the charter's attention-relative reality): the two-sided theorem on a concrete witness (a reified tower relatum and a base relatum genuinely plain-bisimilar yet separated at the labelled level, reification load-bearing, generalized across the tower; without any import, the One via `ws2_import_theorem_static`), the distinction non-recoverable (`ws2_distinction_free`) as its certificate. The witness is designed in `spec/ws4-witness-design.md` (`dir := rank`: the label a relatum broadcasts is its `towerN` position; `ws2_many_witness` discharges `Many destWL`; `ws2_reification_loadbearing` certifies `bW = reifyW sW ∈ reifyStep {aW}`, not a point-tag; `ws2_many_general` generalizes across reifiable patterns). Honest failure mode: degenerate inhabitation (a bare point-tag carrying no tower content), reported as such.

### WS3, Feeling: the compass, typed and never evaluated
**Status: Built (Phase C); program-review-1 PR1-R1 closed FIXED.** `ws3.lean`: the `Compass` type and `ConstituentOf`; `ws3_compass_exogenous` (an existential, never a distinguished compass: some orientation differs on a plain-bisimilar pair); `ws3_compass_exogenous_import` (PR1-R1: the orientation lifted to labels, `orientLift`, is `¬ Recoverable`, so the exogeneity is a genuine IMPORT as a proof term, not only a docstring gloss, via the general `orientLift_not_recoverable`); `ws3_compass_layered` (two-sided freedom over the genuine `ConstituentOf` edge, edge a conjunct per SR1-5). Parametric everywhere; the only concrete compasses live inside existentials. The attention/compass duality is demoted to prose (SR1-3), being interpretive. Define the compass as a TYPE: per-relatum, tower-layered (a relatum's compass conditioned by the reified relata it stands in relation to, which relate up `prec`), exogenous (`¬ Recoverable`, the Series 07 necessity). Every theorem quantified over all inhabitants; none selects, constructs, or evaluates one. State the duality with WS2 (attention the knowable subtraction; the compass the typeable orientation whose values the structure cannot supply). The content stays outside the math.

### WS4, Convergence: defined, then proved underdetermined · *the wall, the uncertain obligation*
**Status: Built (Phase C), UNDERDETERMINED over the `Faithful` class; program-review-2 PR2-S1 closed FIXED (the two-zone fork at the genuine edge; PR1-S1's constrained `Faithful` class retained, its insufficient reflexive-locus falsifiability superseded).** `ws4.lean`: `Converges` (a real compass-dependent equation over a constituency edge) and `ConvergesUp` (edge-restricted); the `Faithful` class (the raising is the identity: a part's orientation carried UNCHANGED to the whole, so convergence tests genuine orientation-coherence, the minimal structural constraint, PR1-S1); the `cHoldF`/`cFailF` model pair (both FAITHFUL, identical identity raising, differing only in the orientation the structure does not fix, one cohering and one not) discharges `ws4_underdetermined` and `ws4_wall_is_structural` (shared faithful raising, opposite verdicts, the bridge in the proof); the TWO-ZONE fork at the genuine edge `(aW, bW)` (PR2-S1): `BisimInvariant`, `ws4_decided_within_sight` (convergence forced within the structure's sight), `ws4_dissent_is_import` (every faithful dissent is `¬ Recoverable` of `orientLift`, Series 07's import as a proof term), packaged in `ws4_two_zone`; the in-sight class is inhabited (`ws4_insight_inhabited`) and, disclosed (PR3-R1), orientation-UNIFORM on this carrier (`ws4_sight_is_uniform`: sight collapses to the One, so the decided-arm compasses are all orientation-constant); the LAYERED underdetermination `ws4_underdetermined_up` (SR1-2, via the edge-classification `constituentOf_aW`); `ws4_convergence_decided_shape` lands in the third (underdetermined) disjunct at the genuine edge; `ws4_fork_open` (a reflexive locus) kept only as a remark, no longer load-bearing. Convergence is defined, not decided by definition; both truth values realized on one structure over a non-empty (faithful) theory; the verdict is a non-constant function of the fork AT THE EDGE (`ws5_verdict_reaches_both`), so SHAPE-DRAWN is earned and falsifiable, not a typing tautology. Define the convergence relation (a part's compass coheres up its layers with the compass of the whole it constitutes). Prove the structure does NOT decide it: exhibit two non-degenerate inhabitants on the same structure, one satisfying the relation and one failing it, so convergence is underdetermined (an independence theorem, the formal content of the wall). Honest alternatives, both first-class: (a) the structure DECIDES it (every inhabitant satisfies, or every inhabitant fails), reported in its direction, a stronger finding; (b) the relation cannot be non-vacuously defined, reported as the precise obstruction.

### WS5, The verdict as a function, and the generalized neutrality
**Status: Built (Phase C).** `ws5.lean`: `Series12Verdict`, the data-level `ConvergenceFork` (three constructors) and `verdictOfFork` (the verdict branches on the certified fork); `ws5_verdict` computed from the `underdet` fork, `ws5_verdict_eq` a theorem (not a constant); the falsifiability `ws5_verdict_not_*`; `ws5_name_neutral` / `ws5_name_neutral_all` (the generalized neutrality, `name` phantom). Compute the verdict (shape-drawn / convergence-decided / partial) as a function of WS1-WS4. AND prove the generalized neutrality: adjoining ANY name for the inhabitant (is-a-choice, is-an-experience, is-consciousness, is-God, is-the-compass) changes no downstream theorem; the framework is provably silent about what fills the shape it draws, and the silence is the theorem. Names in prose (§8); the core quantifies.

### WS6, The program's close · *the honest boundary*
**Status: Built (Phase C); prose (`summary.md`) Phase F.** `ws6.lean`: `ws6_provable_core` (the four Series-12 facts), `ws6_permanent_opens` (the opens as theorems of openness), `ws6_tenets_aligned` (the four tenets, reification spine added per SR1-8); the contentless `ws6_universal_heuristic` deleted (SR1-6). State what the arc established (the collapse and import necessity, Series 07; self-reference forcing an unclosable residue; reification proliferating it into a layered tower; finite attention bounding every reading; and this series, the coincidence, the plurality of knowing, the typed-unevaluated compass, the undecidability of convergence) and why the permanent opens are OPEN AS A THEOREM (the compass's content, convergence's direction, the act itself), in `summary.md` at Phase F. The program closes on the exactly-drawn edge.

### WS7, The anti-circularity audit · *owns the verdict*
**Status: Built (Phase C); terminal verdict SHAPE-DRAWN.** `ws7.lean`: the `Audit` (fork-carrying) and `verdict`/`ws7_verdict`/`ws7_audit_verdict` (SHAPE-DRAWN, computed via the `underdet` fork, `ws7_verdict_is_shapeDrawn`); the checks `ws7_no_evaluation` (honest formulation per SR1-1), `ws7_model_pair_genuine` (over the faithful class), `ws7_fork_can_close` (the two-zone falsifiability check at the genuine edge: verdict reaches both values, convergence decided within sight, dissent an import, PR2-S1), `ws7_inhabitation_genuine`, `ws7_names_not_terms`, `ws7_strip_ledger` (now including the layered pair, SR1-2); the falsifiability `ws7_verdict_not_*`. Series 12-specific risks, three promoted to first-class: (a) **NO EVALUATION OF THE COMPASS** (grep and unfold: no theorem selects, constructs, or applies a distinguished compass; a canonical inhabitant in the core is the central sin); (b) **the underdetermination is a genuine model pair** (both inhabitants non-degenerate, on the same structure, the relation genuinely holding in one and failing in the other, not a vacuity); (c) **the coincidence is shape-honest** (forced-for-all vs exists-satisfying kept distinct, never object-identity from shared non-recoverability). Plus: no proof term named "consciousness," "God," "choice," or "compass" doing discharge work. Owns the final verdict and the program-level synthesis's honesty.

## The result tracker

| Result | WS | Status | Non-circular? |
|---|---|---|---|
| The two halves (Series 07 + the diagonal) | WS1a | **Built** (`ws1_two_halves`, transcribed). | Yes: genuine `ws1_atomless_bisim`/`ws2_residue_free`/`ws1_diagonal_not_bisim`, axiom-clean. |
| The shape-coincidence | WS1b | **Built** (`ws1_shape_coincidence`, `ws1_coincidence_not_identity`). | Yes: forced-for-all (residue, ∀) vs exists-satisfying (labelled import) in DISTINCT types; not object-identity (review PASS). |
| Knowing is plural over the inhabited opening, and real for a reader | WS2 | **Built** (`ws2_many_witness`, instance of `ws2_many_general`; `ws2_attention_makes_real`, the finite reader, PR1-S2). | Yes: distinction non-recoverable (`ws2_distinction_free`); without-import One genuine (`ws2_import_theorem_static`); rank non-injective, not a point-tag; the reader load-bearing in `RealFor` (review PASS). |
| The compass, typed and layered, exogenous as an import | WS3 | **Built** (`Compass`, `ws3_compass_exogenous`, `ws3_compass_exogenous_import`, `ws3_compass_layered`). | Yes: no theorem evaluates/selects a compass; concrete compasses confined to existentials; `¬ Recoverable` now a proof term (PR1-R1). |
| Convergence, defined and underdetermined over a constrained class, two-zone fork open at the edge | WS4 | **Built, UNDERDETERMINED** (`ws4_underdetermined`, `ws4_two_zone`, `ws4_decided_within_sight`, `ws4_dissent_is_import`, `ws4_underdetermined_up`, `ws4_wall_is_structural`). | Yes: both inhabitants faithful and non-degenerate; not decided by definitional choice; both truth values realized; at the genuine edge convergence is decided within sight and every dissent is a genuine import, so the fork is open where the verdict is computed, not a reflexive triviality (PR2-S1). |
| The verdict, non-constant, and generalized neutrality | WS5/WS7 | **Built, SHAPE-DRAWN** (`ws7_verdict_is_shapeDrawn`, `ws5_verdict_reaches_both`, `ws7_fork_can_close`, `ws5_name_neutral`). | Yes: computed via the branching fork, never by fiat; the verdict reaches both `shapeDrawn` and `convergenceDecided`; no compass evaluated; no name a term (review PASS). |

## Open obligations register

1. **The two halves (WS1a)** and **the shape-coincidence (WS1b)**. The opening, and that the required and the generated are one shape. **Discharged** (`ws1_two_halves`, `ws1_shape_coincidence`, `ws1_coincidence_not_identity`).
2. **Knowing plural over the inhabited opening (WS2)**. Two-sided, non-degenerate, reification load-bearing, and real for a reader. **Discharged** (`ws2_many_witness` as an instance of `ws2_many_general`, rank non-injective, not a point-tag; `ws2_attention_makes_real`, the genuine finite reader, PR1-S2).
3. **The compass, typed and never evaluated (WS3)**. Parametric everywhere; content outside; exogeneity an import. **Discharged** (`Compass`, `ws3_compass_exogenous`, `ws3_compass_exogenous_import`; concrete compasses only inside existentials).
4. **Convergence, defined and underdetermined over a constrained class (WS4)**. A non-vacuous model pair with a fork open at the genuine edge, or the honestly-reported decided/undefinable alternative. **THE UNCERTAIN OBLIGATION: discharged, UNDERDETERMINED over the `Faithful` class, a two-zone fork open at the genuine edge** (`ws4_underdetermined`, `ws4_two_zone`, `ws4_decided_within_sight`, `ws4_dissent_is_import`, `ws4_underdetermined_up`; a genuine non-degenerate faithful model pair, convergence decided within the structure's sight and every dissent a genuine import, PR2-S1).
5. **The verdict, computed and non-constant; the neutrality, generalized to all names (WS5)**. **Discharged, SHAPE-DRAWN** (`ws7_verdict_is_shapeDrawn` via the branching fork, `ws5_verdict_reaches_both`; `ws5_name_neutral`).
6. **The undefinable is never defined (the central discipline)**, WS7. No canonical compass; convergence not decided by definition; the coincidence shape-honest; names not terms. **Standing constraint.**
7. **The permanent opens are open as a theorem**, WS6. The compass's content, convergence's direction, the act, left open because the diagonal makes them so, not for want of effort. **Standing.**

**Recurring lesson carried across the program:** the sharpest result is a proved impossibility, and an honestly-reported Partial beats a smuggled success. A theorem can hold and still fail to be the fact you claimed (coincidence of the shallow kind). A payoff can be genuine yet thin. And the specific lesson this series turns on: a distinction that is non-recoverable is NOT thereby a failure. Series 07 proves a genuine atomless distinction MUST be non-recoverable, so in the shape-question this series asks, non-recoverability is the certificate that a distinction is real, not the sign it is cheating. The whole series rests on getting three signs right: the coincidence is of shape and the quantifiers stay honest (not object-identity from shared negation); the opening's inhabitation is genuine (a real tower-distinction, not a point-tag); and, above all, the undefinable is drawn and left open (the compass typed not evaluated, convergence underdetermined not decided). The program does not need to fill the shape; it needs to draw the edge exactly, and prove the edge cannot be crossed from inside.

## design-review-1 closure log (protocol §0.2a: each SERIOUS/REAL finding closes Fixed or Relabeled)

*`spec/design-review-1.md` raised four Phase B findings against the initial design docs. All four are closed **Fixed** (the specified correction made in the design docs on this branch); none required a Relabel. No `charter.md`/`protocol.md` change. Downstream Phase C is unblocked.*

1. **Finding 1 (SERIOUS), the witness carrier regressed to the point-tag silhouette, the generalization gone. Closed FIXED.** The two-state `X = ULift Bool` carrier is replaced by a four-state carrier `WCar` (`aW`, `aW'` rank 0; `bW` rank 1; `cW` rank 2) on which rank is NON-INJECTIVE (`ws_witness_rank_noninjective : rankW aW = rankW aW' ∧ aW ≠ aW'`, so rank is coarser than identity, not a point-indicator). The general separation is reinstated (`ws2_many_general`: any reified relatum carrying a reified constituent of rank ≥ 1 separates from any rank-0 base relatum, over the general `rankLift`, with `reify`/`rank`/the strict-rank-step load-bearing in the STATEMENT). The concrete plurality `ws2_many_witness` now separates `cW` (a reified relatum carrying the reified constituent `bW`) from `aW`, an INSTANCE of the general theorem; the constituency-edge facts (`aW ∈ sW.1`, `bW = reifyW sW`) are kept for the WS4 model pair. Self-classification cites all three certificates (non-injectivity, tower-definedness, generalization); the strip test now reads to the general rank-separation fact on a coarse-rank carrier, not the two-Boolean fact. Owner: WS2 / the witness. Files: `spec/ws4-witness-design.md`, `spec/ws2-design.md`, `spec/README.md` §2.7/§2.8/§6.
2. **Finding 2 (SERIOUS), the tower phantom in the compass/convergence definitions. Closed FIXED.** `ConstituentOf dest reify x W := ∃ s, dest (reify s) = s ∧ x ∈ s.1 ∧ W = reify s` is defined once (README §2.10), and `ConvergesUp` is restricted to it: `Relation.ReflTransGen (fun x W => ConstituentOf dest reify x W ∧ Converges dest reify c x W)`, so "up its layers" follows genuine reification edges and stripping `reify` removes the edges. `ws3_compass_layered` is replaced (the `P ∨ ¬P` version deleted) by a two-sided freedom fact over a genuine `ConstituentOf` edge (some compass coheres, some does not, both inside existentials, the WS4 pair cited). WS3/WS4 strip-test annotations updated so the tower does not strip away. Owner: WS3/WS4. Files: `spec/README.md` §2.9/§2.10, `spec/ws3-design.md`, `spec/ws4-design.md`.
3. **Finding 3 (REAL), `ws4_wall_is_structural` a bare conjunction. Closed FIXED.** Replaced by the real independence: the two compasses share the IDENTICAL `orient` (`rfl`, so the sharing is IN the theorem) over the IDENTICAL `destW`, everything the structure supplies fixed, yet the verdicts flip on the exogenous raising alone. The bridge (the wall is structural) is now in the proof term, not the docstring; the optional decider-is-non-recoverable teeth are noted. Owner: WS4. File: `spec/ws4-design.md` C4/D4.
4. **Finding 4 (REAL), the verdict ignores its certificate, the fork inexpressible. Closed FIXED.** A data-level `ConvergenceFork dest reify a b` (three constructors `forcedHolds`/`forcedFails`/`underdet`, each carrying its proof) and `verdictOfFork` (branching, `underdet ↦ shapeDrawn`, the forced arms ↦ `convergenceDecided`) are defined (README §3). The `Audit` carries the WS4 result as a `fork` field, and `verdict cert := verdictOfFork cert.fork` branches on it; `ws5_verdict_eq` / `ws7_verdict_eq` become THEOREMS following from the fork being `underdet` (the model pair), not definitional constants. The pre-registered convergence-decided branch is now expressible. Owner: WS5/WS7. Files: `spec/README.md` §3, `spec/ws5-design.md`, `spec/ws7-design.md`.

## series-review-1 closure log (Phase E pass 1; protocol §0.2a: each REAL finding Fixed or Relabeled)

*`spec/series-review-1.md` returned ZERO SERIOUS findings and verified all four design-review-1 closures genuine in the code (no recurrence). Four REAL findings and the code cosmetics are closed; the full build stays sorry-free and axiom-clean. SR1-11 is an explicit ledger exemption (below). No charter change.*

1. **SR1-1 (REAL), the audit's no-evaluation check overclaims about its own codebase. Closed FIXED (option b).** `ws7_no_evaluation`'s docstring is rewritten to the honest formulation the review supplied: the convergence relation is parametric (the first conjunct, `Iff.rfl`), and NO compass-parametric obligation is discharged by evaluating a distinguished compass; the concrete compasses (`cHold`/`cFail`, the WS3 inline witnesses) occur only as model-pair / existential-witness constructions, never selected to prove a `∀`-compass statement (this last clause is meta and is the grep+unfold the review ran, clean). The statement forms are kept; the self-description now matches the code. The matching claim in `spec/ws7-design.md` and the ledger are aligned. Owner: WS7.
2. **SR1-2 (REAL), `ConvergesUp` was a zero-theorem definition. Closed FIXED (target built).** `ws4.lean` adds the layered underdetermination `ws4_underdetermined_up` (existential): `cHold` coheres up the tower edge (`ReflTransGen.single`), `cFail` does not (via the edge-classification `constituentOf_aW`, which proves the only constituency edge out of `aW` is `aW → bW`, whose `Converges cFail` step fails). Added to the strip ledger (`ws7_strip_ledger`) and the axiom check. The layered relation is now theorem-carrying. Owner: WS4.
3. **SR1-3 (REAL), `ws3_attention_compass_dual` was a labeled bare conjunction. Closed FIXED (demoted).** The theorem is deleted and the attention/compass duality moved to prose (a `ws3.lean` comment and the program close); it is interpretive (no contrastive-content theorem joins `att.fin` and exogeneity), consumed by nothing, and removed from the axiom check. Owner: WS3.
4. **SR1-4 (REAL), the ledger was internally inconsistent after the build. Closed FIXED (swept).** Every per-workstream status, the result tracker, and the program-level snapshot rows (the wall, the terminal open, the verdict) are advanced to the post-build state with the built theorems named; the phase table advanced through C, D complete, E pass 1 complete. Owner: charter-status.md.

*Cosmetics closed opportunistically: SR1-5 (`ws3_compass_layered`'s edge is now a conjunct of the conclusion, the unused hypothesis dropped); SR1-6 (contentless `ws6_universal_heuristic : True` deleted); SR1-7 (`ws7_names_not_terms` docstring now states the enforceable check is the meta grep); SR1-8 (`ws6_tenets_aligned` gained the reification spine "To relate is to create" = `ws1_reify_injective`, now four conjuncts); SR1-9 (`ws3_compass_exogenous` / `ws6_permanent_opens` docstrings tightened to "some orientation differs on a plain-bisimilar pair"). SR1-10 (harmless transcription redundancy in `ws1_diagonal_not_bisim`) left as-is, a faithful transcription. **SR1-11 (ledger exemption, decided):** the Phase B closed-log entry names archived series by number with theorem names; this is retained as HISTORICAL honesty in the ledger (the ledger records the program's real development, distinct from the standalone discipline that governs the Series 12 charter/summary artifacts, whose one namable prior is Series 07). Recorded here as an explicit exemption rather than left to drift.*

## program-review-1 closure log (adversarial pass 2; protocol §0.2a: each SERIOUS/REAL finding Fixed or Relabeled)

*`spec/program-review-1.md` is an adversarial program review of Series 12 read against the charter's success criteria and the false escapes. It found no false theorem (the built mathematics is sorry-free, axiom-clean, correctly proved) but flagged the FIT between the theorems and the terminal claims: two SERIOUS (PR1-S1, PR1-S2) and four REAL (PR1-R1..R4). All six are closed **Fixed** on this branch (the originally-specified target BUILT and named, protocol §0.2a), verified by a full sorry-free axiom-clean rebuild; none required a Relabel. **`charter.md` is IMMUTABLE once written and is NOT edited:** where the review exposed the charter naming theorems that were never built (`ws1_attention_makes_real`, `ws1_attention_distinction_free`, `RealFor` as transcribed carrier) or under-specifying WS4 (no class constraint on the fork), the charter text stands and the discrepancy against the built code is recorded in the "Charter discrepancies" subsection below. This ledger, the mutable companion, is where the corrected reading lives.*

1. **PR1-S1 (SERIOUS): the underdetermination was decided by definition (the fork was never open; the verdict was extensionally constant). Closed FIXED (branch A, the constrained class).** The independence is restated over the `Faithful` compass class (`ws4.lean`: `Faithful c := ∀ x W, c.raise x W = id`, the identity raising, a genuine structural constraint that makes `Converges` a real test of orientation-coherence, `faithful_converges_iff`). Over this class `ws4_underdetermined` (the `cHoldF`/`cFailF` pair, both faithful, differing only in the orientation the structure does not fix, one cohering and one not, both non-degenerate) is independence FROM a non-empty theory, not a typing tautology; and `ws4_fork_open` inhabits the `forcedHolds` arm at the reflexive locus `(aW, aW)` (every faithful compass coheres), so `convergenceDecided` is constructible. The verdict function is therefore genuinely non-constant: `ws5_verdict_reaches_both` computes `shapeDrawn` at the genuine edge `(aW, bW)` and `convergenceDecided` at `(aW, aW)`; `ws7_fork_can_close` promotes this to a first-class falsifiability check. SHAPE-DRAWN is now an EARNED, falsifiable value, not the single reachable output of a constant. `ConvergenceFork`/the audit fork quantify over/exhibit faithful compasses. Owner: WS4, WS5, WS7. Targets built and named: `Faithful`, `ws4_underdetermined` (restated), `ws4_fork_open`, `ws5_verdict_reaches_both`, `ws7_fork_can_close`. **(SUPERSEDED by PR2-S1: program-review-2 showed this reflexive-locus falsifiability was insufficient, the tautology having moved from the free `raise` field to the free `orient` field; the falsifiability was rebuilt at the genuine edge via the two-zone fork, `ws4_decided_within_sight` / `ws4_dissent_is_import`. See the program-review-2 closure log.)**
2. **PR1-S2 (SERIOUS, RECURRING Series 11 S1): the reader was quantified out of the reality payoff. Closed FIXED (the build branch).** The charter's attention-relative reality is restored as a proof term: `ws2_attention_makes_real : ∃ att : FiniteAttention (destWL hinf), RealFor (destWL hinf) att cW` builds a genuine finite reader (focus `aW`, reads `{aW}`, finite by `Set.finite_singleton`, grounded by `ReflTransGen.refl`) for which the tower relatum `cW` is real, the `att.reads` membership (the reader) LOAD-BEARING, via the shared `ws2_cW_distinguishes_aW`. `FiniteAttention` gains its first genuine theorem. The charter names nonexistent carrier theorems (`ws1_attention_makes_real`, `ws1_attention_distinction_free`, `RealFor` as "transcribed unchanged"); the charter is immutable, so these are NOT struck from it but recorded as discrepancies below, with the built reading here: `FiniteAttention`/`RealFor`/`AttentionDistinguishes` are Series 12's OWN WS2 definitions (not Series 07 carrier), the reader payoff is `ws2_attention_makes_real`, and the certificate is `ws2_distinction_free`. Owner: WS2, charter-status. Target built and named: `ws2_attention_makes_real`.
3. **PR1-R1 (REAL): "the compass is exogenous (`¬ Recoverable`)" named no theorem. Closed FIXED (the bridge built).** `ws3.lean` adds `orientLift` (the `orient` analog of `rankLift`) and the general `orientLift_not_recoverable`, and `ws3_compass_exogenous_import : ∃ c, ¬ Recoverable (orientLift (destW hinf) c.orient)`: the witness orientation lifted to labels is a genuine import (the exogenous orientation label separates the plain-bisimilar pair `aW`, `bW`). The `¬ Recoverable` gloss now has a proof-term correlate; `ws3_compass_exogenous` (orientation-freedom) is kept as the existential it always was. Owner: WS3. Target built and named: `ws3_compass_exogenous_import`.
4. **PR1-R2 (REAL): the anti-conflation theorem assumed the non-identity it advertised. Closed FIXED (hypothesis discharged).** `ws1_coincidence_not_identity_witness` discharges the `residue insp₁ ≠ residue insp₂` hypothesis of `ws1_coincidence_not_identity` with the concrete constant-`True`/constant-`False` inspection pair (pointwise-opposite residues on any inhabited `Hold`), so the anti-conflation is non-vacuous: distinct inhabitants of the shape genuinely exist. The coincidence's honest parametric reading (one shape-wrapper `Opening`, two type-heterogeneous recoverability notions, forced-for-all vs exists-satisfying) is stated in the swept prose. Owner: WS1, docs. Target built and named: `ws1_coincidence_not_identity_witness`.
5. **PR1-R3 (REAL): "no attention holds the whole" is a rename of the diagonal, not a theorem about the built attention type. Closed FIXED (demoted, recorded).** `TotalAttention`/`ws3_no_total_attention`/`ws2_attention_subtractive` docstrings now DISCLOSE that these are the transcribed diagonal (`SelfTotal`/`ws1_no_self_total_hold`) under the `TotalAttention` rename, NOT predicates relating `FiniteAttention`'s bounded reader to self-total holds; the subtractive/attention reading is carried in prose. `FiniteAttention`'s genuine theorem is `ws2_attention_makes_real` (PR1-S2), which softens the finding. Owner: WS2/WS3. Demotion recorded (no target theorem claimed to exist).
6. **PR1-R4 (REAL): the ledger carried claims the build falsifies. Closed FIXED (swept).** This ledger (mutable, not the charter) is swept after S1/S2/R1/R2/R3: the carrier row names only built theorems (finite-attention machinery reclassified as Series 12's own, the nonexistent `ws1_attention_makes_real`/`ws1_attention_distinction_free` removed from the ledger's own claims); the WS4 rows report the `Faithful`-class open fork (not "resolved in the expected direction" as a typing fact); the WS3 row carries `ws3_compass_exogenous_import`; the program-level snapshot, the result tracker, and the obligation register are advanced. The charter's own instances of the same claims are immutable and are recorded as discrepancies below. Owner: charter-status.md.

*Cosmetics (PR1-C1..C5): PR1-C3/C4 (nominal docstring overclaims) touched opportunistically in the sweep; PR1-C1 (`ws4_underdetermined_up` at chain length 1), PR1-C2 (`ws2_many_general` over-hypothesis) left as-is (harmless, named targets built as named); PR1-C5 is a note on the review's non-blindness, no code action.*

## program-review-2 closure log (adversarial pass 3; the recurrence check on the program-review-1 closures)

*`spec/program-review-2.md` ran the protocol §0.2a recurrence check on the six program-review-1 closures FIRST. It verified five as genuine (PR1-S2, R1, R2, R3, R4) and re-graded PR1-S1 as RECURRING (PR2-S1): the `Faithful` closure moved the tautology from the free `raise` field to the free `orient` field, so the fork at any genuine (distinct-relata) edge was still settled a priori and the new falsifiability lived at a reflexive NON-edge holding by `rfl`. It raised PR2-S1 (SERIOUS, recurring), PR2-R1 and PR2-R2 (REAL), and five cosmetics. All are closed on this branch (targets built and named), verified by a full sorry-free axiom-clean rebuild (standard three only) and the closure gate. No `charter.md` edit (see PR2-R1).*

1. **PR2-S1 (SERIOUS, RECURRING PR1-S1 count 2): over the faithful class the trichotomy is decided by pair-reflexivity; the falsifiability was a reflexivity triviality at a non-edge. Closed FIXED (the build branch, the two-zone fork at the genuine edge).** Built exactly the review's named targets over the R1 tool (`orientLift_not_recoverable`), a handful of lines each:
   - `ws4_dissent_is_import : ∀ c, Faithful c → ¬ Converges c aW bW → ¬ Recoverable (orientLift (destW hinf) c.orient)` (`faithful_converges_iff` turns dissent into `c.orient aW ≠ c.orient bW`; `aW`, `bW` are SHNE and plain-bisimilar; `orientLift_not_recoverable` closes it). EVERY faithful compass that fails convergence at the genuine edge does so by a genuine import: the not-seeing is literally Series 07's work, as a proof term, universally quantified over compasses.
   - `BisimInvariant c := ∀ x y, (∃ R, IsBisim (destW hinf) R ∧ R x y) → c.orient x = c.orient y`, with `ws4_decided_within_sight : ∀ c, Faithful c → BisimInvariant (destW hinf) c → Converges c aW bW` (via `ws1_atomless_bisim`). Within what the structure can SEE, convergence at the edge is DECIDED, in the holding direction (charter §2-WS4 alternative (a) at the sub-class level).
   - `ws4_two_zone` packages the two-zone fork at the SAME edge `(aW, bW)`: underdetermined over the full faithful class, decided over the in-sight sub-class, every dissent an import. The verdict fork (`ConvergenceFork`) is generalized to a class parameter `InClass`; `s12_fork` (full faithful, `underdet`, `shapeDrawn`) and `s12_fork_insight` (in-sight sub-class, `forcedHolds`, `convergenceDecided`) are BOTH at the genuine edge, so `ws5_verdict_reaches_both` and `ws7_fork_can_close` now witness falsifiability WHERE THE VERDICT IS COMPUTED. `ws4_fork_open` (the reflexive locus) is kept only as a remark, no longer load-bearing. Owner: WS4, WS5, WS7. Targets built and named: `ws4_dissent_is_import`, `BisimInvariant`, `ws4_decided_within_sight`, `ws4_two_zone`, generalized `ConvergenceFork`, `s12_fork_insight`.
2. **PR2-R1 (REAL): the charter was edited to record progress. Closed FIXED (already reverted).** The program-review-1 closure had edited `charter.md` (naming `Faithful`/`ws4_fork_open` in §2-WS4). That edit was reverted in full on this branch (commit reverting `charter.md` to its original bytes) before program-review-2; the charter now stands verbatim and every mismatch is recorded in the "Charter discrepancies" subsection below (progress lives here, not in the bar). No charter edit remains.
3. **PR2-R2 (REAL, conditional on PR2-S1): the ledger asserted uncertainty/openness that did not exist. Closed FIXED (swept).** After PR2-S1: the snapshot rows ("the wall," "the terminal open," "headline target"), the WS4 workstream row, the result tracker, and the obligation register are rewritten around the two-zone fork at the genuine edge (decided within sight, dissent-as-import), not the reflexive-locus `ws4_fork_open`; the PR1-S1 closure entry is marked SUPERSEDED. The opening blurb and Charter-discrepancies item 2 are advanced; a new discrepancy item 3 records the charter's "diagonal's work" against the built Series-07-import reading. Owner: charter-status.md.

*Cosmetics: PR2-C1 (`ws6_provable_core` attributing `¬ Recoverable` to the orientation-freedom conjunct) FIXED, the import conjunct `ws3_compass_exogenous_import` added and the docstring re-glossed. PR2-C2..C4 (carryovers of PR1-C4/C1/C2) left as-is (harmless, unowed). PR2-C5 (the tower-generalized reader) left as-is (nothing owed; the sanctioned singleton reader `ws2_attention_makes_real` stands).*

## program-review-3 closure log (adversarial pass 4; the recurrence check on the program-review-2 closures)

*`spec/program-review-3.md` ran the protocol §0.2a recurrence check on the program-review-2 closures FIRST and verified them genuine: `ws4_dissent_is_import`, `ws4_decided_within_sight`, and the class-parameterized fork were built as named and traced sound, each structure-specific (consuming the `ws_SHNE` certificates and the collapse engine at the pair), so the defect's consequence no longer decides the terminal verdict. **The design-review-1 Finding 2 lineage (the strip-test failure that recurred through two closures) closes at count THREE.** This is the first program-review pass with ZERO SERIOUS findings and no recurrence. Four REAL findings, all housekeeping at the edge of the fix (none touches a payoff, none moves the verdict), are closed on this branch; verified by a full sorry-free axiom-clean rebuild (42 headlines, standard three only) and the closure gate.*

1. **PR3-R1 (REAL): the in-sight sub-class was uncertified as inhabited, and on this carrier every member is orientation-constant (the `NonDegenerate.orient_nonconst` degeneracy), undisclosed. Closed FIXED (two theorems + disclosure, and it strengthens the reading).** `ws4_insight_inhabited : ∃ c, Faithful c ∧ BisimInvariant (destW hinf) c` (the constant-orientation compass; an existential about the class, never discharging a `∀`-obligation), so `s12_fork_insight` / `convergenceDecided` rest on a genuinely inhabited class, not a vacuity-capable `forcedHolds`. `ws4_sight_is_uniform : ∀ c, BisimInvariant (destW hinf) c → ∀ x y, c.orient x = c.orient y` (every state `SHNE`, so every pair plain-bisimilar, so a sight-bound orientation is globally constant): what the structure can SEE is the One at the orientation level, Series 07 replayed, so the decided zone is decided BECAUSE sight collapses to the uniform orientation. The constancy (and that decidedness therefore holds at every pair, the edge being where the underdet arm lives) is now disclosed in the `ws4_decided_within_sight` / `s12_fork_insight` docstrings and both summaries. Owner: WS4/WS5. Targets built and named: `ws4_insight_inhabited`, `ws4_sight_is_uniform`.
2. **PR3-R2 (REAL): sweep residuals narrating superseded mechanisms as load-bearing. Closed FIXED (prose only).** The `ws4.lean` module header (which still narrated the reflexive-locus `ws4_fork_open` route as the falsifiability carrier, contradicting that lemma's own remark docstring) is rewritten around the two-zone fork at the genuine edge. `spec/ws4-design.md`'s winner section (which still presented the pre-`Faithful` `ws4_wall_is_structural`, shared *orientation* / `cHold`-`cFail`, and attributed the not-seeing to `ws2_residue_free`) gains a BUILD REALIZATION note recording the `Faithful`-class inversion (built `cHoldF`/`cFailF` share the identity *raising*, differ in the *orientation*) and striking the `ws2_residue_free` attribution (no WS4 proof uses it; the not-seeing is Series 07's import work, `ws4_dissent_is_import`). Owner: `ws4.lean` header, `spec/ws4-design.md`.
3. **PR3-R3 (REAL): the committed axiom log was two closure commits stale. Closed FIXED (regenerated).** `spec/axiom-check-log.md` recorded 32 headlines and predated the PR1/PR2 closures while the ledger asserted a verified clean rebuild; it is regenerated from `lake build Series12.AxiomCheck` to all 42 current headlines (all on the standard three, `ws_witness_rank_noninjective` on `propext`/`Quot.sound`), with a capture-provenance header naming this closure. The trust chain the reviews rely on (they cannot recompile) is current again. Owner: `spec/axiom-check-log.md`.
4. **PR3-R4 (REAL): both summaries under-reported the adversarial history. Closed FIXED (one sentence each, mirroring the README).** `summary.md` and `summary-technical.md` said only that a blind/series review returned zero SERIOUS; both now record the three program reviews (two SERIOUS findings on the convergence leg, since closed; the third clean), and the technical summary notes series-review-1's disclosed non-blindness. Owner: `summary.md`, `summary-technical.md`.

*Cosmetics (PR3-C1..C6): none owed. PR3-C5 (the converse bridge `BisimInvariant c → Recoverable (orientLift …)`, which would make the import boundary a biconditional) and PR3-C6 (a `ConstituentOf` proof carried in the fork type) noted for the record, not taken; the current one-directional prose is accurate. PR3-C1..C4 are carryovers, left as-is.*

## Charter discrepancies (recorded; `charter.md` is immutable once written)

*`charter.md` is fixed once written (protocol §1: "Edited only to fix an error in the design itself, never to record progress"; on this branch we treat it as strictly immutable and record every mismatch here instead). Program-review-1 and program-review-2 exposed places where the charter text does not match the built code. The charter stands verbatim; the authoritative built reading is this ledger. Each discrepancy below gives the charter's wording, the mismatch, and the built theorem that governs.*

1. **Nonexistent theorem names (charter §1 opening, §1 one-paragraph, §2-WS2, §3 "the carrier," §6).** The charter names `ws1_attention_makes_real` and `ws1_attention_distinction_free` and lists `RealFor` under the carrier "transcribed unchanged." **These do not exist in the Series 12 build.** They are Series 11 theorem names (that series' honestly re-graded Bookkeeping re-hit), never transcribed into Series 12. Built reading: the many is real reader-wise via `ws2_attention_makes_real` (WS2's own theorem), the distinction's certificate is `ws2_distinction_free`, and `FiniteAttention`/`RealFor`/`AttentionDistinguishes` are Series 12's OWN WS2 definitions over the carrier, not transcribed Series 07/Series 11 carrier. (`ws3_no_total_attention` is the transcribed diagonal `ws1_no_self_total_hold` under the `TotalAttention` rename, PR1-R3.)
2. **The convergence underdetermination under-specifies the class (charter §1, §2-WS4, §5.2, §6).** The charter says "exhibit two inhabitants of the compass type on the SAME structure, one under which the convergence relation holds and one under which it fails, both non-degenerate," with no constraint on the compass class. Taken over ALL compasses this is a typing tautology (the free `raise` field flips convergence on every structure; the fork is never open; PR1-S1). Built reading: the independence is over the constrained `Faithful` class (`raise = id`), and at the genuine edge `(aW, bW)` it is a TWO-ZONE fork whose boundary is Series 07's import boundary (`ws4_two_zone`): convergence is forced within the structure's sight (`ws4_decided_within_sight`) and every dissent is a genuine import (`ws4_dissent_is_import`, `¬ Recoverable`), so `convergenceDecided` is reachable and SHAPE-DRAWN falsifiable at the edge (`ws5_verdict_reaches_both`). The charter's "genuinely uncertain obligation is WS4" (§5.2) is honored by this construction; the charter's unconstrained phrasing is the discrepancy.
3. **"The not-seeing is structural, the diagonal's work" (charter §1, §2-WS4).** The charter attributes the wall to the DIAGONAL. No WS4 proof uses `ws1_no_self_total_hold`, `residue`, or any diagonal fact. Built reading: the not-seeing is SERIES 07's import work, as a proof term, `ws4_dissent_is_import` (every faithful dissent at the edge is `¬ Recoverable`), resting on the plain-bisimilarity of `aW`, `bW` (the collapse engine). The charter's "diagonal's work" phrasing is interpretive prose; the mechanized wall stands on Series 07's import boundary.
4. **The `¬ Recoverable` exogeneity gloss (charter §1, §2-WS3, §6).** The charter calls the compass "exogenous because Series 07 forces it, `¬ Recoverable`." No charter-named theorem stated `¬ Recoverable` of a compass-induced lift; the built proof term is `ws3_compass_exogenous_import` (PR1-R1), alongside the orientation-freedom existential `ws3_compass_exogenous`.

## Closed log

*Phase C complete (Build-all): every Series 12 design is realized in `formal/Series12/wsNN.lean`, aggregated by `Series12.lean`, with `AxiomCheck.lean`; the full build is sorry-free and axiom-clean (standard three only), the closure gate passes, and the terminal verdict is SHAPE-DRAWN. The witness carrier is the four-state `WCar` (`ULift (Fin 4)`: `aW`, `aW'` rank 0; `bW` rank 1; `cW` rank 2), with `destW`/`reifyW`/`rankW`/`destWL` and the `dir := rank` labelling; `ws2_many_witness` separates `cW` (a reified relatum carrying the reified constituent `bW`) from the base `aW`, an instance of the general `ws2_many_general`, rank non-injective (`ws_witness_rank_noninjective`). The compass is `Compass dest reify Or` (WS3), held parametric; `Converges`/`ConvergesUp` (edge-restricted) and the `cHold`/`cFail` model pair (WS4) discharge `ws4_underdetermined`; the fork `ConvergenceFork`/`verdictOfFork` (WS5) makes the verdict branch. Two disclosed build realizations, faithful, not retargets: (i) the total `IsReify` is unsatisfiable on the concrete atomless carrier (Cantor), so the witness supplies the pointwise reification facts (`ws_reify_pointwise_sW`/`_sW₂`) the lemmas consume, the deviation the charter pre-registered; (ii) `Converges` is defined in `ws4.lean` (its design home) and the WS5/WS7 verdict type + fork live in `ws5.lean` (the earliest consumer, importing WS4), so `WS5 → WS6 → WS7` dependency order holds; no signature is changed. Next: Phase D (blind series-review).*

*Phase A complete: charter, charter-status, protocol written. The terminal question is the shape of the undefinable: the coincidence (WS1) of what Series 07 requires and what the diagonal generates, in the shape `¬ Recoverable`; knowing plural over the inhabited opening (WS2); the compass typed, layered, and never evaluated (WS3); convergence defined and proved underdetermined (WS4); the verdict computed and the neutrality generalized to all names (WS5); and no name ever a proof term.*

*Phase B complete: `spec/README.md` (the design index) plus the seven `spec/wsNN-design.md` and `spec/ws4-witness-design.md` written. The shared objects are LOCKED once in `spec/README.md` §2 and cited everywhere (the two Series-12 design duties, protocol §2): the opening `Opening realizable c := ¬ realizable c` (§2.7); `Many destL` (§2.8); the compass type `Compass dest reify Or` with fields `orient : X → Or`, `raise : X → X → Or → Or` (§2.9); the convergence relation `Converges c x W := c.raise x W (c.orient x) = c.orient W` and its layered closure `ConvergesUp` (§2.10); the generalized neutrality `ws5_name_neutral` (§2.11); and `Series12Verdict` (§3). The design's central moves, each foreclosing a false escape: WS1 states the coincidence as forced-for-all (`ws2_residue_free`, ∀ insp) vs exists-satisfying (`ws4_labelLoop_not_recoverable`, a witnessed import), with an anti-identity guard (distinct residues inhabit the shape), never object-identity. WS2/the-witness draws the plurality on a concrete `dir := rank` carrier where a reified relatum `bW = reifyW sW` and a base relatum `aW` are plain-bisimilar (`ws1_atomless_bisim`) yet rank-separated at the labelled level, `reify`/`reifyStep`/`towerN`/rank load-bearing, the genuine tower-distinction Series 10 (`ws2_reify_bisim_embeds`) and Series 11 (`ws1_attention_makes_real`) could not draw (both landed on `labelLoop`, a point-tag, re-graded Bookkeeping re-hit). WS3 holds the compass parametric (the only concrete compasses live inside existentials: `ws3_compass_exogenous`, the model pair). WS4 earns the underdetermination with a genuine non-degenerate model pair (`cHold`/`cFail` on the same carrier, sharing a non-constant orientation, differing only in the free raising, one cohering and one not), never deciding convergence by definition. WS5 computes the verdict from an audit and proves name-neutrality parametrically. WS7 owns the verdict and the five promoted checks. Next action: Phase C (build `formal/Series12/wsNN.lean`, `Series12.lean`, `AxiomCheck.lean`) in the dependency order WS1 → WS2 → WS3 → WS4 → WS5 → WS6 → WS7. The one disclosed structural deviation (total `IsReify` is unsatisfiable on a concrete atomless carrier by Cantor; the pointwise reification facts the lemmas actually consume, `dest (reify sW) = sW`, `(sW).1 ≠ ∅`, `∀ x ∈ (sW).1, SHNE dest x`, are supplied) is owned by `spec/ws4-witness-design.md` and routed here.*

---

*No em dashes in final academic copy. The premise presupposed and named is Series 07: the differentiator cannot be recovered from the relating. What is proved, not assumed: the shape-coincidence with honest quantifiers; inhabitability with a non-degenerate witness; the underdetermination of convergence by a genuine model pair, or the honestly-reported alternative if the structure decides. What is never proved, by design: the compass's content, convergence's direction, the act itself. The names, consciousness, choice, God, the compass, are the interpretive thesis, in prose, never terms. This is the program's terminal series: its completion is the exactly-drawn edge of the undefinable, and the drawing, not the filling, is the terminus.*
