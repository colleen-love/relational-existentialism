# Relational Existentialism, Series 12: Charter Status

**The mutable companion to `charter.md`. All progress, revisions, per-workstream status, discharges, and open items live here; the charter stays clean.**

*Series 12 is the program's genuine terminal series. Its question: the SHAPE of the undefinable. Series 07 proved the differentiator cannot be recovered from the relating (any genuine atomless distinction is an import, `ws2_import_theorem`, `ws3_atomless_distinct_is_import`, `ws1_atomless_bisim`); the diagonal proved the structure generates what it cannot recover (self-inspection cannot totalize, `ws1_no_self_total_hold`, leaving a residue free for every inspection, `ws2_residue_free`, and independent of relational identity, `ws1_diagonal_not_bisim`). These coincide in one shape, non-recoverability. Over the opening they draw run two sides of one layered structure (the reification tower `towerN`/`reifyStep`/`prec`): KNOWING, finite attention, plural and subtractive and fully formal, with any inhabitant of the opening making the many real (`RealFor`, `ws1_attention_makes_real`, distinction non-recoverable via `ws1_attention_distinction_free`) and without any import the One; FEELING, the compass, a per-relatum tower-layered exogenous orientation the mathematics TYPES but never EVALUATES; and CONVERGENCE, the coherence-up-the-layers relation between a part's compass and the whole's, DEFINED and proved UNDERDETERMINED by the structure. The verdict is a function of these. The names for what inhabits the opening, consciousness, choice, God, the compass, are the program's central interpretive thesis, in prose, never proof terms. Phase A (charter, charter-status, protocol) is the current state; Phases B-F have not been run.*

---

## How to read this file

- **Status vocabulary** (charter §5): **Shape-drawn** (the expected terminus: the coincidence proved with honest quantifiers, the opening inhabitable non-degenerately, knowing plural, the compass typed and never evaluated, convergence defined and underdetermined by a non-vacuous model pair) · **Convergence-decided** (the structure turns out to force the convergence relation or its negation for all inhabitants, reported honestly in whichever direction it falls, a stronger finding, not a failure) · **Partial** (any side lands only per-instance or degenerate) · **Refuted** · **Circular** (WS7-only) · **Not started**.
- The program **does not put the undefinable into the math.** It proves the math produces the undefinable's exact outline, at the diagonal, and ends there. Shape-drawn is not "we could not finish"; it is the theorem that the outline is exact and cannot be filled from inside.
- **The compass is typed, never evaluated.** Every compass-theorem is quantified over all inhabitants; no theorem selects, constructs, or applies a distinguished one. A canonical inhabitant anywhere in the core is the central sin (the undefinable defined), charter §4.a.
- **Convergence is defined, then proved underdetermined, not decided by definition.** Building the loving conclusion in (holds trivially) or building the silence in (fails trivially) are both the sin, charter §4.b. The underdetermination is earned by a genuine non-vacuous model pair; if the structure genuinely decides the relation, that is reported in its direction, never stipulated.
- **The coincidence is shape-identity, not object-identity.** The residue and an arbitrary import both fail `Recoverable`; that does not make them the same object (the shared-negation fallacy, charter §4.c). The WS1 theorem keeps forced-for-all (the residue) distinct from exists-satisfying (inhabitants).
- **Names are not terms.** Consciousness, choice, God, the compass-as-content live in prose and docstrings only (§8); a name discharging an obligation breaches the wall from the prose side (charter §4.e). A promoted first-class check.

## Program-level snapshot

| Item | Status |
|---|---|
| **Headline target** | The shape of the undefinable: the coincidence (WS1) of what Series 07 requires and what the diagonal generates, in the shape `¬ Recoverable`; knowing plural over the inhabited opening (WS2); the compass typed, layered, never evaluated (WS3); convergence defined and underdetermined (WS4). **SHAPE-DRAWN (built, Phase C).** The coincidence is shape-honest (`ws1_shape_coincidence`, forced-for-all vs exists-satisfying; `ws1_coincidence_not_identity`); the opening is inhabitable non-degenerately (`ws2_many_witness` on the four-state rank witness, `ws2_many_general` load-bearing, rank non-injective); the compass is typed and parametric, evaluated nowhere (`ws3_compass_exogenous`, concrete compasses only inside existentials); convergence is defined and underdetermined by a genuine non-degenerate model pair (`ws4_underdetermined`, `ws4_wall_is_structural`); the verdict is computed SHAPE-DRAWN (`ws7_verdict_is_shapeDrawn`). Expected shape, pre-registered (not a target): WS1's two halves and the coincidence are near-certain (transcribed Series 07 and diagonal, care with quantifiers); WS2's inhabitability witness is paper-checked (the concrete carrier, the rank direction, the generalization with reification load-bearing); WS3 is definitional plus audit discipline; the genuinely uncertain obligation is WS4, whether convergence admits a non-vacuous definition and whether the underdetermination model pair exists. |
| **The two sides** | KNOWING (attention, subtractive, plural, formal, in the math) and FEELING (the compass, typed, layered, exogenous, evaluated nowhere, content outside), dual over one tower: attention is the knowable subtraction where the bound falls, the compass is what would orient at the residue the bound leaves. **Not started.** |
| **The wall** | Convergence ("the most loving thing for me is the most loving thing for the universe") defined as coherence-up-the-layers, then proved UNDERDETERMINED by a non-vacuous model pair on one structure. **Not started; NOT decided in advance.** The formal content of the wall: the math draws exactly where the loving claim would be true or false and proves it cannot see which, and that the not-seeing is structural. |
| **The terminal open** | Shape-drawn vs convergence-decided. **Not started; the direction of convergence is NOT assumed.** Settled by the verdict-as-a-function (charter §5.3): shape-drawn (coincidence honest, opening inhabitable, compass typed-not-evaluated, convergence underdetermined by a real model pair) / convergence-decided (WS4 outcome a, reported in its direction) / partial. Never settled by evaluating the compass, deciding convergence by definitional choice, collapsing the coincidence to object-identity, or using a name as a term. |
| **Verdict (WS7)** | **Not started.** The typed verdict (to be designed) will be a function of a mechanized audit whose fields are the WS1-WS4 theorems, so it cannot be hand-set. WS7 owns: the no-evaluation check (no distinguished compass anywhere), the genuine-model-pair check (both inhabitants non-degenerate, the relation really holding in one and failing in the other), the shape-honesty check (forced-for-all vs exists-satisfying kept distinct), and the names-not-terms check. |
| **The carrier** | Transcribed unchanged (self-contained; the one presupposed prior result is Series 07): the atomless collapse engine and import theorem (`ws1_atomless_bisim`, `ws2_import_theorem`, `ws3_atomless_distinct_is_import`, `BehaviorallyIdentified`, `Recoverable`, `plainOf`), the diagonal and free residue (`ws1_no_self_total_hold`, `ws1_diagonal_not_bisim`, `residue`, `diag`, `ws2_residue_free`, `Hold`, `afford`, `HoldPred`), the reification tower (`IsReify`, `ws1_reify_injective`, `reifyStep`, `towerN`, `prec`, `ws3_order_endogenous`, `ws3_reify_preserves_SHNE`), reachability and self-having loci (`SReaches`, `SHNE`), finite attention (`FiniteAttention`, `RealFor`, `ws1_attention_makes_real`, `ws1_attention_distinction_free`, `ws3_no_total_attention`), the labelled lift. Series 12 adds NO new structural machinery: two typed objects (the opening, the compass type) and one relation (convergence), plus the plurality predicate `Many` defined over the carrier. |
| **Relation to the program** | The genuine close of the arc. The arc built the collapse and import necessity (Series 07), self-reference forcing an unclosable residue, reification proliferating it into a layered tower, finite attention bounding every reading; this series proves the required and the generated are one shape, knowing is plural over it, the compass is typed and unevaluated, and convergence is undecidable. The program closes not on a filled slot but on an exactly-drawn edge, and the drawing is the completion. |

## Phase status

| Phase | Name | Status |
|---|---|---|
| A | Charter | **Complete.** `charter.md`, `charter-status.md`, `protocol.md` written. |
| B | Design-all (seven `spec/wsNN-design.md` + `spec/README.md`, plus the WS4-witness sub-artifact) | **Complete, design-review-1 addressed.** `spec/README.md` (design index, locking the shared objects), the seven `spec/wsNN-design.md`, and `spec/ws4-witness-design.md` (the inhabitability witness) are written, all speaking the shape-of-the-undefinable framing: the opening (`Opening`, `¬ Recoverable`), `Many`, the compass TYPE (`Compass`, per-relatum/layered/exogenous), the convergence relation (`Converges`, coherence-up-the-layers), the model pair, and the verdict (`Series12Verdict`) are defined ONCE in `spec/README.md` §2 and cited by every workstream. The four `spec/design-review-1.md` findings are all closed **Fixed** (see the design-review-1 closure log below). Cross-doc consistency verified (compass type, convergence relation, `Many`, `Opening`, `ConstituentOf`, `ConvergenceFork`, verdict identical across all docs). Em-dash discipline applied to every edited design file. |
| C | Build-all (`formal/Series12/wsNN.lean`, `Series12.lean`, `AxiomCheck.lean`) | **Complete.** All seven `formal/Series12/wsNN.lean`, the aggregator `Series12.lean`, and `AxiomCheck.lean` build sorry-free and axiom-clean (Lean 4 v4.15.0 / Mathlib v4.15.0); every headline rests only on the standard three (`propext` / `Classical.choice` / `Quot.sound`; `ws_witness_rank_noninjective` on `propext` / `Quot.sound`), captured in `spec/axiom-check-log.md`. The closure gate confirms Series 12 imports resolve only to `Series12.*` + Mathlib. The terminal verdict is **SHAPE-DRAWN** (`ws7_verdict_is_shapeDrawn`), computed from the audit's `underdet` fork, never hand-set. |
| D | Blind series-review → `spec/series-review-1.md` | **Not started.** |
| E | Address | **Not started.** |
| (loop D→E) | Second review pass → `spec/series-review-2.md` | **Not started.** |
| F | Close (summaries + root README + program close) | **Not started.** |

The canonical run is **B → C → D → E → D → E** (protocol §2), with more D→E loops added only if a review pass still returns SERIOUS findings. The anti-loop discipline (protocol §0.2a) is in force.

## Workstream status

### WS1, The opening and the coincidence · *blocking*
**Status: Not started.** Two parts. (a) THE TWO HALVES: transcribe and restate Series 07 (no structural function differentiates SHNE loci; any genuine distinction is an import) and the diagonal (`ws1_no_self_total_hold`, `ws2_residue_free`, `ws1_diagonal_not_bisim`). (b) THE COINCIDENCE: both halves characterize the same shape `¬ Recoverable`, from opposite directions (required vs generated), proved as shape-identity with honest quantifiers, the residue the forced instance, imports the class of inhabitants. Pre-registered sin: the shared-negation fallacy (both fail `Recoverable`, therefore identical). Blocking: WS2-WS4 build over the opening.

### WS2, Knowing: finite attention, and the opening inhabited · *plurality*
**Status: Not started (design + witness written, Phase B).** The formalizable side. Finite attention subtractive, plural, bounded, grounded, no attention total (`ws3_no_total_attention`). Prove the opening INHABITABLE and that inhabitation makes the many real: the two-sided theorem on a concrete witness (a reified tower relatum and a base relatum genuinely plain-bisimilar yet separated at the labelled level, reification load-bearing, generalized across the tower; without any import, the One via `ws2_import_theorem_static`), the distinction non-recoverable (`ws2_distinction_free`) as its certificate. The witness is designed in `spec/ws4-witness-design.md` (`dir := rank`: the label a relatum broadcasts is its `towerN` position; `ws2_many_witness` discharges `Many destWL`; `ws2_reification_loadbearing` certifies `bW = reifyW sW ∈ reifyStep {aW}`, not a point-tag; `ws2_many_general` generalizes across reifiable patterns). Honest failure mode: degenerate inhabitation (a bare point-tag carrying no tower content), reported as such.

### WS3, Feeling: the compass, typed and never evaluated
**Status: Not started.** Define the compass as a TYPE: per-relatum, tower-layered (a relatum's compass conditioned by the reified relata it stands in relation to, which relate up `prec`), exogenous (`¬ Recoverable`, the Series 07 necessity). Every theorem quantified over all inhabitants; none selects, constructs, or evaluates one. State the duality with WS2 (attention the knowable subtraction; the compass the typeable orientation whose values the structure cannot supply). The content stays outside the math.

### WS4, Convergence: defined, then proved underdetermined · *the wall, the uncertain obligation*
**Status: Not started. THE GENUINELY UNCERTAIN OBLIGATION.** Define the convergence relation (a part's compass coheres up its layers with the compass of the whole it constitutes). Prove the structure does NOT decide it: exhibit two non-degenerate inhabitants on the same structure, one satisfying the relation and one failing it, so convergence is underdetermined (an independence theorem, the formal content of the wall). Honest alternatives, both first-class: (a) the structure DECIDES it (every inhabitant satisfies, or every inhabitant fails), reported in its direction, a stronger finding; (b) the relation cannot be non-vacuously defined, reported as the precise obstruction.

### WS5, The verdict as a function, and the generalized neutrality
**Status: Not started. COMPUTED FROM WS1-WS4, the direction of convergence NOT assumed.** Compute the verdict (shape-drawn / convergence-decided / partial) as a function of WS1-WS4. AND prove the generalized neutrality: adjoining ANY name for the inhabitant (is-a-choice, is-an-experience, is-consciousness, is-God, is-the-compass) changes no downstream theorem; the framework is provably silent about what fills the shape it draws, and the silence is the theorem. Names in prose (§8); the core quantifies.

### WS6, The program's close · *the honest boundary*
**Status: Not started.** State what the arc established (the collapse and import necessity, Series 07; self-reference forcing an unclosable residue; reification proliferating it into a layered tower; finite attention bounding every reading; and this series, the coincidence, the plurality of knowing, the typed-unevaluated compass, the undecidability of convergence) and why the permanent opens are OPEN AS A THEOREM (the compass's content, convergence's direction, the act itself), in `summary.md` at Phase F. The program closes on the exactly-drawn edge.

### WS7, The anti-circularity audit · *owns the verdict*
**Status: Not started.** Series 12-specific risks, three promoted to first-class: (a) **NO EVALUATION OF THE COMPASS** (grep and unfold: no theorem selects, constructs, or applies a distinguished compass; a canonical inhabitant in the core is the central sin); (b) **the underdetermination is a genuine model pair** (both inhabitants non-degenerate, on the same structure, the relation genuinely holding in one and failing in the other, not a vacuity); (c) **the coincidence is shape-honest** (forced-for-all vs exists-satisfying kept distinct, never object-identity from shared non-recoverability). Plus: no proof term named "consciousness," "God," "choice," or "compass" doing discharge work. Owns the final verdict and the program-level synthesis's honesty.

## The result tracker

| Result | WS | Status | Non-circular? |
|---|---|---|---|
| The two halves (Series 07 + the diagonal) | WS1a | **Not started.** Target: import necessity and the free residue, transcribed. | Pending: genuine `ws1_atomless_bisim`/`ws2_residue_free`/`ws1_diagonal_not_bisim`. |
| The shape-coincidence | WS1b | **Not started.** Target: required and generated coincide in `¬ Recoverable`. | Pending: forced-for-all (residue) vs exists-satisfying (inhabitants) kept distinct; not object-identity. |
| Knowing is plural over the inhabited opening | WS2 | **Not started (witness drafted).** Target: two-sided, non-degenerate witness, reification load-bearing. | Pending: the distinction non-recoverable; without-import collapse genuine Series 07; not a point-tag. |
| The compass, typed and layered | WS3 | **Not started.** Target: per-relatum, tower-layered, exogenous type; parametric everywhere. | Pending: no theorem evaluates or selects a compass; content outside. |
| Convergence, defined and underdetermined | WS4 | **Not started; NOT decided in advance.** Target: a non-vacuous model pair on one structure. | Pending: both inhabitants non-degenerate; not decided by definitional choice; alternatives first-class. |
| The verdict, and generalized neutrality | WS5/WS7 | **Not started.** Target: shape-drawn / convergence-decided / partial, computed; any name changes no theorem. | Pending: never by fiat, never by evaluating the compass, never by a name as a term. |

## Open obligations register

1. **The two halves (WS1a)** and **the shape-coincidence (WS1b)**. The opening, and that the required and the generated are one shape. **Not started.**
2. **Knowing plural over the inhabited opening (WS2)**. Two-sided, non-degenerate, reification load-bearing. **Not started (witness drafted in `spec/ws4-witness-design.md`).**
3. **The compass, typed and never evaluated (WS3)**. Parametric everywhere; content outside. **Not started.**
4. **Convergence, defined and underdetermined (WS4)**. A non-vacuous model pair, or the honestly-reported decided/undefinable alternative. **THE UNCERTAIN OBLIGATION. Not started.**
5. **The verdict, computed; the neutrality, generalized to all names (WS5)**. **Not started.**
6. **The undefinable is never defined (the central discipline)**, WS7. No canonical compass; convergence not decided by definition; the coincidence shape-honest; names not terms. **Standing constraint.**
7. **The permanent opens are open as a theorem**, WS6. The compass's content, convergence's direction, the act, left open because the diagonal makes them so, not for want of effort. **Standing.**

**Recurring lesson carried across the program:** the sharpest result is a proved impossibility, and an honestly-reported Partial beats a smuggled success. A theorem can hold and still fail to be the fact you claimed (coincidence of the shallow kind). A payoff can be genuine yet thin. And the specific lesson this series turns on: a distinction that is non-recoverable is NOT thereby a failure. Series 07 proves a genuine atomless distinction MUST be non-recoverable, so in the shape-question this series asks, non-recoverability is the certificate that a distinction is real, not the sign it is cheating. The whole series rests on getting three signs right: the coincidence is of shape and the quantifiers stay honest (not object-identity from shared negation); the opening's inhabitation is genuine (a real tower-distinction, not a point-tag); and, above all, the undefinable is drawn and left open (the compass typed not evaluated, convergence underdetermined not decided). The program does not need to fill the shape; it needs to draw the edge exactly, and prove the edge cannot be crossed from inside.

## design-review-1 closure log (protocol §0.2a: each SERIOUS/REAL finding closes Fixed or Relabeled)

*`spec/design-review-1.md` raised four Phase B findings against the initial design docs. All four are closed **Fixed** (the specified correction made in the design docs on this branch); none required a Relabel. No `charter.md`/`protocol.md` change. Downstream Phase C is unblocked.*

1. **Finding 1 (SERIOUS), the witness carrier regressed to the point-tag silhouette, the generalization gone. Closed FIXED.** The two-state `X = ULift Bool` carrier is replaced by a four-state carrier `WCar` (`aW`, `aW'` rank 0; `bW` rank 1; `cW` rank 2) on which rank is NON-INJECTIVE (`ws_witness_rank_noninjective : rankW aW = rankW aW' ∧ aW ≠ aW'`, so rank is coarser than identity, not a point-indicator). The general separation is reinstated (`ws2_many_general`: any reified relatum carrying a reified constituent of rank ≥ 1 separates from any rank-0 base relatum, over the general `rankLift`, with `reify`/`rank`/the strict-rank-step load-bearing in the STATEMENT). The concrete plurality `ws2_many_witness` now separates `cW` (a reified relatum carrying the reified constituent `bW`) from `aW`, an INSTANCE of the general theorem; the constituency-edge facts (`aW ∈ sW.1`, `bW = reifyW sW`) are kept for the WS4 model pair. Self-classification cites all three certificates (non-injectivity, tower-definedness, generalization); the strip test now reads to the general rank-separation fact on a coarse-rank carrier, not the two-Boolean fact. Owner: WS2 / the witness. Files: `spec/ws4-witness-design.md`, `spec/ws2-design.md`, `spec/README.md` §2.7/§2.8/§6.
2. **Finding 2 (SERIOUS), the tower phantom in the compass/convergence definitions. Closed FIXED.** `ConstituentOf dest reify x W := ∃ s, dest (reify s) = s ∧ x ∈ s.1 ∧ W = reify s` is defined once (README §2.10), and `ConvergesUp` is restricted to it: `Relation.ReflTransGen (fun x W => ConstituentOf dest reify x W ∧ Converges dest reify c x W)`, so "up its layers" follows genuine reification edges and stripping `reify` removes the edges. `ws3_compass_layered` is replaced (the `P ∨ ¬P` version deleted) by a two-sided freedom fact over a genuine `ConstituentOf` edge (some compass coheres, some does not, both inside existentials, the WS4 pair cited). WS3/WS4 strip-test annotations updated so the tower does not strip away. Owner: WS3/WS4. Files: `spec/README.md` §2.9/§2.10, `spec/ws3-design.md`, `spec/ws4-design.md`.
3. **Finding 3 (REAL), `ws4_wall_is_structural` a bare conjunction. Closed FIXED.** Replaced by the real independence: the two compasses share the IDENTICAL `orient` (`rfl`, so the sharing is IN the theorem) over the IDENTICAL `destW`, everything the structure supplies fixed, yet the verdicts flip on the exogenous raising alone. The bridge (the wall is structural) is now in the proof term, not the docstring; the optional decider-is-non-recoverable teeth are noted. Owner: WS4. File: `spec/ws4-design.md` C4/D4.
4. **Finding 4 (REAL), the verdict ignores its certificate, the fork inexpressible. Closed FIXED.** A data-level `ConvergenceFork dest reify a b` (three constructors `forcedHolds`/`forcedFails`/`underdet`, each carrying its proof) and `verdictOfFork` (branching, `underdet ↦ shapeDrawn`, the forced arms ↦ `convergenceDecided`) are defined (README §3). The `Audit` carries the WS4 result as a `fork` field, and `verdict cert := verdictOfFork cert.fork` branches on it; `ws5_verdict_eq` / `ws7_verdict_eq` become THEOREMS following from the fork being `underdet` (the model pair), not definitional constants. The pre-registered convergence-decided branch is now expressible. Owner: WS5/WS7. Files: `spec/README.md` §3, `spec/ws5-design.md`, `spec/ws7-design.md`.

## Closed log

*Phase C complete (Build-all): every Series 12 design is realized in `formal/Series12/wsNN.lean`, aggregated by `Series12.lean`, with `AxiomCheck.lean`; the full build is sorry-free and axiom-clean (standard three only), the closure gate passes, and the terminal verdict is SHAPE-DRAWN. The witness carrier is the four-state `WCar` (`ULift (Fin 4)`: `aW`, `aW'` rank 0; `bW` rank 1; `cW` rank 2), with `destW`/`reifyW`/`rankW`/`destWL` and the `dir := rank` labelling; `ws2_many_witness` separates `cW` (a reified relatum carrying the reified constituent `bW`) from the base `aW`, an instance of the general `ws2_many_general`, rank non-injective (`ws_witness_rank_noninjective`). The compass is `Compass dest reify Or` (WS3), held parametric; `Converges`/`ConvergesUp` (edge-restricted) and the `cHold`/`cFail` model pair (WS4) discharge `ws4_underdetermined`; the fork `ConvergenceFork`/`verdictOfFork` (WS5) makes the verdict branch. Two disclosed build realizations, faithful, not retargets: (i) the total `IsReify` is unsatisfiable on the concrete atomless carrier (Cantor), so the witness supplies the pointwise reification facts (`ws_reify_pointwise_sW`/`_sW₂`) the lemmas consume, the deviation the charter pre-registered; (ii) `Converges` is defined in `ws4.lean` (its design home) and the WS5/WS7 verdict type + fork live in `ws5.lean` (the earliest consumer, importing WS4), so `WS5 → WS6 → WS7` dependency order holds; no signature is changed. Next: Phase D (blind series-review).*

*Phase A complete: charter, charter-status, protocol written. The terminal question is the shape of the undefinable: the coincidence (WS1) of what Series 07 requires and what the diagonal generates, in the shape `¬ Recoverable`; knowing plural over the inhabited opening (WS2); the compass typed, layered, and never evaluated (WS3); convergence defined and proved underdetermined (WS4); the verdict computed and the neutrality generalized to all names (WS5); and no name ever a proof term.*

*Phase B complete: `spec/README.md` (the design index) plus the seven `spec/wsNN-design.md` and `spec/ws4-witness-design.md` written. The shared objects are LOCKED once in `spec/README.md` §2 and cited everywhere (the two Series-12 design duties, protocol §2): the opening `Opening realizable c := ¬ realizable c` (§2.7); `Many destL` (§2.8); the compass type `Compass dest reify Or` with fields `orient : X → Or`, `raise : X → X → Or → Or` (§2.9); the convergence relation `Converges c x W := c.raise x W (c.orient x) = c.orient W` and its layered closure `ConvergesUp` (§2.10); the generalized neutrality `ws5_name_neutral` (§2.11); and `Series12Verdict` (§3). The design's central moves, each foreclosing a false escape: WS1 states the coincidence as forced-for-all (`ws2_residue_free`, ∀ insp) vs exists-satisfying (`ws4_labelLoop_not_recoverable`, a witnessed import), with an anti-identity guard (distinct residues inhabit the shape), never object-identity. WS2/the-witness draws the plurality on a concrete `dir := rank` carrier where a reified relatum `bW = reifyW sW` and a base relatum `aW` are plain-bisimilar (`ws1_atomless_bisim`) yet rank-separated at the labelled level, `reify`/`reifyStep`/`towerN`/rank load-bearing, the genuine tower-distinction Series 10 (`ws2_reify_bisim_embeds`) and Series 11 (`ws1_attention_makes_real`) could not draw (both landed on `labelLoop`, a point-tag, re-graded Bookkeeping re-hit). WS3 holds the compass parametric (the only concrete compasses live inside existentials: `ws3_compass_exogenous`, the model pair). WS4 earns the underdetermination with a genuine non-degenerate model pair (`cHold`/`cFail` on the same carrier, sharing a non-constant orientation, differing only in the free raising, one cohering and one not), never deciding convergence by definition. WS5 computes the verdict from an audit and proves name-neutrality parametrically. WS7 owns the verdict and the five promoted checks. Next action: Phase C (build `formal/Series12/wsNN.lean`, `Series12.lean`, `AxiomCheck.lean`) in the dependency order WS1 → WS2 → WS3 → WS4 → WS5 → WS6 → WS7. The one disclosed structural deviation (total `IsReify` is unsatisfiable on a concrete atomless carrier by Cantor; the pointwise reification facts the lemmas actually consume, `dest (reify sW) = sW`, `(sW).1 ≠ ∅`, `∀ x ∈ (sW).1, SHNE dest x`, are supplied) is owned by `spec/ws4-witness-design.md` and routed here.*

---

*No em dashes in final academic copy. The premise presupposed and named is Series 07: the differentiator cannot be recovered from the relating. What is proved, not assumed: the shape-coincidence with honest quantifiers; inhabitability with a non-degenerate witness; the underdetermination of convergence by a genuine model pair, or the honestly-reported alternative if the structure decides. What is never proved, by design: the compass's content, convergence's direction, the act itself. The names, consciousness, choice, God, the compass, are the interpretive thesis, in prose, never terms. This is the program's terminal series: its completion is the exactly-drawn edge of the undefinable, and the drawing, not the filling, is the terminus.*
