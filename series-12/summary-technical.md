# Relational Existentialism, Series 12: Technical Status Summary

*A machine-checked drawing of the shape of the undefinable: the coincidence of the Series 07 import necessity with the diagonal residue in the single shape `¬ Recoverable`; the inhabited opening (plurality on a genuine tower-distinction); the compass typed and never evaluated; and convergence defined and proved underdetermined. The terminal series; the verdict is SHAPE-DRAWN.*

## 1. The headline

> **The differentiator the relating cannot recover (Series 07) and the residue self-inspection cannot contain (the diagonal) COINCIDE in one shape, `¬ Recoverable`; over the opening they draw, knowing is plural, the compass is typeable but not evaluable, and convergence is defined and provably undecidable by the structure.**

Four machine-checked payoffs, one computed verdict:

- **The coincidence (WS1).** `Opening realizable c := ¬ realizable c` is parametric in both the candidate type and the recoverability notion. `ws1_shape_coincidence` states the two inhabitations from opposite quantifier directions: FORCED-FOR-ALL (`∀ dest insp, Opening ResidueRecoverable insp`, the free residue `ws2_residue_free`) and EXISTS-SATISFYING (`Opening Recoverable (labelLoop hinf)`, a witnessed labelled import). They live in DIFFERENT TYPES, so object-identity is not expressible. Honestly this is a parametric negation wrapper over two type-heterogeneous recoverability predicates (`ResidueRecoverable`, `Recoverable`), one shape-schema instantiated twice, not one notion applied twice; `ws1_coincidence_not_identity` proves distinct residues both inhabit the shape, and `ws1_coincidence_not_identity_witness` DISCHARGES its hypothesis with the constant-`True`/constant-`False` inspections (pointwise-opposite residues on any inhabited `Hold`), so the anti-conflation is non-vacuous and shared non-recoverability cannot entail identity.
- **The inhabited opening (WS2).** `Many destWL` on a concrete four-state carrier: a reified relatum `cW` carrying the reified constituent `bW` and a base relatum `aW` are plain-bisimilar (`ws1_atomless_bisim` after `ws_plainOf`) yet rank-separated at the labelled level, an INSTANCE of the general `ws2_many_general` (with `reify`/`rank`/strict-rank-step load-bearing in the statement), on a carrier where rank is NON-INJECTIVE (`ws_witness_rank_noninjective`). The reality is also reader-relative: `ws2_attention_makes_real` builds a genuine `FiniteAttention` (a bounded reader) for which `cW` is real (`RealFor`), the reading load-bearing. Without an import, the One (`ws2_no_import_is_one`). The distinction's non-recoverability is its certificate (`ws2_distinction_free`).
- **The compass, typed (WS3).** `Compass dest reify Or` is a per-relatum orientation plus a raising over an exogenous space `Or`. `ws3_compass_exogenous` (an existential) proves some orientation differs on a plain-bisimilar pair, so orientation is not forced by the relating; `ws3_compass_exogenous_import` sharpens this to a proof term, that the orientation lifted to labels (`orientLift`) is `¬ Recoverable`, a genuine import (via the general `orientLift_not_recoverable`); `ws3_compass_layered` gives the two-sided freedom over the genuine constituency edge. Every compass-theorem is parametric; the only concrete compasses live inside existentials.
- **Convergence, underdetermined over the faithful class (WS4).** `Converges c x W := c.raise x W (c.orient x) = c.orient W`, a real compass-dependent equation over a constituency edge; `ConvergesUp` is its reflexive-transitive closure ALONG `ConstituentOf` edges. The independence is over the `Faithful` class (`c.raise x W = id`, the identity raising, a genuine structural constraint: convergence then tests orientation-coherence, `faithful_converges_iff`). `ws4_underdetermined` and the layered `ws4_underdetermined_up` exhibit two FAITHFUL non-degenerate compasses on one structure (`cHoldF`/`cFailF`), one cohering and one not, differing only in the orientation the structure does not fix; `ws4_wall_is_structural` carries the bridge (shared faithful raising by `rfl`, opposite verdicts) in the proof term. The fork is open AT THE GENUINE EDGE `(aW, bW)` as a TWO-ZONE fork whose boundary is Series 07's import boundary (`ws4_two_zone`): `ws4_decided_within_sight` forces convergence over the in-sight sub-class (`BisimInvariant` orientations, forced to agree on the plain-bisimilar `aW`, `bW`), and `ws4_dissent_is_import` proves every faithful dissent at the edge is `¬ Recoverable` of `orientLift` (Series 07's import, as a proof term). So the verdict reaches both `shapeDrawn` (full faithful class) and `convergenceDecided` (in-sight sub-class) at the SAME edge (`ws5_verdict_reaches_both`, `ws7_fork_can_close`); `ws4_fork_open` (a reflexive locus) is kept only as a remark, no longer load-bearing.

## 2. Verification status

- **`sorry`-free:** no proof-position `sorry`, `admit`, `native_decide`, `sorryAx`, or custom `axiom` anywhere in `ws1`–`ws7` (grep-clean; the `decide` calls are on decidable `ULift Bool` / `WCar` (in)equalities and small `ℕ` facts).
- **No custom axioms:** every headline theorem rests only on Mathlib's standard three, `propext` / `Classical.choice` / `Quot.sound` (`ws_witness_rank_noninjective` on `propext` / `Quot.sound`). `Classical.choice` enters via `Classical.propDecidable` for the reification section's `Set`-equality and via `toPk` / `Cardinal`, a Mathlib-base axiom, not a new one.
- **In-build verification, machine-run:** `Series12/AxiomCheck.lean` imports the whole build and runs `#print axioms` on the headline theorem of every workstream, committed at [`spec/axiom-check-log.md`](./spec/axiom-check-log.md), against **Lean 4 v4.15.0 / Mathlib v4.15.0**.
- **Closure:** `scripts/gate.sh` confirms `series-12/formal/` imports resolve only to `Series12.*` roots plus Mathlib. Series 12 is WHOLLY STANDALONE; the one prior result it presupposes and names, Series 07, is transcribed, not imported.
- **Blind series review (`spec/series-review-1.md`):** ZERO SERIOUS findings; the four Phase-B design-review closures verified genuine in the code (no recurrence). Four REAL findings (SR1-1 an audit self-description overclaim, SR1-2 an orphaned layered relation, SR1-3 a decorative duality theorem, SR1-4 a stale ledger) and code cosmetics were all closed in Phase E without touching the mathematics of the payoffs; the verdict was unchanged.

## 3. The spine

- **The carrier (transcribed).** The plain `P_κ`-coalgebra and the collapse engine (`ws1_atomless_bisim` / `hneRel_isBisim`, the Series 07 floor), the labelled/import test (`plainOf`, `Recoverable`, `labelLoop`, `ws4_free_label_is_import`, `ws4_labelLoop_not_recoverable`), the diagonal spine and free residue (`ws1_no_self_total_hold`, `ws1_diagonal_not_bisim`, `residue`, `ws2_residue_free`), the reification section and tower (`IsReify`, `ws1_reify_injective`, `reifyStep`, `towerN`, `prec`, `ws3_reify_preserves_SHNE`), and the diagonal renamed for attention (`ws3_no_total_attention = ws1_no_self_total_hold`, the transcribed diagonal under `TotalAttention`), all re-namespaced `Series12.WSn`. (`FiniteAttention`/`RealFor`/`AttentionDistinguishes` are Series 12's own definitions over this carrier, not transcribed; see the reader theorem `ws2_attention_makes_real`.)
- **The genuinely new Lean.** `Opening` + the coincidence, `ws1_coincidence_not_identity_witness` (WS1); `Many`, the four-state `dir := rank` witness carrier (`WCar`, `destW`, `reifyW`, `rankW`, `rankLift`, `destWL`), `ws2_many_general`, `ws_witness_rank_noninjective`, `FiniteAttention`/`RealFor`/`AttentionDistinguishes` and the reader theorem `ws2_attention_makes_real` (WS2); `Compass`, `ConstituentOf`, `ws3_compass_exogenous`, `orientLift`/`ws3_compass_exogenous_import`, `ws3_compass_layered` (WS3); `Converges`, `ConvergesUp`, the `Faithful` class, the `cHoldF`/`cFailF` faithful model pair, `ws4_underdetermined`, the two-zone edge fork (`BisimInvariant`, `ws4_decided_within_sight`, `ws4_dissent_is_import`, `ws4_two_zone`), `ws4_underdetermined_up`, `ws4_wall_is_structural` (WS4); `Series12Verdict`, `ConvergenceFork`, `verdictOfFork`, `ws5_verdict`, `ws5_verdict_reaches_both`, `ws5_name_neutral` (WS5); `ws6_provable_core`, `ws6_permanent_opens`, `ws6_tenets_aligned` (WS6); the `Audit`, `ws7_verdict` / `ws7_audit_verdict`, the checks incl. `ws7_fork_can_close` (WS7).
- **The witness carrier, concretely.** `WCar := ULift (Fin 4)`, with `aW`, `aW'` (rank 0, self-loops), `bW = reifyW {aW}` (rank 1), `cW = reifyW {bW}` (rank 2). `destW` gives each a nonempty singleton successor set, so all four are `SHNE`; the constituency graph is the path `aW → bW → cW` (used in `constituentOf_aW` for the layered negative side). The labelled lift `destWL := rankLift destW rankW` broadcasts each relatum's tower rank; `plainOf destWL = destW`.

## 4. The verdict, and the two disclosed realizations

- **The verdict is computed, not chosen.** `ConvergenceFork` is a data-level three-constructor inductive recording which disjunct of the trichotomy WS4 discharged; `verdictOfFork` branches (`underdet ↦ shapeDrawn`, the forced arms ↦ `convergenceDecided`). The fork landed at `underdet` (the model pair), so `ws5_verdict_eq` / `ws7_verdict_eq` are THEOREMS (not definitional constants) and `ws7_audit_verdict : verdict (ws7_audit hinf) = .shapeDrawn` ties the verdict to the certified audit. Had the structure forced convergence or its negation, the same function would have computed `convergenceDecided`.
- **Disclosed realization 1 (charter-pre-registered).** A total reification section `IsReify destW reifyW` is unsatisfiable on the concrete atomless carrier (Cantor: `|PkObj κ WCar| > |WCar|`), so the witness supplies the POINTWISE reification facts the lemmas consume (`ws_reify_pointwise_sW`, `ws_reify_pointwise_sW₂`), never the total section. Faithful narrowing, disclosed, not a retarget.
- **Disclosed realization 2 (dependency homing).** `Converges` is defined in `ws4.lean` (its design home) and the verdict type + fork in `ws5.lean` (the earliest consumer, importing WS4 whose `Converges`/`NonDegenerate` the `underdet` constructor references), so the `WS5 → WS6 → WS7` order holds. No signature changed; `ws3_compass_layered` states the raise-equation raw (which IS `Converges` unfolded) to avoid a forward reference.

## 5. What is open, by design and by theorem

Three things stay permanently open, each open AS A THEOREM (the diagonal makes them so, not want of effort), stated in `ws6_permanent_opens`: the **content of the compass** (the residue is free for every inspection, `ws2_residue_free`; the orientation is not forced by the relating, `ws3_compass_exogenous`), the **direction of convergence** (underdetermined, `ws4_underdetermined`), and the **differentiating act itself** (choice, consciousness, the will; named only in prose, never a term). The universal theses (every κ-free tower, every inhabitant) are the un-rangeable quantifier, reported heuristic; the mechanized core `ws6_provable_core` is the honest floor.

The honest terminus is **SHAPE-DRAWN** (`ws7_verdict_is_shapeDrawn`): the coincidence proved with the quantifiers honest, the opening inhabitable non-degenerately, the compass typed and evaluated nowhere, and convergence defined and underdetermined by a genuine model pair. The program closes not on a filled slot but on an exactly-drawn edge, and the drawing is the completion.
