# Relational Existentialism, Series 13: Technical Status Summary

*A machine-checked account of the FIT: the Series 12 shape-coincidence upgraded to a Galois connection between inspections and labelled coalgebras, via a mint that carries the diagonal's total residue into the class of imports, with the mint's non-surjectivity up to equivalence the payoff. The verdict is DUAL at the flat layer.*

## 1. The headline

> **The diagonal manufactures imports; the manufacture is a Galois connection and not an equivalence; and the imports it cannot manufacture are located, structurally, and never sorted.**

Four machine-checked payoffs, one computed verdict:

- **The two orders (WS1, the knot).** The obstruction was a VARIANCE WALL: transport is universal (`∀ insp`) and its only engine is the diagonal (`ws2_residue_distinct`), so the mint must broadcast the residue, which is antitone under pointwise content-implication. The resolution orders inspections BY their residue (`instLEInsp`), making the residue covariant and the mint monotone; and orders labelled coalgebras with the residue-position covariant and the reference-position ANTITONE through its self-bit at `h₀` (`instLELab : le d₁ d₂ := leC d₁.cT d₂.cT ∧ (d₂.cF h₀ → d₁.cF h₀)`). Both orders are proved non-trivial (`ws1_orders_insp_nontrivial`, `ws1_orders_lab_nontrivial`), so DISCONNECTED was not triggered and the connection is not vacuous.
- **The mint and the transport (WS2).** `mintL h₀ insp : Lab dest h₀ := ⟨residue insp, insp h₀⟩`. `ws2_mint_lands_in_opening` and its universal form `ws2_transport_forall` prove every minted label fails `Recoverable`, the engine being the diagonal itself: recovering the minted label would realize what `ws2_residue_free` proves unrealizable. The FORCED-FOR-ALL funds the EXISTS-SATISFYING. Exogeneity is PROVED, not assumed: `ws2_mint_exogenous` (with `plainOf_coalg_val` proving rather than assuming the ground) and `ws2_mint_not_plain_function`, which overshoots its contract in the honest direction.
- **The connection (WS3).** `readInsp` is the best-approximation adjoint, folding the reference-bit into the residue so the Galois iff is exact. `ws3_galois : GaloisConnection (mintL h₀) (readInsp h₀)` (Mathlib), with `ws3_mint_monotone` / `ws3_read_monotone`. `ws3_roundtrip_closure` gives `readInsp ∘ mintL = id` up to the order (a two-sided `≤`; the order is a preorder, so literal equality is unavailable). `ws3_roundtrip_not_identity` proves the other round trip is NOT the identity at a named witness, discharging both disjuncts of the fold rather than asserting the gap. The connection is genuinely adjoint, not an isomorphism in disguise.
- **The defect (WS4).** `outWit h₀ h₁ : Lab dest h₀ := ⟨(fun _ => True), (fun h => h ≠ h₁)⟩`. `ws4_mint_not_surjective` proves `¬ ∃ insp, labEquiv h₀ (mintL h₀ insp) (outWit h₀ h₁)` on `h₁ ≠ h₀`: mintability is tested UP TO `≈` (`labEquiv b b' := b ≤ b' ∧ b' ≤ b`), not the cheaper literal equality that would have made DUAL true by construction. `ws4_exclusion_structural` carries the reason: every mint satisfies the DIAGONAL LINK `b.cT h₀ = ¬ b.cF h₀` (by `rfl`, since `mintL`'s two fields are `residue insp` and `insp h₀`), the link survives `≈`, and `outWit` is off it. The label does the excluding, not cardinality, universe level, or typing (`labelLoop` was rejected as artifactual in design). `ws4_mint_essentially_surjective_degenerate` builds the losing branch: on `|Hold| = 1` the mint IS essentially surjective.

## 2. Verification status

- **`sorry`-free and axiom-clean:** no proof-position `sorry`, `admit`, `native_decide`, `sorryAx`, or custom `axiom` in `ws1`–`ws5`. Every headline rests only on Mathlib's standard three (`propext`, `Classical.choice`, `Quot.sound`), captured by `Series13/AxiomCheck.lean` at [`spec/axiom-check-log.md`](./spec/axiom-check-log.md), against **Lean 4 v4.15.0 / Mathlib v4.15.0**.
- **Closure:** `scripts/gate.sh` now covers `series-13/formal/` (executed, not assumed) and confirms imports resolve only to `Series13.*` roots plus Mathlib. Series 13 is WHOLLY STANDALONE; the two prior results it presupposes and names, Series 07 (`ws2_import_theorem`, `ws3_atomless_distinct_is_import`, `ws1_atomless_bisim`) and Series 12 WS1 (`ws1_shape_coincidence`, `ws1_coincidence_not_identity`, `ws1_coincidence_not_identity_witness`), are transcribed, not imported.
- **Blind series review, pass 1 (`spec/series-review-1.md`):** TWO SERIOUS findings, both on the series' signature risk (the order solved-for-the-map): SR1-1 (the antitone reference clause never exercised non-vacuously) and SR1-2 (the mint points not certified where the mint lives). Both closed as genuine **Fixed** under §0.2a, verified at pass 2 by unfolding the proof terms: the antitone clause now fires in a certificate (`True → False`, on witnesses sharing `cT` by `rfl`, so the residue clause cannot be doing the work). Neither closed as a third adjacent theorem.
- **Blind series review, pass 2 (`spec/series-review-2.md`):** ZERO SERIOUS, no recurrence. All five discipline checks pass (fork-open, genuine-connection, exogeneity, structural-defect, names-not-terms), the genuine-connection check now passing for the right reason. Four REAL findings, all bounded, none touching the verdict: SR2-1 (the axiom check is builder-attested, reviewer-unverified), SR2-2 (no carrier exhibited; since closed, DUAL now UNCONDITIONAL at a named two-hold carrier), SR2-3 (a design signature that provably cannot be built), SR2-4 (blindness not mechanically honorable while criteria and metaphysics shared a file; closed by extracting [`spec/success-criteria.md`](./spec/success-criteria.md), so the reviewer's seed is now a file that does not contain the prose).

## 3. The spine

- **The carrier (transcribed).** The plain `P_κ`-coalgebra and the collapse engine (`ws1_atomless_bisim`, `ws2_import_theorem`, `ws3_atomless_distinct_is_import`, `ws3_dichotomy`); the labelled/import test (`plainOf`, `Recoverable`, `labelLoop`, `ws4_labelLoop_not_recoverable`, `ws4_label_survives_quotient`, `ws4_recoverable_not_import`); the diagonal spine and free residue (`Hold`, `afford`, `HoldPred`, `diag`, `SelfTotal`, `ws1_no_self_total_hold`, `ws1_insp_not_surjective`, `ws1_diagonal_not_bisim`, `residue`, `ws2_residue_distinct`, `ws2_residue_free`, `ws2_residue_is_import`); and Series 12 WS1's coincidence apparatus (`Opening`, `ResidueRecoverable`, `ws1_two_halves`, `ws1_shape_coincidence`, `ws1_coincidence_not_identity`, `ws1_coincidence_not_identity_witness`). All re-namespaced `Series13.WSn`.
- **The genuinely new Lean.** `Insp` (the inspection type), `Lab` (the two-field mint codomain: `cT` the residue-position self-loop label, `cF` the reference-position), `instLEInsp` / `instLELab` and their non-triviality (WS1); `mintL`, `coalg` / `plainOf_coalg_val`, `ws2_mint_lands_in_opening`, `ws2_transport_forall`, `ws2_mint_exogenous`, `ws2_mint_nontrivial`, `ws2_mint_not_plain_function` (WS2); `readInsp`, `ws3_galois`, `ws3_mint_monotone`, `ws3_read_monotone`, `ws3_roundtrip_closure`, `ws3_roundtrip_interior`, `ws3_roundtrip_not_identity` (WS3); `labEquiv`, `outWit`, `coalg_recoverable_of_eq`, `ws4_mint_not_surjective`, `ws4_exclusion_structural`, `ws4_mint_essentially_surjective_degenerate` (WS4); `Series13Verdict`, `FitFork`, `verdictOfFit`, `ws5_verdict_eq`, `ws5_verdict_degenerate`, the falsifiability triple (`ws5_verdict_not_total`, `ws5_verdict_not_disconnected`, `ws5_verdict_not_partial`), the audit checks (`ws5_audit_genuine_connection`, `ws5_audit_exogeneity`, `ws5_audit_structural_defect`), `ws5_dual_witnessed`, `ws5_total_witnessed` (WS5).
- **The two negations, concretely.** The mint's fields are `⟨residue insp, insp h₀⟩`, so at `h₀` the residue-position IS the negation of the reference-position (`residue insp h₀ = ¬ insp h₀ h₀ = ¬ (mintL h₀ insp).cF h₀`, by `rfl`). That is the diagonal link, and it is why the diagonal-born import's two halves are tethered: a content and its own self-application's negation. `outWit`'s halves are decoupled (`⊤` and `≠ h₁`), and the decoupling is what no inspection produces. Interpretively (prose only): the diagonal's `¬` is a self-not, derived and free; the import's `≠` is an other-not, requiring an individual already picked out, which behavioral identity structurally does not supply.

## 4. The verdict, and the disclosed realizations

- **The verdict is computed, not chosen.** `FitFork` is a data-level inductive recording which disjunct WS4 discharged; `verdictOfFit` branches. `ws5_verdict_eq : verdictOfFit … = .dual` on `h₁ ≠ h₀` and `ws5_verdict_degenerate : … = .total` on the single-hold carrier are THEOREMS, not definitional constants, and the same function reaches both values. The falsifiability triple proves the verdict is not the other three. `ws5_dual_witnessed` / `ws5_total_witnessed` exhibit both branches firing.
- **Disclosed realization CD-1 (the `Lab`-vs-`LkObj` narrowing, per SR1-3).** The charter speaks of an order on "labelled coalgebras"; the build realizes the mint-codomain as `Lab dest h₀`, a two-field struct of contents, with `coalg` realizing it as a genuine `LkObj` on the two-region carrier `MCar = ULift Bool`. Every `Recoverable` fact IS about a real coalgebra (`coalg b`), so `¬ Recoverable (coalg (outWit …))` is a genuine coalgebra import fact; the narrowing is only in SCOPE, non-surjectivity proved onto the two-region self-loop coalgebras up to `labEquiv` rather than onto all labelled coalgebras over `dest`. A faithful realization, disclosed, not a retarget.
- **The flat-layer bound.** The reification tower (`IsReify`, `reifyStep`, `towerN`) is deliberately outside the transcribed carrier. Series 13 is a single-layer result and says so: whether the connection commutes with reification (LAYER STABILITY) is a named open, and every TOTAL is scoped as "TOTAL at the flat layer," bounded by the unexamined tower.

## 5. What is open, by design and by theorem

The program's permanent opens are untouched: the content of the compass, the direction of convergence, and the differentiating act itself. Series 12's result stands; this series fills none of them and adds no name.

**One new permanent open joins them, open AS A THEOREM by the same result that locates it: the classification of the out-of-image imports.** `ws4_mint_not_surjective` proves an import lies outside the mint's reach up to `≈`. No theorem, definition, or discharged obligation sorts it into the given or the chosen; grep-clean across two review passes, and the central sin the charter names (§4.a) was never approached. The defect LOCATES and stops.

Two further bounds are named rather than glossed: **layer stability** (does the fit survive reification?) and the **CD-1 scope** (does non-surjectivity hold onto all labelled coalgebras, not only the realized two-region class?).

The honest verdict is **DUAL at the flat layer** (`ws5_verdict_eq`), on the domain (CD-1) and the layer the series openly draws it, unconditional at a named two-hold carrier. The required and the generated do not merely share a shape; they face each other as an adjoint pair, the adjunction has a genuine defect, and the defect is the door Series 07 left open, now located exactly and still unopened.
