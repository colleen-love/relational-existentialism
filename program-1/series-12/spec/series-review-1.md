# Series 12, Series Review 1 (Phase D)

**The blind whole-series review of the Phase C build on this branch, per protocol §2 Phase D. Findings carry stable IDs (SR1-N) for the Phase E closure and the pass-2 recurrence check.**

**Disclosure (protocol §3).** This review is Claude-reviewing-Claude, and it is NOT blind in the protocol's strict sense: the reviewer held the full context, including the charter's motivating prose and the complete design history, rather than only the code, the design signatures, and the charter criteria. That limitation is stated, not waved away. The anchors that keep the review honest despite it: every check below was run mechanically against the source (greps, the captured axiom log, statement-by-statement comparison of code to design signatures), the two load-bearing proofs were traced line by line (`ws2_many_general`, the model-pair discharges), and every finding cites the exact declaration and the exact defect, so the findings are independently checkable.

**Scope.** `series-12/formal/Series12/{ws1..ws7, AxiomCheck}.lean` + `Series12.lean` (1,271 lines), `spec/axiom-check-log.md`, `charter-status.md`, against the design contracts in `spec/*.md` and the charter's success criteria (§6) and false escapes (§4).

---

## 0. Hygiene and the design-review-1 recurrence check

**Hygiene: PASS.** No `sorry` and no custom `axiom` anywhere in `formal/` (the greps hit docstring prose only). No cross-series imports (`import Series12.*` and Mathlib only); the series is self-contained, with the Series 07 engine and the diagonal transcribed, not imported. The captured `spec/axiom-check-log.md` records 35 headline theorems, every one on exactly `propext` / `Classical.choice` / `Quot.sound`. The names-not-terms grep is clean: no declaration or proof term named consciousness, God, choice, or compass-as-content (the type `Compass` is the permitted shape).

**Recurrence check on design-review-1 (protocol §0.2a): all four closures verified GENUINE in the code, none target-avoiding.**
- **F1 (witness/point-tag):** the built carrier is the four-state `WCar` with `aW'`; `ws_witness_rank_noninjective` exists as its own theorem; `ws2_many_general` exists with `reify`/`rank`/strict-rank-step load-bearing in the statement; `ws2_many_witness` is its instance at `(cW, aW)`. Verified: the general proof was traced in full and is sound (the labelled edge `(rank (reify s), z)` exists via `hpt`, the base side carries label `rank y = 0`, and `omega` closes on `hstep`, `hw₀rank`, `hyrank`; note `hw₀rank` IS load-bearing here, resolving the design-review nit in the keep direction, recorded below as SR1-note).
- **F2 (tower phantom):** `ConstituentOf` is defined once (ws3) and `ConvergesUp` is edge-restricted to it (ws4); `ws3_compass_layered` is the two-sided freedom fact over a genuine edge, the `P ∨ ¬P` version gone.
- **F3 (bare-conjunction wall):** `ws4_wall_is_structural` carries the bridge in the statement and proof term (`(cHold).orient = (cFail).orient` by `rfl`, opposite verdicts, difference isolated to the raising).
- **F4 (constant verdict):** `ConvergenceFork` is a data-level three-constructor inductive; `verdictOfFork` genuinely branches; `ws5_verdict_eq`/`ws7_verdict_eq` are theorems through the `underdet` branch (a forced fork would compute `convergenceDecided` through the same function); the falsifiability certificates route through the fork.

---

## 1. The protocol's Phase D checks, run

**The no-evaluation check (§0.4, the central check).** Every compass-theorem that discharges a compass-parametric obligation is parametric; no theorem evaluates a distinguished compass to prove something for all compasses; concrete compasses are confined to ws3 (inline existential witnesses) and ws4 (the model pair). WS5, WS6, WS7 contain no occurrence of `cHold`/`cFail` and consume only existential projections. **PASS in substance, with one letter-level defect: SR1-1 below** (the audit's own description of this check overclaims, and one theorem's statement form breaches the stated discipline's letter).

**The underdetermination check (§0.5, the wall).** `Converges c x W := c.raise x W (c.orient x) = c.orient W` is a real compass-dependent equation (not `True`, not `False`, not reflexive). `ws4_underdetermined` carries the `ConstituentOf` edge INSIDE the theorem (a strengthening over the design signature), and the model pair is genuine: `cHold`/`cFail` on the same `destW`, sharing one non-constant `orient` (`rfl`), differing only in the raising (`not` vs `id`, both non-constant on `ULift Bool`), one cohering, one not, both `NonDegenerate` discharged. Both truth values realized; nothing vacuous; nothing decided by definition; the trichotomy (`ws4_convergence_decided_shape`) lands in the third disjunct by exhibiting the pair. **PASS at the one-layer relation. One gap at the layered relation: SR1-2 below.**

**The shape-honesty check (§0.6).** `Opening realizable c := ¬ realizable c` is parametric in both the candidate type and the recoverability notion. `ws1_shape_coincidence` keeps forced-for-all (`∀ dest insp, Opening ResidueRecoverable insp`) and exists-satisfying (the witnessed labelled import) in DIFFERENT TYPES, so object-identity is not even expressible; `ws1_coincidence_not_identity` proves shared shape cannot entail identity (distinct residues both inhabit it); `ws1_two_halves` states the required (Series 07 trichotomy import) and the generated (free residue, universal diagonal) sides. **PASS.** (Noted, not a defect: the coincidence's import-side witness is the junk label, forced by dependency order since ws1 cannot see ws2's rank witness; this is even pointed, since a junk label inhabiting the shape IS the anti-identity argument, and `ws7_strip_ledger` cites both the junk label and the genuine tower witness.)

**The genuine-inhabitation check (§0.6a).** The separated relatum `cW` is a reified relatum carrying the reified constituent `bW` (rank 1), `dest (reify sW₂) = sW₂` pointwise, on a carrier where rank is non-injective (`aW`/`aW'`) and the separation is an instance of the general theorem. The without-import side is the genuine engine (`ws2_no_import_is_one` via `ws2_import_theorem_static`; the plain-bisimilarity of the pair via `ws1_atomless_bisim` after `ws_plainOf`). The distinction's non-recoverability is proved as its certificate (`ws2_distinction_free`). **PASS. Not a point-tag.**

**The names-not-terms check (§0.6b).** Grep clean (above). The formal surrogate `ws5_name_neutral`/`ws7_names_not_terms` has a phantom `name` parameter, honestly described in its docstring; the enforceable check is this meta-level grep, which passed. **PASS, with a labeling nit: SR1-7.**

**The duality check.** `ws3_attention_compass_dual` is a bare conjunction of a structure-field projection (`att.fin`, true by construction of `FiniteAttention`) and a restatement of `ws3_compass_exogenous`. The duality is carried entirely by the docstring. **FAIL as a theorem-form claim: SR1-3 below.**

**The strip test (§0.3).** The coincidence strips to two non-recoverability facts with visibly distinct quantifiers (and `ws7_strip_ledger` states exactly these). The plurality strips to a GENERAL labelled-separation fact with the reification hypotheses in the statement, on a coarse-rank carrier: NOT the two-Boolean fact (the two-Boolean shape survives only as the transcribed touchstone `ws4_label_survives_quotient`, correctly quarantined). The convergence strips to the model-pair fact. **PASS.**

**Cross-workstream laundering.** Every Audit field and every WS5/WS6/WS7 projection was traced to a genuine WS1-WS4 theorem; no claim rests on a hypothesis another workstream left open; the fork's `underdet` constructor carries `(ws4_underdetermined).2` itself. **PASS.**

**The verdict fork.** The code delivers SHAPE-DRAWN, computed (`ws7_audit_verdict : verdict (ws7_audit hinf) = .shapeDrawn`), matching its object: coincidence honest + opening inhabitable non-degenerately + compass parametric + convergence underdetermined by a real pair. The alternatives are expressible and would be computed had they held. **PASS.**

**The axiom-check status.** `#print axioms` was actually run and captured (35 records, standard three). The `True`-typed `ws6_universal_heuristic` is correctly NOT among the checked headlines. **PASS.**

---

## 2. Findings

**SERIOUS: none.** Checked against every SERIOUS criterion: no canonical compass discharges a parametric obligation; convergence is not decided by definition (both values realized by non-degenerate inhabitants); the coincidence is shape-only (different types, distinct quantifiers); the inhabitation is not a point-tag (non-injective rank, general theorem, reification in the statement); no name is a proof term; no design-review-1 finding recurred (all four verified Fixed in the code).

### SR1-1 (REAL) — the audit's no-evaluation check overclaims about its own codebase. Owner: WS7 (and WS4 under one resolution).
`ws7_no_evaluation`'s docstring asserts "the only concrete compass is inside an existential; no theorem has a distinguished compass as a non-existential subterm." The codebase falsifies the letter of this: `ws4_wall_is_structural` names `cHold`/`cFail` in its STATEMENT (not under an `∃`), and the four helper lemmas (`cHold_converges`, `cFail_not_converges`, `cHold_nondeg`, `cFail_nondeg`) are declarations with distinguished compasses as non-existential subterms. Moreover the theorem's formal content (an `Iff.rfl` unfold plus a restatement of the exogeneity existential) cannot certify the docstring's claim; that claim is inherently meta and is checked by this review, which finds it false AS STATED and true under the honest formulation ("no compass-parametric obligation is discharged by evaluating a distinguished compass; concrete compasses occur only as model-pair/witness constructions"). The substance of the central sin is absent; the audit's self-description is wrong. **Correction owed (exactly one of):** (a) restate `ws4_wall_is_structural` existentially (`∃ c₁ c₂, c₁.orient = c₂.orient ∧ Converges … c₁ aW bW ∧ ¬ Converges … c₂ aW bW`, a strict weakening that still carries the bridge) and make the four helper lemmas `private` (or inline them), after which the docstring is true as written; or (b) keep the statement forms and amend `ws7_no_evaluation`'s docstring (and the matching line in `spec/ws7-design.md` and the ledger) to the honest formulation. Record which in `charter-status.md`.

### SR1-2 (REAL) — `ConvergesUp` is an orphaned definition: the layered form has zero theorems. Owner: WS4.
The Finding-2 fix correctly put the tower into `ConvergesUp`'s definition, but no theorem anywhere mentions `ConvergesUp` (grep: docstrings only). The charter's convergence is "coheres UP ITS LAYERS"; the wall is proved only at the one-layer relation. The layered underdetermination is PROVABLE on this carrier: positive side, `ConvergesUp … cHold aW bW` by `ReflTransGen.single ⟨ws3_edge_aW_bW hinf, cHold_converges hinf⟩`; negative side, `¬ ConvergesUp … cFail aW bW` by inversion on the first step (the constituency edges of `WCar` are exactly `(aW, bW)` and `(bW, cW)`: any `s` with `x ∈ s.1` and the pointwise fact forces `s.1 = {aW}` or `s.1 = {bW}`, so the only edge out of `aW` is to `bW`, whose `Converges cFail` step fails; a small edge-classification lemma plus `Relation.ReflTransGen.head_induction_on` or `cases_head`). **Correction owed:** build `ws4_underdetermined_up` (the layered pair, both sides), and add it to the strip ledger and the axiom check; or, if the negative side genuinely resists, build the positive side and record the precise remaining gap in `charter-status.md` routed to WS4. The layered relation must not remain a zero-theorem definition in the series build.

### SR1-3 (REAL) — `ws3_attention_compass_dual` is a labeled bare conjunction. Owner: WS3.
The theorem conjoins a field projection (`fun att => att.fin`, true by construction of the `FiniteAttention` type, zero content) with a restatement of `ws3_compass_exogenous`. "THE DUALITY" is carried by the docstring; the conjuncts never interact. This is the same juxtaposition pattern design-review-1 Finding 3 retired from the wall theorem, recurring at WS3 (it is NOT a §0.2a RECURRING, which tracks findings by ID; it is the same shape at a new site). **Correction owed (exactly one of):** strengthen to a statement with contrastive content if one exists (none is obvious to this reviewer: the duality is interpretive); or demote, moving the duality reading to prose/docstring (WS6/§8 territory), deleting the theorem, and recording the demotion in `charter-status.md`. A demotion here is the honest close and costs nothing downstream (nothing consumes it; it is in the axiom log and would be removed from it).

### SR1-4 (REAL) — the ledger is internally inconsistent after the build. Owner: charter-status.md.
The program-level snapshot's headline row records "SHAPE-DRAWN (built, Phase C)" with theorem names, and the Phase C closed-log entry is complete (both disclosed realizations recorded), but the rest of the ledger was not swept: "The wall" and "The standing open" rows still read "Not started," as do the per-workstream sections and the result tracker (27 stale "Not started" occurrences). The ledger is the instrument the recurrence guard runs on; it must not contradict itself. **Correction owed:** sweep every table and section to the post-build state (built theorems named per row; the phase table advanced through C with D in progress).

### Cosmetic / acceptable (fix opportunistically in Phase E; none blocks)
- **SR1-5.** `ws3_compass_layered` takes `hedge` as a hypothesis but never uses it; the freedom facts hold for any pair. Either make the edge a conjunct of the conclusion (mirroring `ws4_underdetermined`, strictly more honest) or add one docstring line ("the edge sites the statement; the freedom is unconditional").
- **SR1-6.** `ws6_universal_heuristic : True := trivial` is a contentless named theorem. Delete it or demote to a module comment (it is already correctly absent from the axiom check).
- **SR1-7.** `ws5_name_neutral`/`ws7_names_not_terms`: the phantom `name` parameter is the object-level surrogate of a meta claim; the docstrings are honest about the mechanism but should add one line saying the enforceable check is the meta-level grep (run by this review: clean).
- **SR1-8.** `ws6_tenets_aligned`'s docstring says "the four tenets" and lists three; the theorem has three conjuncts. Add the reification spine ("To relate is to create": e.g. `ws1_reify_injective` applied, or the `reify_mem_reifyStep` instance from the witness) as a fourth conjunct, or correct the docstring to three.
- **SR1-9.** Docstring precision: `ws3_compass_exogenous` and `ws6_permanent_opens` gloss the exogeneity existential as "the compass is `¬ Recoverable`"; the theorem's content is "some orientation differs on a plain-bisimilar pair" (orientations are unconstrained by bisimilarity). Tighten the prose; the theorems are right.
- **SR1-10.** `ws1_diagonal_not_bisim`'s first conjunct is subsumed by its second (instantiate the universal); harmless transcription redundancy.
- **SR1-11.** The ledger's Phase B closed-log entry names archived series by number with their theorem names, contra the standalone discipline applied to Series 12 documents (the one namable prior is Series 07). Historical honesty in the log is valuable; scrub the numbers to "the program's earlier re-graded runs" or record an explicit ledger exemption, either is fine, but decide it rather than drift.

**SR1-note (no action owed, recorded for the ledger).** The design-review nit on `ws2_many_general`'s `hw₀rank` hypothesis resolved in the KEEP direction: under source-labeling the hypothesis is genuinely load-bearing in the built proof (the `omega` closes on `rank (reify s) > rank w₀ ≥ 1` against `rank y = 0`), so the statement's "carries a reified constituent" family is both honest and used. No signature reshaping occurred; the disclosure discipline held.

---

## 3. What survives cleanly

The verdict itself. SHAPE-DRAWN is genuinely computed from a fork that could have said otherwise, and every leg it stands on checks out: the coincidence with honest quantifiers in unshareable types (WS1, which this review passes wholesale, transcriptions verified by inspection including the diagonal, the residue machinery, and the tower); the inhabited opening on a genuine tower-distinction (WS2 wholesale: the general theorem's proof traced sound, the witness an instance, rank non-injective, the plain merge genuine, non-recoverability proved as the certificate); the parametric compass with concrete inhabitants confined to witnesses (WS3, modulo SR1-3's decorative duality theorem); the earned underdetermination at the one-layer relation with the bridge in the proof term (WS4, modulo SR1-2's missing layered instance); the branching verdict with real falsifiability (WS5/WS7); the honest restatements (WS6, modulo two docstring nits); both disclosed build realizations faithful and on the ledger (the pointwise-`IsReify` narrowing pre-registered by the charter; the `Converges`-in-ws4 / fork-in-ws5 homing with no signature change); and the full axiom log.

## 4. Honest bottom line

Zero SERIOUS findings; the four design-review-1 closures are genuine in the code; the verdict is honestly computed and survives adversarial reading. Four REAL findings remain: the audit overclaiming about its own code (SR1-1, the most important, because an audit that misdescribes its codebase is the seed of the laundering pattern even when the substance is clean), the orphaned layered relation (SR1-2, buildable on this carrier), the decorative duality theorem (SR1-3, demotion is honest), and the inconsistent ledger (SR1-4, mechanical). All four are bounded, none touches the mathematics of the payoffs, and none changes the verdict. Per the canonical run (B → C → D → E → D → E), Phase E should close SR1-1 through SR1-4 (each Fixed or Relabeled, named in the ledger), take the cosmetics opportunistically, and series-review-2 will run the §0.2a recurrence check on these IDs first.
