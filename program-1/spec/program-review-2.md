# Relational Existentialism, Program Review 2 (adversarial; the recurrence check on the program-review-1 closures, run first per protocol §0.2a)

**Scope.** The closure commit `ac5cdf2` ("Address program-review-1 findings") read against `spec/program-review-1.md`'s correction-owed binaries, the charter's success criteria, and protocol §0.2a. Files traced line by line: `ws1.lean` (the R2 witness), `ws2.lean` (the S2 reader theorem and the R3 disclosures), `ws3.lean` (the R1 bridge: `orientLift`, `orientLift_not_recoverable`, `ws3_compass_exogenous_import`), `ws4.lean` in full (the S1 closure: `Faithful`, the `cHoldF`/`cFailF` pair, `ws4_underdetermined`, `ws4_fork_open`, `ws4_wall_is_structural`, the layered pair), `ws5.lean`/`ws7.lean` (the fork, `ws5_verdict_reaches_both`, `ws7_fork_can_close`), `AxiomCheck.lean`, and the swept prose (README, charter, charter-status, both summaries, `spec/ws2-design.md`, `spec/README.md`). `ws6.lean` read for the R1 sweep residual.

**Verification note (disclosed).** As in program-review-1, this environment cannot fetch the Mathlib build cache, so the build was not independently recompiled. What was run: sorry/custom-axiom greps (clean), names-not-terms grep (clean), cross-series import check (clean; the gate script's logic re-read, sound), AxiomCheck coverage check (every new headline is listed: `ws1_coincidence_not_identity_witness`, `ws2_attention_makes_real`, `ws3_compass_exogenous_import`, `ws4_underdetermined`, `ws4_fork_open`, `ws4_underdetermined_up`, `ws5_verdict_reaches_both`, `ws7_fork_can_close`), and line-by-line proof tracing of every theorem cited in a finding. No finding below depends on a compilation question.

**Grading.** As in program-review-1. For a RECURRING finding the correction owed is the binary of protocol §0.2a and nothing else.

---

## The recurrence check on the six closures (the review's first duty)

- **PR1-S2 — FIXED, verified.** `ws2_attention_makes_real : ∃ att : FiniteAttention (destWL hinf), RealFor (destWL hinf) att cW` is exactly the named target, built with exactly the sanctioned construction (focus `aW`, reads `{aW}`, `Set.finite_singleton`, `ReflTransGen.refl`), proof traced sound through `ws2_cW_distinguishes_aW` and `ws2_many_general`. The reader (`att.reads` membership) is load-bearing in the statement. The nonexistent carrier theorems are struck from the charter and ledger; `FiniteAttention`/`RealFor`/`AttentionDistinguishes` are honestly reclassified as Series 12's own definitions. The Series 11 S1 inheritance closes on both prongs. This is a genuine fix.
- **PR1-R1 — FIXED, verified (one cosmetic residual).** `orientLift`, `plainOf_orientLift`, and the general `orientLift_not_recoverable` are sound (the `rankLift` mechanism, correctly generalized), and `ws3_compass_exogenous_import` is the named target verbatim: an existential, the witness compass confined to it, the `¬ Recoverable` gloss now a proof term. Residual: `ws6_provable_core`'s docstring (untouched by the closure) still attributes "`¬ Recoverable`" to its `ws3_compass_exogenous` conjunct, which proves orientation-freedom, not non-recoverability (PR2-C1).
- **PR1-R2 — FIXED, verified.** `ws1_coincidence_not_identity_witness` discharges the assumed non-identity with the constant-`True`/constant-`False` pair on any inhabited `Hold`, as specified; proof traced sound.
- **PR1-R3 — FIXED, verified.** The `TotalAttention`/`ws3_no_total_attention`/`ws2_attention_subtractive` docstrings now disclose the rename and the bare conjunction at all three sites; no target theorem is claimed to exist. The honest demotion.
- **PR1-R4 — substantially FIXED.** The carrier row names only built theorems; the WS3 row carries the import theorem; the snapshot is advanced. Residual conditional on S1: see PR2-R2.
- **PR1-S1 — RECURRING.** See PR2-S1. The named targets were built as named, in good faith, and the artifact genuinely improved; but the defect's consequence still decides the verdict, which is precisely the case §0.2a's recurrence check exists for, as program-review-1's own recurrence facet stated in advance.

---

## SERIOUS findings

### PR2-S1 (SERIOUS, RECURRING: program-review-1 S1, count 2; the design-review-1 Finding 2 strip-test facet, program count 3) — The constraint pins the component the constrained relation no longer tests: over the faithful class the trichotomy is decided by pair-reflexivity, the forced arms remain meta-provably uninhabitable at every distinct-relata locus on every structure, and the new falsifiability is carried by a reflexivity triviality at a locus that is not a constituency edge. Owner: WS4, WS5/WS7 downstream, with README/charter/summaries as prose co-owners.

**The defect, mechanically.** `Faithful c := ∀ x W, c.raise x W = id` pins `raise`; `faithful_converges_iff` then reduces `Converges` to `c.orient x = c.orient W`; and `orient` is an unconstrained field. Consequently, on ANY coalgebra, ANY section, ANY pair `(a, b)` with `Or = ULift Bool`:

- If `a ≠ b`: `forcedHolds` is uninhabited (the faithful compass `⟨indicator of a, id⟩` fails the equation) and `forcedFails` is uninhabited (the faithful compass `⟨const, id⟩` satisfies it); the bare `underdet` pair is constructible, and the non-degenerate pair on any carrier with a state outside `{a, b}`.
- If `a = b`: every faithful compass coheres, by `rfl`.

So the trichotomy's value is a function of pair-equality, by a six-line argument that uses nothing of Series 12: not the tower, not `ConstituentOf`, not atomlessness, not Series 07, not `destW`. This is program-review-1 S1's argument verbatim with the free field renamed: there the tautology ran through unconstrained `raise`; the closure pinned `raise` and the tautology now runs through unconstrained `orient`, the exact component that `Converges`-under-`Faithful` exclusively tests. The strip test of design-review-1 Finding 2 fails a third time on the definitions the verdict consumes: strip `dest`/`reify` from `Faithful` and from `Converges`-under-`Faithful` and nothing changes (both are `dest`/`reify`-phantom).

**Why this meets the bar (the program's own bars).**

1. *The verdict still rests on a settled outcome.* The verdict is computed from the fork at `(aW, bW)`, a distinct-relata pair; by the above, `underdet` was the only reachable arm there, settled the moment `Faithful` was chosen, before any Lean. `ws5_verdict_reaches_both` is true, but the second value is reached at `(aW, aW)`, which is NOT a constituency edge: `constituentOf_aW` itself proves the only edge out of `aW` goes to `bW`, and the witness tower has no self-membered reified pattern, so no reflexive edge exists anywhere on it. `convergenceDecided` is constructible only off the domain where the charter's convergence concept ("a whole it constitutes") lives, and it is constructed there by `rfl`: protocol §5's "holds trivially" sin sign, here doing the load-bearing falsifiability work. The charter-status claim "the fork genuinely open" is true of the fork TYPE across loci and false of the fork at any genuine part-and-whole edge, which is where the verdict is computed.

2. *The prose claims are falsified by the code, again.* README: "undecidability here is an earned, falsifiable finding, not a tautology of an unconstrained field" (the unconstrained field is now `orient`); "it can close to `convergence-decided`" (not at any distinct-relata edge, provably). `summary.md`: "at a reflexive locus, where the part is the whole" (there is no part-whole edge at that locus; the phrase supplies a constituency the code refutes). And the sentence program-review-1 §3 falsified stands unedited in FOUR artifacts (README, charter §2-WS4, `summary.md`, `spec/ws4-design.md`): "the not-seeing is structural, the diagonal's work." No WS4 proof uses `ws1_no_self_total_hold`, `residue`, `ws1_atomless_bisim`, or any Series 07 or diagonal fact. Unchanged from program-review-1.

3. *The recurrence standard was pre-stated.* Program-review-1's S1 recurrence facet: "the recurrence check exists precisely for the case where a closure satisfies the fix's letter while the defect's consequence decides the verdict." The closure satisfied the letter (the named targets, built and named, executor blameless); the consequence (exactly one reachable verdict over the domain of genuine edges) persists.

**What genuinely improved (full credit).** The independence is now from a non-empty theory; the model pair shares its raising by `rfl` inside `ws4_wall_is_structural`; `ConstituentOf` sites the underdetermination statement; `NonDegenerate` is carried; and `ws4_fork_open` is itself a ∀-faithful-compass statement, so the parametricity discipline held even in the fix.

**Correction owed (the binary, no third theorem).** The build branch is unusually attractive here, because the R1 closure already built its main tool:

- **Build the specified target — the independence the prose has claimed since the charter, with Series 07 load-bearing in WS4 for the first time.** Two named theorems, both a handful of lines over existing machinery:
  - `ws4_dissent_is_import (hinf) : ∀ c : Compass (destW hinf) (reifyW hinf) (ULift.{u} Bool), Faithful c → ¬ Converges (destW hinf) (reifyW hinf) c aW bW → ¬ Recoverable (orientLift (destW hinf) c.orient)`. Proof: `faithful_converges_iff` turns dissent into `c.orient aW ≠ c.orient bW`; `aW`, `bW` are SHNE and plain-bisimilar; `orientLift_not_recoverable` closes it. Universally quantified over compasses (the parametricity discipline), and it says: EVERY faithful compass that fails convergence at the genuine edge does so by a genuine import. The not-seeing becomes literally Series 07's work, as a proof term, and the falsified prose sentence becomes true.
  - `BisimInvariant c := ∀ x y, (∃ R, IsBisim (destW hinf) R ∧ R x y) → c.orient x = c.orient y`, with `ws4_decided_within_sight : ∀ c, Faithful c → BisimInvariant c → Converges (destW hinf) (reifyW hinf) c aW bW` (via `ws1_atomless_bisim`). Within what the structure can see, convergence at the edge is DECIDED, in the holding direction, per charter §2-WS4 alternative (a) at the sub-class level, a pre-registered honorable outcome. The fork at the SAME edge is then closed within sight and reopened exactly across the import boundary: a two-zone fork whose boundary is structurally defined, making shape-drawn falsifiable AT THE EDGE (it is false within the bisim-invariant class) rather than at a reflexive non-edge. Recompute the verdict and rewrite the WS4 prose around the two-zone fork; `ws4_fork_open` may remain as a remark, no longer load-bearing. **OR**
- **Report the pre-registered outcome:** over `Faithful` the trichotomy at any locus is decided by pair-reflexivity; the edge underdetermination is definitional exogeneity (an honest and still meaningful reading: the compass was PLACED outside the structure, so the structure's silence about it is by construction, not discovery); the WS4 leg lands Partial per charter §5, "earned," "falsifiable," and "the diagonal's work" are struck from README, charter, both summaries, and `spec/ws4-design.md`, and charter-status records that the wall is where the design put it, not where a proof found it.

Either branch is honorable; the current middle (a tautology of a different unconstrained field, reported as an earned independence, with falsifiability carried by a trivial arm at a non-edge) is the same §0.2 sin at one remove.

---

## REAL findings

### PR2-R1 (REAL) — The charter was edited to record progress. Owner: charter.md.

Protocol §1: the charter is "edited only to fix an error in the design itself, never to record progress." The closure's charter edit contains a legitimate design-error repair (the compass class must carry a constraint, exactly PR1-S1's diagnosis) but also names the built artifacts (`Faithful`, `ws4_fork_open`) inside §2-WS4 and retains the unearned "the diagonal's work" sentence. The charter is the fixed bar the recurrence guard measures against; naming a closure's theorems in the bar makes the next recurrence check circular. **Correction owed:** restate §2-WS4 as a requirement (a genuinely constraining class; a fork open at the locus where the verdict is computed) without build names, moving the names to charter-status where progress lives; the "diagonal's work" sentence resolves with PR2-S1's branch.

### PR2-R2 (REAL, conditional on PR2-S1) — The ledger again asserts uncertainty that did not exist. Owner: charter-status.md.

The snapshot's "the genuinely uncertain obligation is WS4... discharged by the `Faithful`-class model pair plus `ws4_fork_open`" recasts the pre-registration around the new class as if its outcome had been open; under PR2-S1 the faithful-class outcome at the genuine edge was again settled a priori. The rows claiming "the fork genuinely open" and "SHAPE-DRAWN is earned and falsifiable" carry the same defect. **Correction owed:** a sweep after PR2-S1 closes, in whichever branch it closes; this is PR1-R4's shape and the same instrument-integrity rationale.

---

## COSMETIC / ACCEPTABLE (fix opportunistically; none blocks)

- **PR2-C1.** `ws6_provable_core`'s docstring attributes "`¬ Recoverable`" to the `ws3_compass_exogenous` conjunct (orientation-freedom). Now that `ws3_compass_exogenous_import` exists, either add it as the conjunct or re-gloss. One line.
- **PR2-C2 (carryover PR1-C4).** `ws5_name_neutral`'s "The silence is the theorem" versus `ws7_names_not_terms`'s honest phantom-parameter note; still unaligned.
- **PR2-C3 (carryover PR1-C1).** `ws4_underdetermined_up`'s positive side still exercises `ConvergesUp` at chain length 1 only; the depth-2 instance over `aW → bW → cW` remains cheap and unowed.
- **PR2-C4 (carryover PR1-C2).** `ws2_many_general`'s `hstep` hypothesis remains used only at `w₀`.
- **PR2-C5.** The S2 closure built the sanctioned singleton reader; the tower-generalized form program-review-1 called "the honest full target" (any attention whose reads meet the base makes `cW` real) is a two-line strengthening of `ws2_attention_makes_real`. Nothing is owed; worth taking.

---

## What survives cleanly

Five of the six program-review-1 closures are genuine and verified: the reader theorem is real and its reader is load-bearing in the statement (the Series 11 inheritance finally closes on both prongs, the arc's longest-running SERIOUS); the exogeneity bridge is the best new mathematics in the commit, a correctly generalized `rankLift` mechanism whose general form (`orientLift_not_recoverable`) is precisely the tool the PR2-S1 build branch needs; the anti-conflation witness is sound; the R3 demotion is honest at every site; the ledger sweep was performed where its targets existed. The mechanical discipline held throughout: sorry-free by grep, no custom axioms declared, names-not-terms clean, no cross-series imports, AxiomCheck extended to every new headline, and no ∀-compass obligation anywhere discharged by a distinguished compass, including inside the fix. The transcription layer and the WS2 plurality complex stand as verified in program-review-1; nothing in the closure commit disturbed a previously traced proof.

## Honest bottom line

This review found no false theorem; every new proof traced reads sound as written. What recurs is the same defect at one remove: the fit between the convergence theorems and the headline claims. Pinning the raising moved the freedom into the orientation, which is the only thing faithful convergence tests, so the fork at any genuine part-and-whole edge was still never open, and the new open-ness lives at a reflexive locus that is not an edge, where the relation holds by `rfl`. The knowing side is now genuinely reader-wise and deserves to be called done. The convergence side closes on the §0.2a binary, and for once the build branch is both cheap and better than the original claim: `ws4_dissent_is_import` plus `ws4_decided_within_sight` would replace "the structure cannot see which" as an assertion with the theorem that dissent from convergence IS an import and coherence is forced within the structure's sight — the two-zone fork, open exactly across the boundary Series 07 characterizes. Until one branch closes, the honest statuses are: knowing plural, label-wise AND reader-wise (done); convergence underdetermined by exogeneity, not yet by independence; the verdict not yet earned as computed. The edge may still be drawable exactly, and the ruler for drawing it is now, for the first time, actually in the toolbox.
