# Relational Existentialism, Program Review 3 (adversarial; the recurrence check on the program-review-2 closures, run first per protocol §0.2a)

**Scope.** The closure commit `fcf787d` ("Address program-review-2 findings") read against `spec/program-review-2.md`'s correction-owed binaries, the charter's success criteria, and protocol §0.2a. Files traced line by line: `ws4.lean` in full (the PR2-S1 closure: `BisimInvariant`, `ws4_dissent_is_import`, `ws4_decided_within_sight`, `ws4_two_zone`, the demoted `ws4_fork_open`), `ws3.lean` (`orientLift`, `orientLift_not_recoverable`, re-traced as the closure's engine), `ws5.lean` (the class-parameterized `ConvergenceFork`, `s12_fork`, `s12_fork_insight`, `ws5_verdict_reaches_both`), `ws7.lean` (the audit, `ws7_fork_can_close`, the verdict theorems), `ws6.lean` (the PR2-C1 conjunct), `ws1.lean`/`ws2.lean` (re-read for context; unchanged by the commit), `AxiomCheck.lean`, and the swept prose (README, `charter-status.md`, both summaries, `spec/ws4-design.md`, `charter.md` as reverted).

**Verification note (disclosed).** As in program-reviews 1 and 2, this environment cannot fetch the Mathlib build cache, so the build was not independently recompiled. What was run: sorry/custom-axiom greps (clean), names-not-terms grep (clean), cross-series import check (`scripts/gate.sh` logic re-read and executed: OK for both series), AxiomCheck-coverage diff against the committed axiom log (a finding: PR3-R3), and line-by-line proof tracing of every theorem cited below. No finding depends on a compilation question.

**Grading.** As in program-reviews 1 and 2. For a RECURRING finding the correction owed is the binary of protocol §0.2a and nothing else.

---

## The recurrence check on the program-review-2 closures (the review's first duty)

- **PR2-S1 — FIXED, verified.** The build branch was taken and the named targets were built as named, verbatim:
  - `ws4_dissent_is_import` is exactly the specified signature (∀-faithful-compass, dissent at `(aW, bW)` ⇒ `¬ Recoverable (orientLift (destW hinf) c.orient)`), proved exactly by the specified route: `faithful_converges_iff` turns dissent into `c.orient aW ≠ c.orient bW`; `ws_SHNE` supplies both atomlessness certificates; `orientLift_not_recoverable` (the PR1-R1 bridge, re-traced sound: the forward clause of any lifted bisimulation forces `orient aW = orient bW` against the derived inequality) closes it. Series 07's work is load-bearing in WS4 for the first time, as a proof term. The parametricity discipline held inside the fix: the theorem is universally quantified; no distinguished compass discharges it.
  - `BisimInvariant` + `ws4_decided_within_sight` are the specified within-sight arm: bisim-invariance plus `ws1_atomless_bisim (destW hinf) aW bW` forces orientation-agreement at the edge, and the faithful raising converts that into `Converges`. Sound as written.
  - `ConvergenceFork` was generalized to the class parameter; `s12_fork` (full faithful, `underdet`, from the non-degenerate `cHoldF`/`cFailF` pair) and `s12_fork_insight` (`Faithful ∧ BisimInvariant`, `forcedHolds` via `ws4_decided_within_sight`) both sit at `(aW, bW)`; `ws5_verdict_reaches_both` and `ws7_fork_can_close` now witness both verdict values at the same genuine edge, and `ws4_fork_open` is demoted to a remark whose own docstring disclaims load-bearing status. This is the fork open where the verdict is computed, as the correction demanded.
  - The consequence test that drove two recurrences no longer bites: the verdict's second value is no longer reached at a reflexive non-edge or by an impossible antecedent, and the full-class freedom of `orient` — which is still, as a bare meta-fact, derivable on any structure — is now itself theorem-ized as exactly Series 07's import at this locus (`ws4_dissent_is_import` uses the SHNE-ness and plain-bisimilarity of the pair; it is not generic). The defect's consequence does not decide the terminal verdict any more; the boundary of the fork is characterized by a proof term. After three passes, the design-review-1 Finding 2 lineage closes.
- **PR2-R1 — FIXED (by the revert path).** The charter stands verbatim (commit `42690b5`); no build names appear in the bar, so the recurrence guard is no longer circular. The charter's §2-WS4 "the diagonal's work" sentence — falsified as a description of the mechanized wall since program-review-1 — remains in the immutable charter but is now quarantined as Charter-discrepancy item 3 in the ledger, with the honest built reading (Series 07's import boundary) recorded beside it. Acceptable: the bar is fixed, the drift is logged where progress lives.
- **PR2-R2 — FIXED, verified.** The snapshot rows ("the wall," "the terminal open"), the WS4 workstream row, the result tracker, and the closure log are rewritten around the two-zone fork at the edge; the PR1-S1 closure entry is explicitly marked SUPERSEDED with the reason. The ledger no longer asserts uncertainty that did not exist. One residual class of stale prose survives *outside* the ledger: PR3-R2.
- **PR2-C1 — FIXED.** `ws6_provable_core` now carries `ws3_compass_exogenous_import` as its own conjunct, and its docstring distinguishes orientation-freedom from the import proof term.
- **PR2-C2 through C5 — carryover, unfixed, acceptable.** The `ws5_name_neutral` docstring ("The silence is the theorem") remains unaligned with `ws7_names_not_terms`'s honest phantom-parameter note; `ws4_underdetermined_up`'s positive side still exercises chain length 1 only; `ws2_many_general`'s `hstep` remains used only at `w₀`; the tower-generalized reader remains untaken. None was owed.

---

## SERIOUS findings

**None.** Checked against every SERIOUS criterion: no canonical compass discharges a parametric obligation (the fix's own ∀-compass theorems included); convergence is not decided by definition (the underdet arm carries a non-degenerate faithful pair; the decided arm is proved, not defined, and its proof consumes the collapse engine); the coincidence remains shape-only; the inhabitation remains the general tower-distinction; no name is a proof term; and no prior SERIOUS finding recurs — the §0.2a check above verified the PR2-S1 closure built the specified targets and that the defect's consequence (a verdict settled a priori, falsifiability carried off-locus) is genuinely discharged. This is the first program-review pass with zero SERIOUS findings.

---

## REAL findings

### PR3-R1 (REAL) — The in-sight sub-class is uncertified as inhabited, and on the witness carrier every one of its members is orientation-constant — the series' own `NonDegenerate.orient_nonconst` degeneracy — undisclosed. Owner: WS4/WS5, with the prose as co-owner.

The mechanics. On the witness carrier every state is `SHNE` (`ws_SHNE`), so `hneRel` is a bisimulation relating *every* pair; hence `BisimInvariant c` forces `c.orient` constant on all of `WCar`. Three consequences:

1. Every compass in the in-sight faithful class fails `NonDegenerate.orient_nonconst`. The `forcedHolds` arm that carries the falsifiability of `shapeDrawn` is therefore inhabited only by collapsed-constant orientations — the exact degeneracy sign protocol §5 names for the model pair, here appearing on the decided side of the fork, and disclosed nowhere (not in the `BisimInvariant`/`ws4_decided_within_sight` docstrings, not in the ledger, not in the summaries).
2. Nothing in the build certifies the class is inhabited at all. `ConvergenceFork.forcedHolds` carries a universally quantified proof and no witness, so `s12_fork_insight` and `convergenceDecided` would be constructible even over an empty class; "genuinely reachable" (README, summary) is carried by a vacuity-capable arm. (The class *is* inhabited — the constant compass — but by inspection, not by theorem.)
3. The edge is not load-bearing in the decided arm: since sight-bound orientations are globally constant here, the same proof closes the fork at every pair, edge or not. The underdet arm is edge-sited (`ConstituentOf` is a conjunct of `ws4_underdetermined`); the decided arm merely happens at the edge.

Why this is REAL and not SERIOUS. The terminal verdict does not rest on it: `shapeDrawn` is computed from the underdet arm, whose model pair is genuinely non-degenerate. And the constancy is arguably the theorem's deepest content rather than its defect — within the structure's sight an atomless world is the One, so the only orientations it can see are uniform, which is Series 07 replayed at the orientation level. But an audit whose falsifiability arm could be vacuous as far as the build certifies, over a class every member of which is degenerate by the program's own standard, is a gap the program's own instruments are built to catch.

**Correction owed (and it strengthens the result):** two short theorems plus a disclosure sweep. `ws4_insight_inhabited : ∃ c, Faithful c ∧ BisimInvariant (destW hinf) c` (the constant compass; honest, since it witnesses an existential about the class, never discharges a ∀-obligation). And `ws4_sight_is_uniform : ∀ c, BisimInvariant (destW hinf) c → ∀ x y : WCar, c.orient x = c.orient y` (two lines via `ws_SHNE`/`hneRel`), which converts the undisclosed degeneracy into the honest and stronger statement: on an every-moment-atomless carrier, what the structure can see admits only the uniform orientation, so the decided zone is decided *because* sight collapses to the One — the same fact, now a disclosure instead of a discovery waiting for a reviewer. Then note in the `ws4_decided_within_sight`/`s12_fork_insight` docstrings and the summaries that the in-sight class's members are orientation-constant on this carrier and that the decidedness holds at every pair.

### PR3-R2 (REAL) — Sweep residuals narrating the superseded mechanisms as load-bearing. Owner: `ws4.lean` (module header), `spec/ws4-design.md`.

Two artifacts still describe walls that the current build refutes:

1. **The `ws4.lean` module header** still narrates the PR1-era mechanism as the falsifiability carrier: "at a reflexive locus `(aW, aW)` EVERY faithful compass coheres, so the forced arm is inhabitable (`ws4_fork_open`) and `convergenceDecided` is constructible. The verdict therefore has more than one reachable value (WS5 `ws5_verdict_reaches_both`)…" — citing for the reflexive-locus route the very theorem that is now the two-zone at-the-edge statement, and contradicting `ws4_fork_open`'s own docstring twenty lines below ("does NOT carry the falsifiability of the verdict"). The file disagrees with itself about which theorem earns the verdict.
2. **`spec/ws4-design.md`'s winner section** still presents the pre-`Faithful` `ws4_wall_is_structural` (shared *orientation*, difference isolated to the *raising*, `cHold`/`cFail`) as the built structural wall, with the commentary "the two compasses share the same `orient` … and differ only in the raising" — the built theorem proves the exact inverse (shared identity *raising*, `cHoldF`/`cFailF` differing only in the *orientation*). The same passage retains "the not-seeing is … the diagonal's freeness (`ws2_residue_free`)," the attribution both prior program reviews falsified (no WS4 proof uses `ws2_residue_free`); the PR2 sweep edited only the §-head parenthetical at line 9 of this file. Designs are stable once committed, but this file already carries build-realization annotations; the winner section needs the matching one.

**Correction owed:** rewrite the `ws4.lean` header around the two-zone fork (the falsifiability at the edge, `ws4_fork_open` a remark), and add a realization note to the ws4-design winner section recording the `Faithful`-class inversion and striking or re-attributing the `ws2_residue_free` sentence. Prose only; no theorem is touched.

### PR3-R3 (REAL) — The committed axiom log is two closure commits stale: none of the eight review-closure headlines is logged, while the ledger asserts a verified clean rebuild. Owner: `spec/axiom-check-log.md`.

`AxiomCheck.lean` prints 40 headlines; `spec/axiom-check-log.md` records 32 and predates both closure commits. Missing: `ws1_coincidence_not_identity_witness`, `ws2_attention_makes_real`, `ws3_compass_exogenous_import` (the PR1 closures) and `ws4_dissent_is_import`, `ws4_decided_within_sight`, `ws4_two_zone`, `ws5_verdict_reaches_both`, `ws7_fork_can_close` (the PR2 closures). The ledger's closure log states the PR2 fixes were "verified by a full sorry-free axiom-clean rebuild (standard three only)," but the one committed instrument that would evidence this for a reviewer who cannot recompile — the log every program review has taken on documented trust — was never regenerated. The trust chain has a two-commit gap at exactly the theorems the reviews most need covered. **Correction owed:** regenerate the log from `lake build Series12.AxiomCheck` and note the capture commit in its header.

### PR3-R4 (REAL) — Both summaries' review-history sentences omit the program reviews. Owner: `summary.md`, `summary-technical.md`.

`summary.md`: "A blind adversarial review of the finished build returned zero serious findings." `summary-technical.md` reports series-review-1's zero-SERIOUS result and stops. Both true of series-review-1 and both materially incomplete: two adversarial program reviews each found a SERIOUS defect in the convergence leg of this build (since closed), and series-review-1 itself disclosed it was not blind in the protocol's strict sense (program-review-1 C5). The root README states the full history honestly — zero-SERIOUS blind review *and* two program reviews that "pressed harder … and pushed the convergence wall" — so the fix is one sentence per summary, mirroring the README. A terminal series whose deepest discipline is not letting prose outrun the artifacts should not let its plain-language summary imply the build's only adversarial vetting came back clean. **Correction owed:** the README's sentence, in both summaries.

---

## COSMETIC / ACCEPTABLE (fix opportunistically; none blocks)

- **PR3-C1 (carryover PR1-C4/PR2-C2).** `ws5_name_neutral`'s "The silence is the theorem" versus `ws7_names_not_terms`'s honest phantom-parameter note; still unaligned.
- **PR3-C2 (carryover PR2-C3).** `ws4_underdetermined_up`'s positive side still exercises `ConvergesUp` at chain length 1; the depth-2 instance over `aW → bW → cW` remains cheap.
- **PR3-C3 (carryover PR2-C4).** `ws2_many_general`'s `hstep` hypothesis remains used only at `w₀`.
- **PR3-C4 (carryover PR2-C5).** The tower-generalized reader (any attention whose reads meet the base makes `cW` real) remains a two-line unowed strengthening of `ws2_attention_makes_real`.
- **PR3-C5.** The converse bridge would earn the boundary wording in full: on the witness carrier, `BisimInvariant c → Recoverable (orientLift (destW hinf) c.orient)` is provable (a constant label lifts any plain bisimulation), which together with `ws4_dissent_is_import`'s contrapositive would make "the two zones separated by Series 07's import boundary" a biconditional rather than two one-directional inclusions. The current prose is accurate as one-directional; the biconditional is worth having, not owed. (Note for the record: assent does not imply in-sight — `cHoldF` converges at the edge yet its orientation is itself a genuine import; no artifact claims otherwise, and none should start.)
- **PR3-C6.** `ConvergenceFork` takes a bare pair `(a, b)`; the edge-ness of the verdict's locus is carried by `ws3_edge_aW_bW` beside it rather than by the type. Carrying a `ConstituentOf` proof in the fork would let "at the genuine edge" be read off the type. Optional.

---

## What survives cleanly

The PR2-S1 closure is the program's best late-arc work: `ws4_dissent_is_import` and `ws4_decided_within_sight` were traced line by line and are sound, both are structure-specific (each consumes the SHNE certificates and the collapse engine at the pair, so neither is the generic six-line freedom argument that felled two prior closures), the parametricity discipline held inside the fix, and the class-parameterized fork puts both verdict values at the locus where the charter's convergence concept lives. The generic derivability of full-class underdetermination — the fact behind PR1-S1 and PR2-S1 — is no longer a hidden defect but the phenomenon under a theorem: the freedom of the orientation *is* the import, proved. The PR1-era verified layer stands undisturbed: the transcription layer, the WS2 plurality complex with its reader, the exogeneity bridge, the anti-conflation witness, the quantifier-honest coincidence. Mechanically: sorry-free by grep, no custom axioms declared, names-not-terms clean, no cross-series imports (gate run: OK for both series), AxiomCheck.lean covers every new headline, and no ∀-compass obligation anywhere is discharged by a distinguished compass. The ledger is, for the first time in three passes, ahead of rather than behind the build — its residual defects (R2, R3) are in the satellite artifacts, not the instrument itself.

## Honest bottom line

This review found no false theorem, no SERIOUS finding, and no recurrence: the design-review-1 Finding 2 lineage — the strip-test failure that recurred through two closures and decided two terminal verdicts prematurely — closes at count three, on the build branch, with the two theorems program-review-2 specified built verbatim and sound. What remains is hygiene at the edges of the fix: the decided zone should certify its own inhabitants and disclose that on this carrier sight admits only the uniform orientation (a disclosure that strengthens the philosophical reading rather than weakening it), two artifacts still narrate superseded walls, the axiom log has not caught up with the theorems it exists to vouch for, and the summaries under-report the adversarial history the README reports honestly. All four close in prose plus perhaps six lines of Lean, none touches a payoff, and none moves the verdict. As of this pass the honest statuses are: knowing plural, label-wise and reader-wise; convergence underdetermined over the faithful class with the fork two-zone at the genuine edge, dissent a proven import, decidedness within sight proven — with the in-sight class's uniformity owed as a disclosure; the terminal verdict SHAPE-DRAWN, computed, and for the first time earned where it is computed. The edge appears to be drawn where the ruler says it is. What is owed now is not a different drawing but the housekeeping that lets a stranger check the ruler.
