# Relational Existentialism, Series 13 (coda): Charter Status

**The mutable companion to `charter.md`. All progress, revisions, per-workstream status, discharges, and open items live here; the charter stays clean.**

*Series 13 is a POST-TERMINAL CODA. Series 12 remains the program's genuine terminal series and this ledger does not reopen it. The question: the FIT. Series 12 WS1 proved the differentiator Series 07 requires (`ws2_import_theorem`, `ws3_atomless_distinct_is_import`, `ws1_atomless_bisim`) and the residue the diagonal generates (`ws1_no_self_total_hold`, `ws2_residue_free`, `ws1_diagonal_not_bisim`) both inhabit `Opening realizable c := ¬ realizable c` from opposite quantifier directions (`ws1_shape_coincidence`), and proved the coincidence is SHAPE-identity, not object-identity, non-vacuously (`ws1_coincidence_not_identity`, `ws1_coincidence_not_identity_witness`). Series 13 asks the question that leaves posed: if the two are not the same thing, HOW do they face each other? The conjecture is a DUALITY, order-theoretic: a MINT carrying every inspection to a non-recoverable labelled coalgebra (the diagonal as an import factory, forced-for-all funding exists-satisfying), a best-approximation adjoint, and a Galois connection whose round-trip defects are theorems. The conjecture's expected FAILURE is its deepest payoff: the mint is NOT essentially surjective, and the imports outside its image, those with no diagonal genealogy, are Series 07's given/chosen fork in order-theoretic clothes. The coda fills nothing the terminus left open and adds no name. Phases A-C are complete: the charter/protocol/ledger, the five per-workstream designs plus the WS1-orders sub-artifact, and the full Lean build (`formal/Series13/`). The build is sorry-free and axiom-clean. The COMPUTED verdict is **DUAL at the flat layer** on carriers of interest (`ws5_verdict_eq`), TOTAL on the degenerate single-hold carrier (`ws5_verdict_degenerate`); the mint's non-surjectivity up to `≈` (`ws4_mint_not_surjective`) is witnessed by `outWit`, off the diagonal link, the exclusion structural and `≈`-surviving. Series 13's carrier is FLAT (the reification tower is deliberately excluded); the layer-stability question is a named open. Next: Phase D (blind series-review).*

---

## How to read this file

- **Status vocabulary** (charter §5): **Dual** (the expected terminus: WS1 orders non-vacuous, WS2 transport proved with exogeneity, WS3 connection with a genuine non-identity round trip, WS4 defect witnessed non-degenerately) · **Total** (the mint essentially surjective, every import diagonal-born, reported honestly in that direction, a stronger and stranger finding that presses the fork toward the generated, not a failure) · **Disconnected** (no non-vacuous pair of orders admits a connection; the WS1 obstruction reported precisely) · **Partial** (any obligation lands only per-instance or degenerate) · **Refuted** · **Circular** (WS7-only) · **Not started**.
- **Series 12 is the terminus; this is a coda.** Nothing here decides the compass's content, convergence's direction, or the differentiating act. The duality sharpens the terminus's own sentence; it does not extend the program past its close. If this series lands DUAL, the classification of the out-of-image imports (which are given, which chosen) is itself a new permanent open, open by the same theorem that locates them.
- **The fork stays open.** The defect theorem LOCATES imports outside the mint's image; it must never CLASSIFY them. Deciding an out-of-image import to be given rather than chosen fills the opening Series 07 proved cannot be filled and Series 12 closed by leaving open, charter §4.a. The words *given* and *chosen* live in prose only, never terms.
- **No connection by fiat.** Both WS1 orders must be proved non-trivial; the connection's content is certified by a proved non-identity round trip. A connection over a vacuous order, or one collapsing to an isomorphism, is the coincidence restated, not the fit proved, charter §4.b.
- **Exogeneity is proved, not assumed.** The mint consumes inspective data above the plain layer; that its operation is non-recoverable from the plain relating is a named obligation, `ws2_mint_exogenous`, charter §4.c. A mint the relating could perform on itself would contradict Series 07.
- **The defect is structural, not artifactual.** The WS4 out-of-image witness must be excluded by its own label, not by cardinality, universe level, or typing accident, charter §4.d.
- **Names are not terms.** Duality, fit, wound, rescue, given, chosen, and the inherited consciousness/choice/God/compass live in prose and docstrings only; a name discharging an obligation breaches the wall from the prose side, inherited from Series 12 §4.e.

## Program-level snapshot

| Item | Status |
|---|---|
| **Headline target** | The fit: upgrade Series 12's shape-coincidence to a structured correspondence. Orders on inspections and on labelled coalgebras (WS1); the mint and its transport-into-the-opening with exogeneity (WS2); the Galois connection with a genuine non-identity round trip (WS3); the defect, an import outside the mint's image up to `≈`, structurally (WS4); the verdict computed (WS5). **BUILT (Phase C), sorry-free and axiom-clean; computed verdict DUAL at the flat layer on carriers of interest.** Expected shape, pre-registered (not a target): WS2 is near-certain (the diagonal does the transport work, the total residue funding existential imports); WS3 is standard once WS1 lands (Mathlib `GaloisConnection` carries the machinery); the genuinely uncertain obligation is **WS1** (whether either order admits a non-vacuous definition compatible with a monotone mint), with DISCONNECTED its pre-registered honest outcome; WS4 is genuinely uncertain in DIRECTION, and both DUAL (defect witnessed) and TOTAL (mint surjective) are first-class findings. |
| **The two legs** | LEG ONE (residue → import, the transport): every within-generated opening manufactures a without-required one; near-certain, the diagonal is total and so a factory for imports. LEG TWO (import → residue, the best-approximation adjoint `readInsp`): the mint is NOT essentially surjective up to `≈` on carriers of interest (`ws4_mint_not_surjective`), that non-surjectivity the WS4 payoff, the fork in order-theoretic clothes. **BUILT.** |
| **The knot** | WS1, the two orders. The whole series rests on a non-vacuous preorder on inspections and a non-vacuous preorder on labelled coalgebras admitting a monotone mint and a non-trivial connection. **BUILT: both orders proved non-trivial** (`ws1_orders_insp_nontrivial`, `ws1_orders_lab_nontrivial`); the variance wall was dissolved (residue order on inspections, reference-antitone order on labelled coalgebras), so DISCONNECTED was not triggered. |
| **The defect (the fork)** | WS4: an import outside the mint's image, structural not artifactual. If witnessed, `ws4_mint_not_surjective` is the series' deepest theorem, and the out-of-image remainder is where the charter's prose places the given. If instead every import is mintable, TOTAL, reported in full. **BUILT (up to `≈`): DUAL at the flat layer on `|Hold| ≥ 2`** (`outWit` off the diagonal link, `≈` no mint, exclusion structural via `ws4_exclusion_structural`); TOTAL at the flat layer on the degenerate single-hold carrier. |
| **The carrier** | To be transcribed unchanged from Series 12 / Series 07: `Opening`, `Recoverable`, `ResidueRecoverable`, `plainOf`, the diagonal apparatus (`residue`, `diag`, `ws2_residue_free`, `Hold`, `afford`), the labelled lift (`LkObj`, `IsBisimL`), `labelLoop`, and the collapse engine (`ws1_atomless_bisim`, `ws2_import_theorem`). Series 13's OWN definitions over this carrier (BUILT): the two orders (`instLEInsp`, `instLELab`), the mint (`mintL`), its adjoint (`readInsp`), and the connection (`ws3_galois`). The reification tower is DELIBERATELY excluded (single-layer scope, a named open). Series 13 adds NO new structural machinery: two orders, one map pair. |
| **Relation to the program** | A coda after the final cadence. Series 12 closed the arc on an exactly-drawn edge; Series 13 asks the terminus's own question once more, slower, and names the fit. It reopens nothing, fills nothing, and if it lands DUAL it adds one new open (the classification of the out-of-image imports), open as a theorem by the same result that locates them. |

## Phase status

| Phase | Name | Status |
|---|---|---|
| A | Charter | **Complete.** `charter.md`, `charter-status.md`, `protocol.md` written. |
| B | Design-all (five `spec/wsNN-design.md` + `spec/README.md`, plus the WS1-orders sub-artifact) | **Complete.** `spec/README.md` (design index, carrier + two orders + map pair locked), `spec/ws1-orders-design.md` (the knot, resolved on paper), `spec/ws1-design.md` … `spec/ws5-design.md` written as a batch. The knot resolves: the residue order on inspections (A-C2) and the residue-covariant/reference-antitone order on labelled coalgebras (B-C3) dissolve the variance wall, admit the monotone mint, and yield a genuine (non-iso) Galois connection; the DUAL terminus is the paper-decidable lead, with DISCONNECTED (the variance wall) pre-registered. |
| C | Build-all (`formal/Series13/wsNN.lean`, `Series13.lean`, `AxiomCheck.lean`) | **Complete.** All five workstreams built in `formal/Series13/ws1.lean … ws5.lean`, `Series13.lean`, `Series13.AxiomCheck.lean`; registered in `lake/lakefile.toml`. Sorry-free and axiom-clean (every headline on the standard three; `spec/axiom-check-log.md`). Three-series `lake build` (07/12/13) succeeds. Computed verdict: **DUAL at the flat layer** on carriers of interest (`ws5_verdict_eq`), TOTAL on the degenerate single-hold carrier (`ws5_verdict_degenerate`). |
| D | Blind series-review → `spec/series-review-N.md` | **Not started.** |
| E | Address | **Not started.** |
| F | Close | **Not started.** |

The canonical run is **B → C → D → E → D → E** (protocol §2), with more D→E loops added only if a review pass still returns SERIOUS findings. The anti-loop discipline (protocol §0.2a) is in force.

## Workstream status

*Series 13 has five workstreams (the coda is narrower than the terminal series' seven): WS1 the two orders, WS2 the mint and transport, WS3 the connection, WS4 the defect, WS5 the verdict-and-audit folded together. All are Not started; the summaries below fix the contract each build will be judged against.*

### WS1, The two orders · *blocking, the knot*
**Status: BUILT (Phase C).** `formal/Series13/ws1.lean`; designed in `spec/ws1-design.md` + `spec/ws1-orders-design.md`. The winning pair: the ORDER ON INSPECTIONS `instLEInsp` by residue (`insp₁ ≤ insp₂ := residue insp₁ ⊑c residue insp₂`, A-C2), and the ORDER ON LABELLED COALGEBRAS `instLELab` on the two-region mint carrier, residue-position covariant + reference-position antitone through its self-bit at `h₀` (B-C3). Both proved non-trivial (`ws1_orders_insp_nontrivial`, `ws1_orders_lab_nontrivial`) on any inhabited-`Hold` carrier. Rejected candidates recorded: pointwise-implication on inspections (A-C1, mint not monotone, the variance wall), reach order (A-C3, does not control the residue), raw successor-⊆ (B-C1, forces a discrete inspection order), residue-only labelled order (B-C2, iso in disguise), label-distinguishing strength (B-C4, monotone-mint not paper-decidable), discrete/indiscrete (fiat), mint-pullback (fiat). DISCONNECTED pre-registered with the variance wall as the precise obstruction. Blocking: WS2-WS4 build over the orders.

### WS2, The mint and the transport theorem · *near-certain*
**Status: BUILT (Phase C).** `formal/Series13/ws2.lean`; designed in `spec/ws2-design.md`. The mint `mintL insp = (residue insp, insp h₀)`: residue-position `⟨true⟩` broadcasts `residue insp`, reference-position `⟨false⟩` broadcasts `insp h₀`; `plainOf (mintL insp)` is `i ↦ {i}` for EVERY inspection (constant, the ground of exogeneity). Transport `ws2_mint_lands_in_opening` (T-C1): `∀ insp, ¬ Recoverable (mintL insp)`, engine `ws2_residue_distinct` at `h₀` (the residue- and reference-positions are label-separated because `residue insp h₀ = ¬ insp h₀ h₀ ≠ insp h₀ h₀`), mirroring `ws4_label_survives_quotient`. Exogeneity `ws2_mint_exogenous` (E-C1) + `ws2_mint_not_plain_function` (E-C2): the residue-distinct pair `(⊤i, ⊥i)` share a plain projection but receive distinct mints, so no plain-relating function reproduces the mint. Guards against the mint below the plain line (discipline 3).

### WS3, The connection · *the fit*
**Status: BUILT (Phase C).** `formal/Series13/ws3.lean`; designed in `spec/ws3-design.md`. The adjoint `readInsp b := realizeAsResidue (gb b)`, folding the reference-bit into the residue (`gb b := fun h => bT b h ∧ (h ≠ h₀ ∨ ¬ bF b h₀)`). `ws3_galois` (C1): `GaloisConnection mintL (readInsp · h₀)`, iff exact because `mintL insp ≤ b ↔ residue insp ⊑c gb b ↔ insp ≤ readInsp b`. Closure `ws3_roundtrip_closure` (C2): `readInsp ∘ mintL = id` (the fold inert on minted coalgebras, whose diagonal link holds). Interior `ws3_roundtrip_interior` (C3): `mintL ∘ readInsp ≤ id`. Non-identity `ws3_roundtrip_not_identity` (C4): strictly below at `bRefActive = (⊤, ⊤)`, where the active reference-bit makes `gb` drop `h₀`, so the connection is NOT an iso. Guards against connection-by-fiat / iso in disguise (discipline 2). Categorical upgrade (C6) a stretch, not owed.

### WS4, The defect · *the fork in order-theoretic clothes, GENUINELY uncertain in direction*
**Status: BUILT (Phase C), at resolution `≈` (pre-build correction 2).** `formal/Series13/ws4.lean`; designed in `spec/ws4-design.md`. Mintability is tested UP TO EQUIVALENCE `labEquiv` (order-equivalence in `instLELab`), not literal equality, because literal non-surjectivity is too fine and DUAL-by-construction (two label-bisimilar coalgebras do the same import work). `≈` sees `bT` fully and `bF` only at `h₀`; mintability up to `≈` reduces to the diagonal link `bT h₀ = ¬ bF h₀`. Computed direction: **DUAL on `|Hold| ≥ 2`** (`ws4_mint_not_surjective`, D-C1: `outWit = (⊤, ≠h₁)` is `¬ Recoverable` and off the link, so `≈` no mint; the exclusion survives `≈`, `ws4_exclusion_structural` D-C2, a proof obligation not an assumption); **TOTAL on `|Hold| = 1`** (`ws4_mint_essentially_surjective_degenerate`, D-C4). The TOTAL target on `|Hold| ≥ 2` was ATTEMPTED (D-C3) and REFUTED by `outWit`; that refutation IS the DUAL finding, computed not assumed. `outWit` is the TEST ARTICLE, not the answer. The theorem LOCATES membership up to `≈`, NEVER sorts (no `Origin`/`genealogy`; *given*/*chosen* prose only, discipline 1). `labelLoop` (D-C5) rejected as a typing artifact. TOTAL (substance monism) is a live outcome, not foreclosed.

### WS5, The verdict, and the audit folded in · *owns the verdict, COMPUTED*
**Status: BUILT (Phase C), verdict genuinely COMPUTED (pre-build correction 2).** `formal/Series13/ws5.lean`; designed in `spec/ws5-design.md`. `Series13Verdict` (Dual/Total/Disconnected/Partial/Refuted/Circular) and the data-level `FitFork` (constructors `defectStructural`/`mintSurjective`/`ordersTrivial`/`perInstance`); `verdictOfFit` branches on the fork. `ws5_fork` is discharged by CASING ON THE CARRIER (the hold count): `if ∃ h₁ ≠ h₀ then .defectStructural … else .mintSurjective …`. So `ws5_verdict_eq : ws5_verdict = Dual` holds on a second-hold carrier (the DUAL branch, the TOTAL target having been refuted) and `ws5_verdict_degenerate : ws5_verdict = Total` on a single-hold carrier — two theorems, NEITHER the lead, the verdict tracks the carrier. `Audit` (V-C1) has one field per WS1-WS4 payoff plus the `fork`. Falsifiability `ws5_verdict_not_*` (V-C2, on a second-hold carrier). The audit folded in (V-C3): genuine-connection, exogeneity, structural-defect (the `≈`-survival version) as theorems; fork-open (membership up to `≈`, never sorts) and names-not-terms by inspection. Strip test aggregated here.

## The result tracker

| Result | WS | Status | Non-circular? |
|---|---|---|---|
| The two orders, non-vacuous | WS1 | **BUILT (`instLEInsp`, `instLELab`; `ws1_orders_insp_nontrivial`, `ws1_orders_lab_nontrivial`).** | Both orders proved non-trivial (a related-unequal pair + an unrelated pair, so neither discrete nor indiscrete); variance wall dissolved (residue order + reference-antitone), DISCONNECTED not triggered. |
| The mint lands in the opening, exogenously | WS2 | **BUILT (`mintL`; `ws2_mint_lands_in_opening`, `ws2_transport_forall`, `ws2_mint_exogenous`, `ws2_mint_not_plain_function`).** | Transport via the diagonal `ws2_residue_distinct` (the `⊤`-plain-bisim is not a label-bisim, `residue insp ≠ insp h₀`); exogeneity a proof term (`plainOf ∘ mintL` constant, mints differ), the mint above the plain layer. |
| The Galois connection, with a genuine defect of round trip | WS3 | **BUILT (`readInsp`; `ws3_galois`, `ws3_roundtrip_closure`, `ws3_roundtrip_interior`, `ws3_roundtrip_not_identity`).** | `GaloisConnection` proved (fold-exact iff), closure = id / interior ≤ id exhibited, interior proved NON-identity at `bRefActive` (not an isomorphism). |
| The defect (non-surjectivity up to `≈`), or its honest absence | WS4 | **BUILT (`labEquiv`, `outWit`; `ws4_mint_not_surjective`, `ws4_exclusion_structural`, `ws4_mint_essentially_surjective_degenerate`).** | Tested UP TO `≈`: DUAL on `\|Hold\| ≥ 2` (a structural out-of-image import, the diagonal link surviving `≈`), TOTAL at the flat layer on `\|Hold\| = 1`. The theorem locates up to `≈`, never sorts. |
| The verdict, computed; the fork left open | WS5 | **BUILT (`Series13Verdict`, `FitFork`, `verdictOfFit`; `ws5_verdict`, `ws5_verdict_eq`, `ws5_verdict_degenerate`, the audit checks).** | Verdict a function of WS1-WS4 (branches on `FitFork` by the carrier's hold count), never hand-set; no out-of-image import classified; no name a term; layer-stability a named open. |

## Open obligations register

1. **The two orders (WS1)**, non-vacuous, admitting a monotone mint. **Open (the knot); DISCONNECTED pre-registered.**
2. **The mint's transport and exogeneity (WS2)**: every inspection's minted label non-recoverable; the mint above the plain layer. **Open; near-certain.**
3. **The connection (WS3)**: `GaloisConnection` with a proved non-identity round trip. **Open; standard once WS1 lands.**
4. **The defect (WS4)**: a structural out-of-image import, or TOTAL. **Open; uncertain in direction, both first-class.**
5. **The verdict, computed; the neutrality inherited (WS5)**. **Open.**
6. **The fork stays open (the central discipline)**, WS5 audit. The defect locates, never classifies; *given*/*chosen* in prose only. **Standing constraint.**
7. **The terminus is not reopened**, WS5/close. Series 12's permanent opens (the compass's content, convergence's direction, the act) are untouched; if DUAL lands, the classification of the out-of-image imports joins them, open by theorem. **Standing.**

**Recurring lesson carried across the program:** the sharpest result is a proved impossibility, and an honestly-reported Partial beats a smuggled success. For this coda the lesson specializes twice. First: non-recoverability is a CERTIFICATE, not a defect (Series 07's lesson, inherited), so the mint landing in `¬ Recoverable` is the transport succeeding, not failing. Second, and the one this series turns on: the DEFECT of the duality is the RESULT. A Galois connection that turned out to be an equivalence would prove the diagonal manufactures the whole rescue (TOTAL), collapsing the given into the chosen and contradicting the interpretive fork the whole program earned; the expected and wanted outcome is that the connection is genuinely adjoint and genuinely non-surjective, its round-trip defects and its out-of-image imports both theorems. The coda does not need the two sides to be the same; it needs to prove exactly how they differ, and to leave the last interval open.

## Charter discrepancies (recorded; `charter.md` is immutable once written)

*None yet. `charter.md` is fixed once written (protocol §1; treated as strictly immutable, mismatches recorded here). No design or build exists, so no discrepancy between the charter and built code can yet arise. Program reviews of the built series will populate this subsection if the charter names a theorem the build does not realize or under-specifies an obligation, exactly as Series 12's ledger recorded four such discrepancies against its immutable charter.*

## Closed log

*Phase A complete: `charter.md`, `charter-status.md`, `protocol.md` written.*

*Phase B complete: `spec/README.md` (design index), `spec/ws1-orders-design.md` (the knot), and `spec/ws1-design.md` … `spec/ws5-design.md` written as a batch. The design LOCKS the carrier transcription (from Series 12 WS1, the cleanest single source) and the three Series-13 additions: the ORDER ON INSPECTIONS `instLEInsp` (by residue), the ORDER ON LABELLED COALGEBRAS `instLELab` (residue-position covariant, reference-position antitone), and the MAP PAIR `mintL` / `readInsp`. The knot is resolved on paper: the residue is antitone under pointwise implication (the variance wall), so ordering inspections BY the residue makes it covariant and the mint monotone, while the labelled order sees the reference-position antitonically so the mint stays monotone AND the connection stays non-iso (the reference-fold in the adjoint makes the Galois iff exact, the closure the identity, and the interior strictly below at `bRefActive`). The defect `outWit` is out-of-image by the diagonal link (`residue insp h₀ = ¬ insp h₀ h₀`), structural not artifactual, and the theorem locates without sorting. The paper-decidable lead is DUAL; DISCONNECTED (the variance wall stands), TOTAL (`|Hold| = 1`, every import on the link), and PARTIAL (iso in disguise / artifactual defect) are pre-registered and first-class. Phase C complete: all five workstreams built (`formal/Series13/ws1.lean` … `ws5.lean`, `Series13.lean`, `Series13.AxiomCheck.lean`), registered in `lake/lakefile.toml`, sorry-free and axiom-clean (`spec/axiom-check-log.md`), the three-series `lake build` green. The four critical checks confirmed: the fork stays open (`ws4_mint_not_surjective` locates up to `≈`, never sorts), the connection is genuine (orders non-trivial, `ws3_roundtrip_not_identity`), the mint is exogenous (`ws2_mint_exogenous` a proof term), the defect is structural (`ws4_exclusion_structural`, the diagonal link surviving `≈`). Realization note: `Lab dest h₀` represents the mint-codomain coalgebras by their two self-loop labels (`h₀` carried in the type for the `Preorder` instance), realized as genuine labelled coalgebras by `coalg`; every `Recoverable` fact is a fact about `coalg b` — a faithful realization of the design's `MCar → LkObj` shape, disclosed here, not a retarget. Next action: Phase D (blind series-review → `spec/series-review-1.md`), then Phase E if it returns SERIOUS findings.*

*Pre-build corrections (2), applied before Phase C, intent "the series must find out, not confirm; TOTAL is a live outcome; the verdict is computed":*
1. *`spec/ws1-orders-design.md`: added the FIFTH fiat mode (B-C6), an order reverse-engineered from the mint's output to force monotonicity/non-iso, distinct from principled contravariance; plus the principled-contravariance defense stated checkably (the `⟨false⟩` position is antitone because the self-application enters the residue negatively, `residue insp h₀ = ¬ insp h₀ h₀`, a sign fixed by `diag` independently of the mint; a reviewer breaks it by exhibiting a covariant-reference order that still works).*
2. *`spec/ws4-design.md` (+ `ws5`, README): retargeted the defect at resolution `≈` (order-equivalence in `instLELab`), because literal non-surjectivity is too fine and DUAL-by-construction. Mintability up to `≈` reduces to the diagonal link; DUAL on `|Hold| ≥ 2` (some off-link import `≈` no mint, exclusion surviving `≈`), TOTAL on `|Hold| = 1` (every import on the link, `≈` a mint), both first-class, the verdict COMPUTED from the hold count. The TOTAL target on `|Hold| ≥ 2` is attempted and refuted; if it had held, TOTAL would be the finding. `outWit` is the test article, not the answer.*

*Addendum (single-layer scope, the tower bound), applied to `ws4`/`ws5`/README: the reification tower (`IsReify`, `reifyStep`, `towerN`, `prec`) is DELIBERATELY outside the transcription; the coda is a SINGLE-LAYER result and says so. The carrier is FLAT (every labelled coalgebra is a flat two-region self-loop). Consequences, all pre-registered: the whole fit is drawn at the flat layer; the LAYER-STABILITY question (does the mint commute with reification? does any tower-carrying import survive outside the mint's image up to `≈`?) is a NAMED OPEN (WS5, the close), not a silent omission; any TOTAL is "TOTAL AT THE FLAT LAYER" (essentially surjective over flat imports, the tower unexamined), a BOUNDED finding whose bound is the tower, not "every import diagonal-born" outright; any DUAL is DUAL at the flat layer. The open is prose/docstring only, never a proof term; naming it bounds the coda's claim and does not sort the fork.*

---

*No em dashes in final academic copy. The premises presupposed and named: Series 07 (the differentiator is an import) and Series 12 WS1 (shape-coincidence, non-identity). What is to be proved, not assumed: the orders' non-vacuity, the mint's transport and exogeneity, the connection and its defect, the non-surjectivity or its honest alternative. What is never proved, by design: which out-of-image imports are given and which chosen, and anything the terminal series left open. Series 12 remains the terminus; this is the fit, and the fit, not the filling, is the coda's whole ambition.*
