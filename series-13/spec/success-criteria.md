# Series 13 — Success Criteria (blind-review seed)

**Extracted verbatim from `charter.md` §6 (the success criteria) and the protocol's promoted first-class
checks, WITHOUT any motivating prose (`series-review-2.md` SR2-4). This file is the review's seed under
protocol §0.1/§5: a blind reviewer opens THIS file, not `charter.md`, so the metaphysical positioning
(`charter.md` §8) is never in view. It contains only the mathematical bar and the discipline checks.**

## The bar (charter §6, verbatim)

1. **The orders are non-vacuous (WS1)**, with rejected candidates recorded.
2. **The mint lands in the opening, exogenously (WS2)**: every inspection's minted label non-recoverable, the mint itself above the plain layer, both as named theorems.
3. **The connection is genuine (WS3)**: `GaloisConnection` proved, closure and interior exhibited, at least one round trip proved non-identity.
4. **The defect is witnessed or its absence reported (WS4)**: a structural out-of-image import, or TOTAL in its full strangeness.
5. **The verdict is computed (WS5)**, the fork left open as a matter of theorem discipline, and no name a term.

## The five discipline checks (protocol §0.4–§0.6b), the review runs each

- **Fork-open (§0.4, the central check).** No theorem, definition, or discharged obligation classifies an out-of-image import (no `Origin`/`genealogy`/`isGiven`/`isChosen`/`classify` term). The defect LOCATES (a membership denial, up to the chosen equivalence), never SORTS.
- **Genuine-connection (§0.5).** Both orders proved non-trivial (neither discrete = equality nor indiscrete = all-related), the labelled order's antitone reference position exercised non-vacuously; and a proved non-identity round trip (not an isomorphism in disguise).
- **Exogeneity (§0.6).** The mint's non-recoverability from the plain relating is a proof term (`ws2_mint_exogenous`), not a docstring gloss.
- **Structural-defect (§0.6a).** The out-of-image witness is excluded by its own label (the diagonal link), not by cardinality/universe/typing.
- **Names-not-terms (§0.6b).** No declaration named `given`/`chosen`/`consciousness`/`God`/`choice`/`compass`/`origin`/`genealogy` (as content); the interpretive vocabulary lives in prose only.

## The strip test (§0.3), per payoff

Delete "duality/fit/wound/rescue/given/chosen/mint/factory/connection/defect" and check the residue:
- **Transport** should strip to: for every inspection, the minted two-region coalgebra fails `Recoverable`, by `ws2_residue_distinct` (the diagonal).
- **Connection** should strip to: a `GaloisConnection` between two non-trivial preorders whose interior round trip is not the identity on a named element.
- **Defect** should strip to: a `¬ Recoverable` coalgebra outside the mint's image (up to the equivalence), excluded by the diagonal link.

## The terminal fork

Dual / Total / Disconnected / Partial (Circular for the audit). The verdict must be a FUNCTION of the WS1–WS4 payoffs (never hand-set), honestly labelled, matching its object, and neither assumed nor reached by closing the fork or faking the connection.

## Axiom bar (charter §7)

Sorry-free and no new axioms: every headline rests only on `propext` / `Classical.choice` / `Quot.sound`.

---

*Prose-free by construction. The motivating reading (the wound and the rescue, the fit, the octave figure) is in `charter.md` §1/§8 and is NOT part of the review seed. `spec/README.md` §0 restates the disciplines with light gloss; the strictly blind seed is this file plus the built code and the design signatures.*
