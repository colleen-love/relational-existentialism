# T2.07 — Self-relation is the trace (D1, realized)

> *Node `T2.07`.* Lean: `RelExist.Trace` (`paper-2/formal/Scratch/Trace.lean`); imports mathlib only.

D1 says the self-relating of a system **is** the trace — output returned to input. This node realizes that as
a greatest fixed point (`σ = Tr` as `gfp`), the finite, internal-time-off instantiation of the canonical D1,
whose general form is the modular flow (the trace its infinite-temperature limit —
[`../../theory/spec/AXIOMS.md`](../../theory/spec/AXIOMS.md), D1).

Narrative: [`paper-two.md`](paper-two.md).
