/-
# `paper-1/` — a self arises from the axioms (new relational arena)

Paper one on the **quantaloid / allegory** arena (relations primitive, `Q` the value-object). This root
gathers the exploration of the theorem debt (`spec/03-theorem-debt.md`):

* `Paper1.Arena` — the vocabulary: `Q`-valued relations, converse, composition, the inclusion order. No self.
* `Paper1.Existence` — **Theorem 1 (proved):** a fixed self-relation exists (relational Knaster–Tarski),
  invoking only completeness — neither A1 nor A2.
* `Paper1.Seam` — **Theorem 6a (proved, 0 axioms):** reflexive opacity (Lawvere; arena-independent).
* `Paper1.Frontier` — Theorems 2–5, 7: the order-proxy of A3's raising is proved; the quantitative content
  (raising bound, sparsity, differentiation, the stability dichotomy, recovery) is **obstructed pending `Q`'s
  trace** — reported, not forced.

The prior (traced-SMC) edifice is archived under `archive/archived/` as structural reference.
-/
import Paper1.Arena
import Paper1.Existence
import Paper1.Seam
import Paper1.Frontier
import Paper1.TraceSeam
import Paper1.TypeIII
import Paper1.RaisingDynamics
import Paper1.Arrow
import Paper1.ModularInterface
import Paper1.ModularBuild
