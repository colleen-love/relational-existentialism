# paper-1 — a self arises from the axioms (skeleton, new arena)

**Skeleton.** Paper one is being rebuilt from scratch on a **new foundation** — a **quantaloid / allegory in
which relations are the primitive arrow** and `Q` is the value-object — replacing the earlier traced-SMC +
operator-representation arena. The spec docs below are **placeholders**; spec II.1 fills them. `formal/` is an
empty `Paper1` root; no theorems yet.

- Scope + plan (stubs): [`spec/00-domain.md`](spec/00-domain.md), [`spec/01-signature.md`](spec/01-signature.md),
  [`spec/02-axioms.md`](spec/02-axioms.md), [`spec/04-provenance.md`](spec/04-provenance.md).

**The prior edifice is preserved, not deleted.** The earlier development (the traced-SMC papers, the theory
and foundation layers, the conservation/sparsity frontier) is archived under
[`../archive/archived/`](../archive/archived) as **structural reference**, catalogued in
[`../archive/archived/INDEX.md`](../archive/archived/INDEX.md). It is the same theory in a representation we
outgrew (the seam is Lawvere either way; conservation is conservation either way), to be **mined and
re-grounded relationally** where relevant — not resurrected wholesale.

See [`../STRUCTURE.md`](../STRUCTURE.md) for the layout and the arena change.
