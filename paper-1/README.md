# paper-1 — a self arises from the axioms (skeleton)

The foundational paper of the second cycle: **how a self emerges from the canonical axioms** `{A1, A2, A3,
D1}`. **Skeleton only** — the spec docs are stubs and `formal/` is empty (`Paper1.lean`); the theorems
(`T1.<n>`) are deferred to the **formalization pass** that follows the rotation.

- Scope + plan: [`spec/00-domain.md`](spec/00-domain.md), [`spec/01-signature.md`](spec/01-signature.md),
  [`spec/02-axioms.md`](spec/02-axioms.md).
- Provenance + pin: [`spec/04-provenance.md`](spec/04-provenance.md).
- The canonical axioms it builds on: [`../theory/spec/AXIOMS.md`](../theory/spec/AXIOMS.md).

The arc: **paper-1** (a self arises) → [`paper-2`](../paper-2) (the self in time: arrow, energy, the seam) →
[`paper-3`](../paper-3) (the self's modular / thermal structure) → paper-4 (conservation / presence,
frontier in [`scratch/`](../scratch)). See [`../STRUCTURE.md`](../STRUCTURE.md).
