# P1.9 — Directed attention

> *Node `P1.9`.* Lean: `RelExist.Attending` (`paper-1/formal/Scratch/Attending.lean`); imports **P1.8**
> (`Decoherence`).

Attention is directed self-relation in the matrix model: the channel that decoheres (P1.8) read as an
*attending*. It feeds the seam-protection node (**P1.12**) — the proof that the self-block cannot fully
decohere — and the quantum-seam/orientation chain (P1.10 → P1.11) that yields the arrow (P1.13).

Narrative: [`paper-one.md`](paper-one.md). Axioms: [`../../theory/spec/AXIOMS.md`](../../theory/spec/AXIOMS.md).
