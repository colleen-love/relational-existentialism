# 04 — Provenance (stub)

> **Skeleton — to be filled in the formalization pass.**

Paper one (*a self arises from the axioms*) has **no formal content yet** — `formal/` is the empty `Paper1`
root, the spec docs are stubs. The provenance ledger and the per-result tier accounting will be written with
the theorems in the formalization pass.

## Version pin — the canonical axiom layer

Paper one will be proved against the **canonical axiom layer** `Theory.Axioms` (prose:
[`../../theory/spec/AXIOMS.md`](../../theory/spec/AXIOMS.md)) — the four canonical axioms `{A1, A2, A3, D1}`,
with A3 a process and the self its derived fixed point. The layer changes only backward-compatibly, so the
pin (recorded here once paper one has content) will keep its meaning.
