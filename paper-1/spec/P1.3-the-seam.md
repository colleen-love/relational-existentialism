# P1.3 — The seam

> *Node `P1.3`.* Lean: `RelExist.Seam` (`paper-1/formal/RelExist/Seam.lean`); imports **P1.2** (`Relating`).

The seam is the self-relation a system cannot completely view from outside — the un-attendable block that the
first-person surplus protects. It is the structural heart paper one's headline results route through: the
arrow (P1.13) is the orientation self-relating cannot escape, and the conserved remainder at the seam is
energy (T.3). Fourth in the spine is **P1.4** (`SeamBridge`).

Narrative: [`paper-one.md`](paper-one.md). Axioms: [`../../theory/spec/AXIOMS.md`](../../theory/spec/AXIOMS.md).
