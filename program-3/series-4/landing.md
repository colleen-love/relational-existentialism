# Series 3.4 landing note

Verdict: **curved** — computed by `ws5_verdict_tied`, every flag bound to its justifying theorem. The capstone lands: the flow couples the grain to the metric by an exact universal law.

## Theorems

- `ws1_dist_self`, `ws1_metric_reads_state` — the dynamical metric is genuine.
- `ws1_grain_underdetermines` — two states with identical charges everywhere and different distances: the static grain does not determine the metric. Series 2.13's INERT recovered as the reason the law must be dynamical.
- `ws2_metric_moves` — a transport stretches one distance to the sentinel and contracts another to one: the metric is dynamical.
- `ws3_attention_is_proximity` — attending is being close (distance at most one), for every state. Structural.
- `ws4_flux_curves` — the flux-curvature law: for every state and applicable move, transporting attention toward a relatum lowers its charge by exactly one and brings it within unit distance. Concentration of the conserved quantity contracts distance — an exact coupling of grain flux to metric change, universal over the dynamics.
- `ws5_verdict_tied` / `ws5_verdict_eq`.

Build green, gate green, standard-three axioms or fewer (`ws5_verdict_eq` on none), sorry-free, no vacuous audits. The metric is a fresh definition at this carrier, named fresh with the reason stated (2.13's `pathDist` lives on four relata); the bridge discipline is satisfied by disclosure, not by a bridge that cannot exist.

## For the end-of-arc review to press on

- The flux-curvature law couples charge change to a distance *bound* (≤ 1), not to a distance *delta*; check the prose does not claim a full field equation. The docstrings scope it as "the shape of gravity in the model's own key."
- `ws1_grain_underdetermines` and `ws4_flux_curves` together say: no static functional dependence, an exact dynamical law. Check the pair is presented as exactly that.
