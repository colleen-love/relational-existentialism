/-
`program-3/series-4/formal/P3S4.lean`

Program 3 Series 4 (3.4), the curve — the aggregator root, and the capstone of the arc.

The metric and the grain are readings of one dynamical state, and the flow couples them by an exact law:
the metric is genuine and the static grain underdetermines it (ws1, keeping Series 2.13 honest); the
metric moves under the flow (ws2); attending is being close (ws3); and transporting attention toward a
relatum lowers its charge by exactly one while bringing it within unit distance (ws4) — concentration of
the conserved quantity contracts distance, for every state and every applicable move. The verdict is tied
(ws5).

The series boundary import is `P3S3` (in `P3S4.ws1`). `P3S4.AxiomCheck` runs the axiom pass.

Sorry-free; axiom-clean beyond Mathlib's standard `propext` / `Classical.choice` / `Quot.sound`.
-/
import P3S4.ws1
import P3S4.ws2
import P3S4.ws3
import P3S4.ws4
import P3S4.ws5
