# Series 3.1 landing note

Verdict: **arrow** — computed by `ws5_verdict_tied`, every flag bound to its justifying theorem. The program's central bet is cashed: irreversibility emerges from coarse-graining the reversible flow.

## Theorems

- `ws1_summary_symmetric` — the direction-erasing summary is direction-blind, for every state.
- `ws1_state_count` — the micro world has 512 states, computed structurally; the loss has a scale.
- `ws2_summary_lossy` — two distinct microstates, one summary (the one-edge pair).
- `ws2_fiber_multiple` — three distinct states in one named fiber (edge forward, back, mutual): the loss counted on a fiber, not asserted.
- `ws2_no_decoder` — no function recovers the microstate from the summary.
- `ws3_micro_loses_nothing` — every transport is injective: the microdynamics destroys no information.
- `ws3_observation_always_lossy` — for every move, "flow then summarize" is non-injective: the loss is permanent, structural (transport bijectivity carries the lossy pair through every move).
- `ws4_summary_is_p2_sym` — the summary is Program 2's symmetric relating (`P2S0.sym`), by theorem.
- `ws4_charge_erased` — the summary erases the charge: what the coarse-graining forgets is the ledger's quantity.
- `ws5_verdict_tied` / `ws5_verdict_eq` — the tied verdict.

Build green, gate green, all headlines on the standard three axioms or fewer (`ws1_summary_symmetric` and `ws4_summary_is_p2_sym` on two; `ws5_verdict_eq` on none), sorry-free, no `native_decide`, no vacuous audits.

## Deviations and notes

None from the outline. Built in the program seat under the revised protocol; awaits the gate review.

## For the gate review to press on

- The arrow is stated at one summary (direction erasure). The claim is "an arrow emerges from this coarse-graining," not "from every coarse-graining" — check the docstrings hold that line.
- `ws3_observation_always_lossy` uses one lossy pair carried through every move; the fiber count (three) is separate. Check the two are not conflated in prose.
