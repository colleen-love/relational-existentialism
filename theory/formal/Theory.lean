/-
# `theory/` — the living frontier (handoff XIII)

The canonical, theory-specific development that is already *not* paper one: the modular A1
(`Theory.ModularFlow`), the generative A3 (`Theory.MutualCoupling`), A2-priority (`Theory.Priority`), the
open-system capstone unifying the modular clock and the dissipative arrow into one generator
(`Theory.OneGenerator`), the einselection principle and the derived definition of presence
(`Theory.Einselection`), plus the shared base they consume, forked up from `paper-1` on genuine demand
(`Theory.We`, `Theory.RotatingSpectrum`, `Theory.BandCoincidence`, `Theory.BandFromAxioms`). Imports only
`theory/` + `foundation/`; references no paper. The only root that moves freely.
-/
import Theory.ModularFlow
import Theory.MutualCoupling
import Theory.Priority
import Theory.OneGenerator
import Theory.Einselection
